2026-05-23 - Issue #5: V8 runtime foundation tracer

Selected #5 as the highest-priority ready-for-agent feature because it establishes the v8 runtime/theme foundation needed by the later catalog, autocomplete, palette, and chrome work.

Implemented:
- Pinned runtime Aksel packages to 8.11.0 and removed the direct runtime @navikt/ds-tokens dependency.
- Switched the app and sandbox to import current Aksel CSS from @navikt/ds-css, then regenerated the checked-in sandbox bundle.
- Kept dark as the default theme and verified the default preview renders after switching between light and dark.
- Updated export metadata/default intro copy to Aksel v8 and accepted THEME_UPDATED sandbox messages so theme toggles do not warn as invalid messages.
- Fixed the sandbox development CSP for 127.0.0.1 so local browser verification has no CSP errors.

Verification:
- npm run typecheck
- CI=1 npm run test
- Browser check at http://127.0.0.1:5173/aksel-arcade/ for default preview render and light/dark toggle

2026-05-23 - PR #14 review follow-up

Fixed a build-only v8 token type regression before merge:
- Replaced old numeric Box/BoxNew padding props with v8 `space-*` tokens.
- Replaced `borderRadius="medium"` with the v8 radius token `borderRadius="8"`.
- Regenerated the sandbox source map so it matches the checked-in sandbox source.

Verification:
- npm run build
- npm run typecheck
- CI=1 npm run test

2026-05-23 - Issue #6: Catalog-backed starter path

Selected #6 as the highest-priority ready-for-agent feature because it provides the shared catalog foundation needed before the palette group/status work (#7), autocomplete contract work (#8), and later built-in/export follow-ups.

Implemented:
- Added a checked-in Aksel v8 starter catalog with current Layout and Components entries, prop metadata, token/value metadata, statuses, snippets, import guidance, and docs URLs.
- Wired the active component palette to prefer catalog entries for the starter subset while preserving existing non-starter manual entries.
- Wired editor tag snippets and autocomplete prop/value metadata to prefer the same catalog source for the starter subset.
- Added catalog regression tests covering the catalog interface, palette adapter, snippet adapter, and autocomplete metadata path.

Verification:
- npm run typecheck
- CI=1 npm run test -- --run

### 2026-06-16 - Issue #272: Expose active Arcade project read resources over MCP

Implemented:

- Selected `#272` as the highest-priority open `ready-for-agent` feature from `#272`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes; `#271` is already complete, and `#272` is the main active-project discovery layer that later MCP resources, mutations, and preview evidence depend on.
- Added a dedicated Desktop MCP project-resource bridge from Electron main to the renderer without reopening or focusing Desktop Arcade: `desktop/main.cjs` now routes `arcade://project/*` reads over a narrow request/response IPC path, validates renderer replies, times out cleanly, and returns `project-unavailable` or `source-not-found` resource errors instead of exposing arbitrary renderer access.
- Extended `desktop/preload.cjs` and `src/services/shellCapabilities.ts` with a narrow `setDesktopMcpProjectResourceReadHandler` preload API, then registered a stable renderer-side handler from `src/hooks/useDesktopMcpProjectResourceBridge.ts` so MCP reads always use the latest project/theme/diagnostics state without effect re-registration churn.
- Added `src/services/desktopMcpProjectResources.ts` plus the small protocol/adapter helpers to shape the real project resources:
  - `arcade://project/manifest` now returns compact JSON with project name, deterministic `projectRevision`, ordered pages, start/active page ids, global/page source resource links, byte/line metadata, and valid/stale page references grouped by Global config and page.
  - `arcade://project/preview-context` now returns only the saved viewport/theme preview preferences.
  - `arcade://project/diagnostics` now returns compact issue summaries for compile errors, runtime errors, and stale page references, including page ids/names, resource URIs where they can be inferred, line/column, and bounded details/snippets.
  - Dynamic source URIs (`arcade://project/source/global/*` and `arcade://project/source/pages/{pageId}/*`) now return pure source text through MCP.
- Kept issue `#273` out of scope: `arcade://desktop/*` resources still stay on the placeholder `not-yet-implemented` path while `resources/list` remains the same six stable direct URIs from `#271`.
- Added focused regression coverage in `tests/unit/services/desktopMcpProjectResources.test.ts`, `tests/unit/services/desktopMcpProjectResourceAdapter.test.ts`, and extended `tests/unit/services/desktopMcpServer.test.ts` so project resource reads, dynamic source reads, revision changes, stale references, and `project-unavailable` errors are all covered.

Verification:

- npm install
- npm run typecheck
- npm run lint
- CI=1 npm run test
- npm run build
- Browser/console smoke check at `http://127.0.0.1:4173/aksel-arcade/` using `npm run preview -- --host 127.0.0.1 --port 4173`, confirming the app shell and live preview render and the browser console stays free of errors/warnings

Next note:

- `#273` can now fill in the three `arcade://desktop/*` resources on top of the same MCP `resources/read` plumbing without touching the new project-resource contract.
- `#275` should reuse `projectRevision`, the dynamic source resource URIs, and the narrow main→renderer request pattern rather than introducing a second state-discovery path for `apply_changes`.

### 2026-06-09 - Issue #190: Multi-page active-page sync across preview, panel, and editor

Implemented:

- Selected #190 as the highest-priority `ready-for-agent` feature from #185-#191 because it closes the core multi-page authoring loop between the panel, editor, and preview, and it directly unblocks #191 stale-reference diagnostics.
- Read issue #181, issue #190, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Asked for a rubber-duck critique before implementation; it pushed the plan toward structured page-level error attribution and a safer preview bridge, so the final implementation keeps `pageId` metadata on preview errors and lets the sandbox queue host-requested page changes across recompiles instead of trusting the stale runtime.
- Extended the preview message contract with `NAVIGATE_TO_PAGE` / `PREVIEW_PAGE_CHANGED`, exposed a pages-aware runtime bridge from `transpileProjectSource`, and updated `public/sandbox.html` so host page selections are queued until the current runtime can actually resolve that page id.
- Updated `src/components/Preview/PreviewPane.tsx` and `src/components/Preview/LivePreview.tsx` so host page selection drives preview navigation, preview-side navigation updates the host `activePageId`, and selecting `Global config` swaps the iframe for a clear no-preview placeholder without dropping compile/runtime error handling.
- Added page-scoped error attribution (`CompileError.pageId` / `RuntimeError.pageId`) and surfaced it in `src/components/Editor/PagePanel.tsx` so the affected page row shows an error indicator while later stale-reference work can layer on top of the same status surface.
- Added regression coverage in `tests/integration/multi-page-preview-sync.test.tsx`, extended `tests/integration/multi-page-preview-runtime.test.tsx` to assert structured compile-error page attribution, and updated `tests/unit/utils/security.test.ts` for the new preview message types.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- npm test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4178/aksel-arcade/` for enabling experimental multi-page authoring, Add page making Page 2 the active editor/preview target without console warnings, Global config placeholder rendering, and no console errors/warnings during the verified flow

Next note:

- #191 can build directly on the structured `pageId` error attribution and the `PREVIEW_PAGE_CHANGED` / `NAVIGATE_TO_PAGE` contract. Keep host-driven page selection going through the preview bridge; the sandbox queue now absorbs source-shape changes safely, which avoids the stale-runtime navigation warnings that appeared when Add page and preview sync happened in the same edit cycle.

### 2026-06-09 - Issue #189: Multi-page page lifecycle actions in the page panel

Implemented:

- Selected #189 as the highest-priority `ready-for-agent` feature from #185-#191 because it is the next concrete multi-page authoring step after #184, lets authors actually manage pages from the new panel, and unblocks later stale-reference/delete-impact work more directly than share, autocomplete, or agent-bridge follow-ups.
- Read issue #181, issue #189, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Asked for a rubber-duck critique before implementation; it caught two real blind spots in the first plan — Add page needed to flip `selectedEditTarget` back to `page`, and inline rename needed UI-side empty-name validation instead of relying on the service throw path — so the final implementation keeps add-page orchestration in `EditorPane` and guards rename in the panel UI.
- Added explicit structural page actions to `src/hooks/useProject.tsx` (`createPage`, `renamePage`, `deletePage`, `setStartPage`) so `src/services/projectSource.ts` remains the single source of truth for lifecycle rules while structural edits still update `lastModified` and preserve working-copy preferences.
- Expanded `src/components/Editor/PagePanel.tsx` and `src/components/Editor/PagePanel.css` with a pinned **Add page** action, per-row action menus, inline rename state with Enter/Esc plus save/cancel buttons, visible start-page status, delete confirmation, and disabled start/delete actions when those lifecycle operations are not allowed.
- Wired `src/components/Editor/EditorPane.tsx` so Add page always moves the editor back to the page target before creating the new page, keeping the selected-edit-target behavior from #184 coherent with the new lifecycle flow.
- Added focused regression coverage in `tests/integration/multi-page-page-panel.test.tsx` for add/rename/set-start/delete/last-page protection, added a last-page delete guard test in `tests/unit/services/projectSource.test.ts`, and updated the manual `AppContext` test stub in `tests/integration/app-share-load-ui.test.tsx` for the expanded project actions.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4175/aksel-arcade/` for enabling experimental multi-page authoring, Add page, inline rename with Enter, Set as start page, delete confirmation, last-page delete protection, and no console errors

Next note:

- #191 can build directly on the new page action menus/dialog state in `PagePanel` and the explicit structural actions in `useProject` when it adds delete-impact counts and stale-reference panel indicators.
- Local Vite ports may auto-increment when earlier sessions are still running; the successful browser verification for this worktree ran on `4175`, not the initially requested `4173`.

### 2026-06-09 - Issue #184: Multi-page page panel and selected edit target

Implemented:

- Selected #184 as the highest-priority `ready-for-agent` feature from #184-#191 because it is the first user-facing multi-page workflow after #183 and it unblocks both #189 page lifecycle actions and #190 preview/editor sync.
- Read issue #181, issue #184, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Asked for a quick rubber-duck critique before implementation; it caught two blind spots in the original plan — editor writes/snippet insertion still routed through the active page, and page mode needed to remain the effective editor target while multi-page is disabled — so the final design added shared edit-target routing helpers instead of only read-side selection.
- Added working-copy preferences for `pagePanelOpen` and `selectedEditTarget`, defaulted older saved working copies safely, and threaded both through settings restore and autosave.
- Added `getSourceForEditTarget`, `updateSourceForEditTarget`, and `resolveSelectedEditTarget` in `src/services/projectSource.ts` so the editor and snippet insertion can read/write either the active page or `globalConfig` without duplicating branching logic.
- Added a push-drawer page panel in the editor with a header toggle, permanent `Global config` row, ordered page rows, and persisted open/closed state whenever experimental multi-page authoring is enabled.
- Bound the JSX/Hooks tabs to the selected edit target while keeping preview behavior on the active page for now: choosing `Global config` swaps the editor source without changing the active page, and choosing a page row updates both the active page and the editor target.
- Added regression coverage in `tests/integration/multi-page-page-panel.test.tsx`, `tests/unit/services/projectSource.test.ts`, and `tests/unit/services/storage.test.ts`, and updated autosave/share-decode tests for the expanded working-copy preference shape.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4174/aksel-arcade/` for enabling experimental multi-page authoring, page panel visibility, Global config selection, page panel hide/show, and no runtime console errors

Next note:

- #189 should build on the persisted `selectedEditTarget` / `pagePanelOpen` working-copy preferences and the edit-target helpers in `src/services/projectSource.ts` rather than adding a second editor-routing layer. #190 can reuse the same split between persisted active page and selected edit target when `Global config` has focus.
- npm run lint
- npm run build
- Browser check at http://127.0.0.1:5174/aksel-arcade/ for default render, catalog-backed palette entries, runnable HStack snippet insertion, and no console errors.

Next note:
- #7 and #8 should extend `src/data/akselCatalog.ts` rather than adding new parallel component/snippet/prop lists. Existing manual data is still present for non-starter entries, but the starter subset now has one catalog source of truth.

### 2026-06-09 - Issue #188: Multi-page preview runtime for the canonical project source

Implemented:

- Selected #188 as the highest-priority `ready-for-agent` feature from #185-#191 because it turns the canonical multi-page project model into an actually navigable preview runtime and directly unblocks #190 active-page sync plus the later stale-reference follow-up.
- Read issue #181, issue #188, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Asked for a rubber-duck critique before implementation; it caught three real blind spots in the first plan — designer-mode page JSX still needed per-page wrapping inside the new page IIFEs, delegated anchor navigation had to use `closest('a')`, and bare Global config JSX needed an explicit compile error instead of colliding with the runtime `App` component — so the final runtime keeps page authoring ergonomic without treating Global config as a screen.
- Added `transpileProjectSource` in `src/services/transpiler.ts` so multi-page preview compiles Global config plus every page into one runtime with shared Global config scope, isolated per-page IIFEs, start-page boot, injected `goToPage()` / `currentPageId`, fresh page remounts on navigation, and delegated page-id `href`/`to` navigation inside the preview.
- Kept `transpileCode` as the single-page path and updated `src/components/Preview/PreviewPane.tsx` to switch between single-page and multi-page transpilation based on the experimental setting, so multi-page edits now recompile from canonical `project.source` without pulling preview/panel synchronization from #190 into this issue.
- Added `tests/integration/multi-page-preview-runtime.test.tsx` covering start-page rendering, shared Global config scope persistence, page-local remount/reset semantics, page-id anchor navigation, and the bare-Global-config compile guard.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4176/aksel-arcade/` for enabling experimental multi-page authoring, adding Page 2, preview start-page boot, `goToPage('page02')` button navigation to the Details page, `Link to="page01"` navigation back to Start, and no console errors

Next note:

- #190 should build on `transpileProjectSource` rather than reintroducing active-page-only compilation. The runtime already owns start-page boot and in-preview navigation; #190 only needs preview/host synchronization plus the Global config placeholder/error-sync layer on top.
- Bare Global config JSX now fails fast on purpose in multi-page mode. Shared components/helpers belong there, but screens still belong in Arcade pages.

### 2026-06-09 - PR #195 review follow-up for Issue #188

Fixed before merge:

- Preserved the current preview page across multi-page recompiles by threading a preview-session key into `transpileProjectSource`, persisting the in-preview page id inside the sandbox runtime, and rehydrating that page on the next execution instead of always snapping back to `startPageId`.
- Added combined-source block mapping in `src/services/transpiler.ts` so multi-page compile errors point back to the originating block (`global config` / `pageXX JSX|Hooks`) with corrected relative line numbers instead of generated bundle offsets.
- Extended `tests/integration/multi-page-preview-runtime.test.tsx` with regressions for page persistence across recompiles and block-level compile error mapping.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test -- --run
- Browser check at `http://127.0.0.1:4177/aksel-arcade/` for multi-page preview navigation to Page 2, staying on Page 2 after editing Page 1, corrected labeled compile errors during an induced broken Page 2 edit, and no console errors on a clean reload

### 2026-06-16 - Issue #274: Baseline targeted Preview evidence capture

Implemented:

- Selected `#274` as the highest-priority open `ready-for-agent` feature from `#274`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes; it is the first unblocked Preview-capture slice and the foundation that later evidence-layer and interaction follow-ups build on.
- Added a dedicated Desktop MCP preview-capture bridge across `desktop/main.cjs`, `desktop/preload.cjs`, `src/services/shellCapabilities.ts`, `src/services/agentBridge.ts`, `src/App.tsx`, and `src/hooks/useDesktopMcpProjectResourceBridge.ts` so `capture_preview_evidence` routes through the active Desktop renderer without focusing the window or mutating the visible project session.
- Added `src/services/desktopMcpPreviewCaptureProtocol.ts`, `src/services/desktopMcpPreviewCaptureAdapter.ts`, `src/services/desktopMcpPreviewCapture.ts`, and `src/services/desktopMcpPreviewCaptureSandbox.ts` to validate/default capture requests, resolve target pages from the active project, run each capture through a fresh hidden sandbox iframe, and finalize the short-lived manifest / frame / screenshot MCP resources.
- Extended `src/services/previewEvidence.ts`, `public/sandbox.html`, and `src/types/messages.ts` so the sandbox capture path now supports baseline `screenshot` and `frame` layers, screenshot scopes `viewport` / `full_page` / `region`, accessibility-or-selector region targeting, SVG screenshot output, and structured capture failures for invalid targets or render timeouts.
- Updated `desktop/mcpServer.cjs` to expose the live `capture_preview_evidence` capability status, keep short-lived `arcade://preview/captures/{captureId}/*` resources in an expiring in-memory store, and serve manifest/frame/screenshot reads independently from the durable `arcade://project/*` resource path.
- Added regression coverage in `tests/unit/services/desktopMcpServer.test.ts` for routed capture success, expired capture-resource reads, and updated capability text, plus the small hook test fix in `tests/unit/hooks/useDesktopMcpProjectResourceBridge.test.tsx` for the new preview-iframe bridge dependency.

Verification:

- npm run typecheck
- CI=1 npm test -- --run
- npm run lint
- npm run build
- Browser/console smoke check at `http://127.0.0.1:4193/aksel-arcade/` using `npm run preview -- --host 127.0.0.1 --port 4193 --strictPort`, confirming the default preview renders and the browser console stays free of errors and warnings

Next note:

- `#277` should build on the existing isolated hidden-sandbox capture bridge and expiring `arcade://preview/captures/{captureId}/*` store instead of trying to inspect the human-visible preview iframe directly; the production sandbox still runs without `allow-same-origin`, so parent-side DOM scraping is the wrong architecture for later evidence layers too.
- `capture_preview_evidence` now intentionally stops at baseline `screenshot` + `frame` resources. `accessibility` and `dom_layout_style` layers are still explicitly unimplemented and should layer onto the same manifest/store contract instead of inventing a second capture path.

2026-05-23 - Issue #7: Palette groups, statuses, and docs links

Selected #7 as the highest-priority ready-for-agent feature because it is unblocked by #6 and unlocks later contextual icon autocomplete (#9) and v8 chrome work (#12).

Implemented:
- Extended the checked-in Aksel catalog with all current icon metadata from `@navikt/aksel-icons`, an experimental FormProgress entry, and legacy markers for BoxNew, Stack, and Grid.
- Updated default palette discovery to expose Layout, Components, and Icons groups while hiding legacy entries from palette and editor autocomplete suggestions.
- Added import-free icon snippets and Docs links to palette cards, then removed redundant Layout/Component/Icon/Experimental card tags because the tabs already define the active group.
- Reused catalog docs/package metadata in export component detection so exported metadata can point icons to @navikt/aksel-icons and Aksel docs.
- Added regression coverage for palette groups, experimental labels, icon snippets, docs metadata, and hidden legacy discovery.
- Fixed Add Component modal polish bugs: initial focus now goes to the search field, the modal container focus border is suppressed, list/card focus rings have inset room, the first card is no longer clipped, and the tablist scrollbar strip between tabs and list is hidden.

Verification:
- npm run typecheck
- CI=1 npm run test -- --run
- Browser smoke checks at local Vite URLs for 900+ Icons palette entries, icon insertion/rendering, hidden legacy layout entries, no redundant card tags, search initial focus, focus-ring spacing, hidden tablist scrollbar strip, and no console errors.

Next note:
- #8 should keep using `AKSEL_SNIPPETS`/catalog metadata for component autocomplete, but icons are intentionally palette-only until #9 adds contextual icon autocomplete.

2026-05-23 - Issue #7 follow-up: Add Component modal fixes

Updated after UX review:
- Replaced the four manually listed icon catalog entries with generated catalog entries from `@navikt/aksel-icons/metadata`, so the Icons tab now reflects all installed Aksel icons.
- Removed redundant Layout, Component, Icon, and Experimental tags from cards; the active tab now carries that categorization.
- Fixed modal focus/layout polish: search receives initial focus, the modal container no longer shows the pink/purple focus border, the search field and card focus rings have inset room, the first card is not clipped, and the tablist scrollbar strip no longer appears as a rectangle between tabs and the list.
- Updated PR #16 description and PRD #4 implementation progress to reflect these follow-up fixes.

Verification:
- npm run typecheck
- CI=1 npm run test -- --run
- Browser smoke checks at local Vite URLs for 900+ icon entries, icon insertion/rendering, no redundant tags, initial search focus, focus-ring spacing, hidden tablist scrollbar strip, and no console errors.

2026-05-24 - Issue #8: Aksel-aware autocomplete contract

Selected #8 as the highest-priority ready-for-agent feature because it is the foundational author-facing autocomplete contract and unblocks contextual icon autocomplete (#9). Worked only on #8.

Implemented:
- Moved the editor autocomplete logic into `src/services/akselAutocomplete.ts` so component tags, subcomponents, props, and prop values use the checked-in Aksel catalog directly.
- Added multi-line JSX opening-tag parsing so formatted tags still suggest props and values.
- Kept default tag suggestions scoped to current/experimental Layout and Component catalog entries, excluding legacy entries and icons until #9 adds contextual icon autocomplete.
- Added catalog-backed value suggestions for enum props, v8 spacing tokens, Box styling tokens (`background`, `borderColor`, `borderRadius`, `borderWidth`, `shadow`), and `data-color`.
- Added user-facing regression coverage in `tests/unit/akselAutocomplete.test.ts` for tag, subcomponent, multi-line prop, enum, spacing, Box token, and `data-color` contexts.

Verification:
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing `InspectionPopover` hook dependency warning)
- Browser smoke check at http://127.0.0.1:5176/aksel-arcade/ for app render and console output.

Next note:
- #9 should build on `src/services/akselAutocomplete.ts` for icon-aware contexts instead of reintroducing broad icon suggestions into normal component tag or prop lists.

2026-05-24 - Issue #8 follow-up: Complete Aksel autocomplete coverage

Updated after autocomplete coverage review:
- Replaced the starter-catalog-only autocomplete surface with generated metadata from `aksel.nav.no/llm.md`, covering documented primitives, components, subcomponents, legacy entries, and docs-backed prop/value tables.
- Added all installed `@navikt/aksel-icons` icons to JSX tag autocomplete and added SVG icon prop/value completions.
- Fixed `Box` prop completion so `asChild` is suggested, and kept catalog token fallbacks for spacing/data-color/Box values where docs types are not literal-expanded.
- Added regression coverage proving all generated docs entries and all 949 installed icons are suggested, plus representative props/values for Box, DataGrid, Combobox, Accordion, and icons.

Verification:
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing `InspectionPopover` hook dependency warning)
- Browser smoke check at http://127.0.0.1:5177/aksel-arcade/ for render, console output, and visible `Box` `asChild` autocomplete.

Next note:
- Keep `src/data/akselAutocompleteData.ts` generated with `node scripts/generate-aksel-autocomplete-data.mjs > src/data/akselAutocompleteData.ts` when Aksel docs/package types change; do not hand-edit the generated autocomplete metadata.

2026-05-24 - PR #17 review and merge history

Reviewed, fixed, and merged PR #17:
- `87051eb` implemented the initial #8 autocomplete contract for catalog-backed tags, subcomponents, props, prop values, v8 spacing, Box tokens, and `data-color`.
- `faa3e84` completed the coverage follow-up with generated `aksel.nav.no/llm.md` metadata, documented primitives/components/subcomponents/legacy entries, all 949 installed Aksel icons, `Box` `asChild`, and icon prop/value completions.
- CodeQL then blocked the PR on the generator: broad HTML tag stripping and direct network-data-to-file output.
- `17068a9` fixed those findings by normalizing docs text without multi-character tag stripping and making `scripts/generate-aksel-autocomplete-data.mjs` write generated TypeScript to stdout.
- Re-ran `npm run typecheck`, `CI=1 npm run test`, `npm run lint`, and a browser smoke check at the local Vite URL; GitHub CodeQL checks refreshed green.
- Merged PR #17 on 2026-05-24 and deleted the remote branch `sjur-gr/ready-feature-pr-2`.

Next note:
- Future autocomplete metadata refreshes should start from `master`, run `node scripts/generate-aksel-autocomplete-data.mjs > src/data/akselAutocompleteData.ts`, and avoid hand-editing generated docs/icon metadata.

2026-05-24 - Issue #9: Contextual icon autocomplete

Selected #9 as the highest-priority ready-for-agent feature because #7 and #8 had landed, making it fully unblocked, and because the current autocomplete surface still let hundreds of icons flood normal tag suggestions.

Implemented:
- Removed icon catalog entries from default JSX tag completion so normal component/primitives suggestions stay focused.
- Added contextual icon suggestions for icon prop JSX/expression contexts such as `icon={<...` and `icon={...`.
- Added icon suggestions for icon-looking JSX tag identifiers, while keeping short prefixes like `<Pl` and real component matches like `<Link` from pulling in icon results.
- Preserved SVG prop and prop-value completions for actual icon tags.
- Kept sandbox icon globals unchanged so all Aksel icons remain available to import-free prototypes at runtime.
- Added engine-level regression coverage for default icon exclusion, icon prop contexts, icon-looking tag identifiers, normal prop-list exclusion, and icon prop/value behavior.

Verification:
- npm run typecheck
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5178/aksel-arcade/ for contextual autocomplete behavior and no runtime console errors.

Next note:
- #10 should focus on import stripping and compatibility aliases without reintroducing automatic rewrites. #11 remains blocked by #10 per the PRD dependency list.

2026-05-24 - Issue #9 follow-up: DogHarnessIcon tag autocomplete

Fixed after dev testing around `DogHarnessIcon` tag completion:
- Kept icon completion scoped to valid JSX tag contexts such as `<DogH`, `<DogHarnessIcon`, and `<Box><DogH`.
- Changed icon completion snippets to insert accessible visible-icon markup when accepted: `<DogHarnessIcon title="a11y-title" fontSize="1.5rem" />`.
- Sorted filled icon variants after their base icon so `DogH` selects `DogHarnessIcon` before `DogHarnessFillIcon`.
- Kept the default `<` component list free of icons, so icons still do not flood normal discovery.
- Added regression coverage for `<DogH`, exact `<DogHarnessIcon`, nested `<Box><DogH`, and icon prop insertion.

Updated definition:
- Icon-aware context means an icon prop value (`icon={<DogH` / `icon={DogH`) or a valid JSX icon tag prefix (`<DogH`, `<DogHarnessIcon`, or nested forms like `<Box><DogH`) when the typed prefix does not collide with a component prefix.

Verification:
- npm run typecheck
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5178/aksel-arcade/ for `<DogH`, `<DogHarnessIcon`, and icon props.

2026-05-24 - Issue #9 follow-up: Browser-tested Dogh autocomplete

Fixed after dev testing showed the real nested editor interaction still missed the intended `DogHarnessIcon` flow:
- Added browser-level Playwright coverage that types the nested `Page` > `Page.Block` > `Box` snippet into the real CodeMirror editor, then types `<Dogh` and verifies the autocomplete tooltip shows `DogHarnessIcon`.
- Made icon tag prefix matching case-insensitive for valid JSX tag prefixes, so `<dogh`, `<Dogh`, `<DOGH`, `<DogH`, and nested `<Box><dogh` all resolve to `DogHarnessIcon`.
- Fixed stale generic tag completion filtering so pausing after `<` and then typing `dogh` refreshes to icon suggestions instead of filtering old component suggestions such as `Dialog.Header`.
- Kept lowercase non-icon HTML/component prefixes from showing normal component suggestions, so `<div` does not become `Dialog`.
- Kept plain child text such as `<Box>DogH` out of icon autocomplete because it is not valid JSX component authoring.

Verification:
- npm run typecheck
- CI=1 npm run test
- npm run lint
- npx playwright test tests/e2e/autocomplete.spec.ts --project=chromium --reporter=line

Next note:
- If icon autocomplete is reported missing again, start with `tests/e2e/autocomplete.spec.ts`; it now exercises the actual browser/CodeMirror interaction rather than only the pure autocomplete service.

2026-05-24 - Issue #10: Prototype compatibility and import-free sandbox

Selected #10 as the highest-priority ready-for-agent feature because it is unblocked by #5 and gates both #11 built-ins/export metadata work and #13 migration hardening. Worked only on #10.

Implemented:
- Replaced the narrow import stripping with support for production-style React, Aksel component/theme/CSS, Aksel icon, and local hooks imports, including aliases, namespace imports, side-effect CSS imports, and type-only named imports.
- Preserved saved/shared source code as-is; imports are stripped only in the transpiled runtime code sent to the sandbox.
- Added explicit compile guidance for unsupported imports instead of letting them reach the sandbox as opaque runtime syntax errors.
- Deduped stripped-import runtime bindings across JSX and Hooks tabs so importing the same alias in both tabs does not create duplicate `const` declarations.
- Added sandbox runtime compatibility aliases for safe old prototype terms: `BoxNew` resolves to current `Box`, and docs-style `Combobox` resolves to `UNSAFE_Combobox`.
- Kept ambiguous legacy layout terms (`Grid`, `Stack`) as clear runtime guidance errors rather than silently aliasing them to `HGrid`, `HStack`, or `VStack`.
- Exposed the full React hook/runtime set used by stripped imports and prevented Aksel component declarations from colliding with React hook names such as `useId`.
- Regenerated the checked-in sandbox bundle so production uses the same compatibility bindings.
- Added regression coverage for import stripping, alias preservation, unsupported import guidance, safe compatibility aliases, and ambiguous legacy guidance.

Verification:
- npm run typecheck
- CI=1 npm run test -- --run
- npm run lint (passes with the existing `InspectionPopover` hook dependency warning)
- npm run build
- Browser smoke check at http://127.0.0.1:5173/aksel-arcade/ for production-style imports, `BoxNew` compatibility, local hook alias imports, and no runtime console errors.

Next note:
- #11 can now migrate built-in examples/export metadata on top of the compatibility layer. Keep import-free authoring as the default, and use `src/services/transpiler.ts` for any future supported-import additions rather than adding separate regex stripping paths.

2026-05-24 - PR #19 review and merge history

Reviewed, fixed, and merged PR #19:
- `492a3e2` implemented #10: supported production-style import stripping, safe `BoxNew`/`Combobox` runtime compatibility, explicit `Grid`/`Stack` guidance, sandbox bundle regeneration, and regression coverage.
- Review found one robustness gap: supported imports with trailing line or block comments were not stripped, which could break pasted production code before sandbox execution.
- `f637a5a` fixed that gap by extending the supported static import matcher and covering commented React, Aksel component, icon, CSS, and local hooks imports in the transpiler regression test.
- Re-ran local validation: `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, and `git diff --check`.
- GitHub CodeQL checks refreshed green after the review fix.
- PR #19 was merged on 2026-05-24, and the remote/local branch `sjur-gr/ready-issue-feature` was deleted.

Next note:
- #11 can start from `master`; #10's compatibility layer is merged. Future supported-import changes should update `src/services/transpiler.ts` and include regression coverage for aliases, comments, and JSX/Hooks duplicate binding behavior.

2026-05-24 - Issue #11: V8 built-ins, export metadata, and project memory

Selected #11 as the highest-priority ready-for-agent feature because #13 is still blocked by #11/#12, and #11 removes stale built-in/export/governance guidance that future migration hardening depends on. Worked only on #11.

Implemented:
- Migrated the default starter, Hooks demo, and form-summary built-ins from `BoxNew`, numeric layout gaps, and legacy radius names to current `Box`, v8 `space-*` spacing tokens, and v8 radius fragments.
- Fixed the form-summary built-in to render the summary progress state as step 3 of 3 while preserving import-free sandbox authoring.
- Extended exported Aksel metadata with exact package versions, current setup docs, `@navikt/ds-css` setup guidance, explicit import-free playground vs production-import guidance, and Aksel breakpoint metadata with `2xl` at `1440px`.
- Updated README and project governance memory/templates to the Aksel v8 CSS, Theme, and current layout primitive model so future agents do not inherit old setup paths.
- Added regression coverage for built-in template migration/transpilation and exported metadata/package/breakpoint guidance.
- Updated PRD issue #4 with the #11 implementation progress.

Verification:
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing `InspectionPopover` hook dependency warning)
- npm run build
- Browser smoke check at http://127.0.0.1:5178/aksel-arcade/ for default starter render, both built-in templates, exported metadata, and no console errors.

Next note:
- #12 should handle app chrome modernization separately. Keep future built-in examples on current `Box`, `HStack`/`VStack`, `HGrid`, `Page`, and v8 `space-*` tokens; export metadata lives in `src/data/akselMetadata.ts` and must continue to distinguish import-free Arcade authoring from production imports.

### 2026-05-24 - #11 V8 built-ins, export metadata, and project memory

Validation work recorded in this branch:

- Selected #11 as the highest-priority `ready-for-agent` feature because #13 remains blocked by #11/#12, and #11 removes stale built-in/export/governance guidance that future migration hardening depends on.
- Migrated the starter, Hooks demo, and form-summary built-in templates from `BoxNew`, numeric layout gaps, and legacy radius names to current `Box`, v8 `space-*` spacing tokens, and v8 radius fragments.
- Fixed the form-summary progress example so the summary step renders as step 3 of 3 while keeping the import-free sandbox authoring model intact.
- Extended exported Aksel metadata with exact package versions, current setup documentation, `@navikt/ds-css` CSS import guidance, explicit import-free playground vs production-import guidance, and current breakpoint metadata including `2xl: 1440px`.
- Updated visible README guidance and hidden project governance memory/templates to the Aksel v8 CSS, Theme, and current layout primitive model so future work does not inherit old setup paths.
- Added regression coverage for built-in template migration, built-in sandbox transpilation, exported metadata contents, package version pins, setup docs, and breakpoint metadata.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, and browser smoke checks of the default starter, both built-in templates, exported metadata, and console output at the local Vite URL.

Follow-up note: #12 should modernize app chrome separately. Keep #11-style content/export/governance changes separate from the #12 chrome refactor, and keep future built-ins on `Box`, `HStack`/`VStack`, `HGrid`, `Page`, and v8 `space-*` tokens.

### 2026-05-24 - #12 Modernized v8 playground chrome

Completed in this branch:

- Worked only on #12 and kept the existing split-pane authoring, preview, palette, share, theme, and inspect workflows intact.
- Replaced app chrome uses of deprecated `BoxNew` with current `Box`, `HStack`, `VStack`, `HGrid`, `Page.Block`, `Tag`, and `Link` primitives across the header, editor toolbar/header, component palette cards, preview chrome, save/project indicators, and notifications.
- Migrated chrome buttons from legacy neutral button variants to current `variant` plus `data-color="neutral"` usage, and changed settings copy from light/dark "mode" to light/dark "theme".
- Tightened custom CSS so it now focuses on shell sizing, modal scroll/focus mechanics, palette card interaction, preview error overlay, iframe/split-pane mechanics, and inspect overlays instead of duplicating Aksel layout/spacing primitives.
- Refreshed the project-controls integration test to assert the new light/dark theme wording while preserving Import -> Share -> Settings order and share metrics.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, and a browser smoke check at `http://127.0.0.1:5179/aksel-arcade/` for app render, preview render, component palette, settings theme copy, inspect toggle, and console output.

Follow-up note: #13 can now start from `master` once #12 is merged. Keep chrome work separate from migration hardening; if later polish touches the palette, preserve the current Layout/Components/Icons grouping and avoid reintroducing `BoxNew` or legacy neutral button variants into app chrome.

### 2026-05-25 - #13 Migration regression hardening

Completed in this branch:

- Worked only on #13 and kept the change focused on regression hardening after the v8 migration.
- Expanded browser-level autocomplete coverage in `tests/e2e/autocomplete.spec.ts` for component tags, subcomponent tags, multi-line JSX props, enum values, v8 spacing tokens, Box styling values, `data-color`, default icon exclusion, and icon-aware contexts.
- Added `tests/e2e/migration-regression.spec.ts` to cover protected playground flows: saved prototype load, live preview/sandbox readiness, theme updates, viewport updates, component palette insertion, share generation, export metadata, import hydration, and inspect overlays.
- Covered migration-sensitive runtime behavior in the browser: v8 `--ax` CSS/theme semantics, safe `BoxNew` compatibility, import-free authoring, and both built-in examples rendering without error overlays.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, `CI=1 npx playwright test tests/e2e/autocomplete.spec.ts tests/e2e/migration-regression.spec.ts --project=chromium --reporter=line`, and a browser smoke/console check at `http://127.0.0.1:5173/aksel-arcade/`.

Follow-up note: #13 is intentionally test-focused. If future migration regressions appear, start with the two Playwright specs touched here before changing product code; keep autocomplete contract coverage in `tests/e2e/autocomplete.spec.ts` and broader protected-flow coverage in `tests/e2e/migration-regression.spec.ts`.

### 2026-05-25 - PR #22 CodeQL review follow-up

Fixed after reviewing PR #22:

- Reviewed the PR diff and found no product-code regressions in the new #13 coverage.
- Fixed the blocking CodeQL alert `js/insecure-temporary-file` in `tests/e2e/migration-regression.spec.ts` by replacing the `os.tmpdir()` import fixture path with Playwright's per-test `testInfo.outputPath(...)`.
- Kept the review fix scoped to the migration regression e2e test; no product behavior changed.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, `CI=1 npx playwright test tests/e2e/migration-regression.spec.ts --project=chromium --reporter=line`, and `git diff --check`.

### 2026-05-25 - +Add modal LinkCards

Completed in this branch:

- Updated the active +Add component palette cards to render as Aksel `LinkCard` components with `LinkCard.Title` and `LinkCard.Description` only.
- Disabled the LinkCard arrow icon and removed the previous Docs links and prop tag row from every palette card.
- Kept card selection working through the LinkCard title anchor, including Space-key activation, and updated component palette tests/selectors for the new card structure.

Verification completed with `npm run type-check`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, and a Playwright browser smoke check of the +Add modal LinkCards, missing arrows, missing Docs links, missing tags, insertion behavior, and console output at `http://127.0.0.1:5173/`.

Follow-up note: the old `src/components/Editor/ComponentPalette.tsx` implementation still exists but is not the active editor palette import. Future palette work should target `src/components/ComponentPalette/ComponentPalette.tsx` unless the legacy file is intentionally revived or removed.

### 2026-05-25 - Issue #25 dependency refresh

Completed for #25:

- Updated the direct Vite dev dependency to `^7.3.3` and refreshed the npm lockfile without adding overrides.
- Resolved the alert-targeted packages to `vite 7.3.3`, `rollup 4.60.4`, `minimatch 10.2.5`/`3.1.5`, `flatted 3.4.2`, `picomatch 4.0.4`, and `postcss 8.5.15`; no vulnerable `picomatch 2.x` remains.
- Kept the Aksel package pins at `8.11.0`; `npm audit` reports 0 vulnerabilities after the refresh.
- Rebuilt the committed sandbox bundle so generated assets match the refreshed dependency graph.

Verification completed with `npm run typecheck`, `npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), `npm run build`, and a browser smoke/console check at `http://127.0.0.1:5173/aksel-arcade/` (no errors; existing sandbox iframe warning only).

Follow-up note: no npm overrides were added. If Dependabot still reports any of #8, #9, #11, #14, #16, #18, #19, #20, #22, or #24 after merge, re-check whether the alert is scanning the default-branch lockfile or a cached dependency graph; the patched versions are present in `package-lock.json`.

### 2026-05-25 - Issue #23 boolean prop autocomplete

Completed for #23:

- Fixed Aksel prop-name autocomplete so boolean-like props apply as JSX shorthand, e.g. `asChild`, instead of the invalid empty string form `asChild=""`.
- Kept enum/string props with value suggestions applying as `prop=""`, so existing value autocomplete behavior remains unchanged.
- Added regression coverage across generated docs props, catalog props, and icon SVG Booleanish props to catch future boolean autocomplete regressions.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and a browser smoke/console check at `http://127.0.0.1:5173/aksel-arcade/`.

Follow-up note: the fix is centralized in `src/services/akselAutocomplete.ts` inside prop option generation. If new prop metadata sources are added, make sure boolean-like types still flow through the same `isBooleanLikeProp` handling instead of adding source-specific apply strings.

### 2026-05-25 - #31 Temporary Agent session header entry

Selected #31 as the highest-priority `ready-for-agent` feature from #31-#38 because it is the only unblocked foundation issue; #32-#38 depend on #31 directly or through the later Agent bridge chain. Worked only on #31.

Completed in this branch:

- Added the first user-visible Agent ActionMenu immediately left of Settings, with start/stop access and permission checkbox controls.
- Added default Agent permissions: source changes on, preview setting changes on, Preview evidence on, and project metadata changes off.
- Published the browser bridge only while temporary Agent access is active at `window.__AKSEL_ARCADE_AGENT_BRIDGE__`, and removed it on stop, component unmount, and page reload.
- Kept status copy activity-based, including revoked/inactive states, without implying durable socket connectivity.
- Added integration coverage for header placement, default permissions, bridge absence before start, start/stop lifecycle, unmount cleanup, reload-style inactive behavior, mandatory read-access copy, and status wording.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), and a browser smoke/console check at `http://127.0.0.1:5173/aksel-arcade/`.

Follow-up note: #32 should build on `src/services/agentBridge.ts` and `src/hooks/useAgentSession.ts` by adding read commands and copyable instructions. Keep share/export out of the bridge command list until the fallback-packaging issue explicitly asks for regression coverage.

### 2026-05-25 - PR #39 CodeQL check follow-up

Fixed after PR #39 checks failed:

- Reproduced the failed gate from the CodeQL annotations: `js/insecure-randomness` flagged the Agent session ID fallback because it used `Math.random()`.
- Replaced the Agent session ID generator with the existing `generateSecureUUID()` Web Crypto helper so the bridge session ID never falls back to insecure randomness.
- Added integration coverage that starts Agent access through the header and verifies the bridge uses the mocked `crypto.randomUUID()` session id.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), and a browser smoke/console check at `http://127.0.0.1:5174/aksel-arcade/`.

### 2026-05-25 - #32 Copy agent instructions and read Arcade-scoped state

Selected #32 as the highest-priority `ready-for-agent` feature from #32-#38 because it is the first unblocked Agent bridge capability after #31. Worked only on #32.

Completed in this branch:

- Added a Copy agent instructions action to the Agent menu. The copied text includes `window.__AKSEL_ARCADE_AGENT_BRIDGE__`, current command names, active permissions, the Arcade authoring contract, scoped-read boundaries, and the reminder that the human must start access before the bridge exists.
- Added read-only bridge commands `getProject()`, `getPreviewContext()`, and `getSessionState()` while Agent access is active.
- Kept read output limited to Arcade project source/name, preview theme/viewport, session status, active permissions, read scope, and command names. It does not expose share/export data, browser storage, clipboard data, cookies, or unrelated page state.
- Recorded Agent read activity in the menu status and kept stale bridge references returning structured `session-revoked` errors after access is stopped.
- Added integration coverage for copied instruction contents, inactive bridge state before start, active read command success, revoked-session errors after stop, activity status, and scoped read shape.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), and a browser smoke/console check at `http://127.0.0.1:5175/aksel-arcade/`.

### 2026-05-25 - #33 Apply source Agent changes with Checkpoints and rollback

Selected #33 as the highest-priority `ready-for-agent` feature from #33-#38 because it is the first unblocked source-mutation feature after #32 and unlocks the later validation, preview/metadata, diagnostics/evidence, and share/export follow-ups. Worked only on #33.

Completed in this branch:

- Added the `applySourceChange({ summary, jsxCode?, hooksCode? })` Agent bridge command for full-field JSX and/or Hooks source replacements when source-change permission is enabled.
- Required a non-empty human-readable summary, returned structured success data with changed fields and the automatic Checkpoint id, and kept stale bridge references returning `session-revoked`.
- Captured an in-memory Checkpoint before every accepted source change, capped automatic rollback history at 10 entries, and cleared Checkpoints when Agent access is stopped or a new temporary session starts.
- Added human-only rollback controls in the Agent menu; the bridge command list intentionally does not expose a restore command.
- Let accepted schema-valid source changes apply immediately without waiting for compile/render success, so the normal preview flow reports later compile/runtime failures.
- Added integration coverage for bridge command shape, successful source apply, atomic JSX+Hooks updates, Checkpoint creation/capping, human rollback, permission denial, absence of bridge rollback commands, and preview compile-error behavior after an accepted invalid source change.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run`, and a browser smoke/console check at `http://127.0.0.1:5175/aksel-arcade/`.

Follow-up note: #34 should harden `applySourceChange` validation around malformed/oversized/unknown-field payloads and whole-command rejection. #35 can extend the same command path to preview settings and project metadata; keep rollback human-only unless a later issue explicitly changes that contract.

### 2026-05-25 - PR #40 build failure follow-up

Fixed after the merged PR #40 broke the GitHub Pages build:

- Reproduced the failing workflow locally with `npm run build`.
- Root cause: `tsc -b` checks `tsconfig.test.json`, and TypeScript build-mode control-flow analysis could not prove bridge command result variables assigned inside React Testing Library `act()` callbacks were initialized before use. It also prevented clean narrowing of the bridge command success/error union.
- Added a small `callBridgeCommand` test helper that wraps the command in `act()` and returns a definitely-assigned result.
- Added `expectBridgeSuccess` so the test keeps a sharp assertion for successful read commands while preserving TypeScript narrowing.
- Kept the fix scoped to `tests/integration/project-controls-layout.test.tsx`; product bridge behavior was unchanged.

Verification completed with `npm run build`, `CI=1 npm run test -- --run tests/integration/project-controls-layout.test.tsx`, `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), and a browser smoke/console check at `http://127.0.0.1:5175/aksel-arcade/`.

Follow-up note: #33 should build source replacement and automatic Checkpoints on top of these bridge/session primitives. Keep share/export out of the bridge command list, and keep read commands constrained to Arcade-scoped state unless a later issue explicitly expands the command contract.

### 2026-05-25 - PR #40 code review follow-up

Fixed after reviewing PR #40:

- Found that the #32 tests did not prove captured bridge references return current project/preview data after in-session changes.
- Found that captured bridge references exposed stale `permissions` snapshots after permission changes, even though `getSessionState()` used current permissions.
- Changed the bridge `permissions` member to a current-state getter backed by the active session controller.
- Added integration coverage proving captured bridge references return updated project source/name, preview theme/viewport, and active permission state after the human changes them during an Agent session.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run`, `npm run lint` (existing `InspectionPopover` hook dependency warning only), and a browser smoke/console check at `http://127.0.0.1:5175/aksel-arcade/`.

### 2026-05-26 - #34 Reject invalid or unauthorized Agent changes as structured errors

Selected #34 as the highest-priority `ready-for-agent` feature from #34-#38 because it is the next PRD follow-up after #33 and unblocks #35's atomic preview/metadata replacement work. Worked only on #34.

Completed in this branch:

- Hardened `applySourceChange` so malformed, unsupported, unauthorized, or oversized requests fail before source mutation or Checkpoint creation.
- Added stable structured Agent error codes for `invalid-request`, `unsupported-field`, and `payload-too-large`, alongside the existing `permission-denied` and `session-revoked` errors.
- Rejected non-object/array payloads, missing summaries, empty changes, non-string source fields, fields outside the current source-change contract, unsupported preview/metadata fields such as `viewportSize`, `theme`, and `name`, and source updates that would exceed the existing 5MB project size limit.
- Preserved project source/name, preview context, active permissions, menu status, and Checkpoint history after rejected bridge calls.
- Added integration coverage for valid changes, malformed payloads, permission denial, unsupported viewport/theme/name fields, oversize rejection, structured error shape, whole-command rejection, and unchanged state after failures.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and a browser smoke/console check at `http://127.0.0.1:5176/aksel-arcade/`.

Follow-up note: #35 can extend the same guarded command path to permitted `viewportSize`, `theme`, and `name` replacements. Reuse the unsupported-field whole-request behavior when adding those fields so mixed invalid or unauthorized Agent changes stay atomic.

### 2026-05-26 - PR #43 code review follow-up

Fixed after reviewing PR #43:

- Found that `applySourceChange` accepted empty or whitespace-only `jsxCode`/`hooksCode` string replacements, which could mutate the project source and create a Checkpoint for unusable source content.
- Added non-empty source field validation before mutation and Checkpoint creation.
- Added regression coverage proving empty JSX and blank Hooks replacements return structured `invalid-request` errors and leave project, preview, permissions, status, and Checkpoint history unchanged.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and a browser smoke/console check at `http://127.0.0.1:5176/aksel-arcade/`.

Follow-up note: #35 should preserve this non-empty source validation when it extends the guarded Agent change path to preview settings and metadata.

### 2026-05-26 - PR #43 build failure follow-up

Fixed after the merged PR #43 broke the GitHub Pages build:

- Reproduced the failing workflow locally with `npm run build` and confirmed the CI error from run `26434521486`.
- Root cause: `useAgentSession` typed its `project` option as a narrow Arcade read-context subset, but the oversize validation path passed that value to `validateProjectSize(Project)`. `npm run typecheck` missed this because the CI build uses `tsc -b`, which checks the referenced app/test configs.
- Changed `UseAgentSessionOptions.project` to the full `Project`, matching the actual caller while keeping the bridge read context scoped to `{ name, jsxCode, hooksCode }` and preview state.

Verification completed with `npm run build`, `npm run typecheck`, `CI=1 npm run test`, and a browser smoke/console check at `http://127.0.0.1:5176/aksel-arcade/`.

Follow-up note: when Agent bridge validation uses project-wide helpers, run `npm run build` before merge because `npm run typecheck` does not catch all build-mode referenced-project errors.

### 2026-05-26 - #35 Allow permitted preview and metadata replacements

Selected #35 as the highest-priority `ready-for-agent` feature from #35-#38 because #34 is closed, #35 is the next Agent mutation capability in the PRD chain, and it unblocks #37's Preview evidence work. Worked only on #35.

Completed in this branch:

- Extended the existing guarded `applySourceChange` command to accept permitted `viewportSize`, `theme`, and `name` full-field replacements alongside `jsxCode` and `hooksCode`.
- Kept permission enforcement atomic: source fields require `sourceChanges`, viewport/theme require `previewSettings`, and project name requires `projectMetadata`, which remains disabled by default.
- Rejected unsupported fields, invalid viewport/theme values, invalid names, malformed source fields, empty changes, missing summaries, and oversized project updates before any mutation or Checkpoint creation.
- Created a Checkpoint before every accepted Agent change and extended human rollback so source/name/viewport/theme fields restore only when they were changed by that Checkpoint.
- Updated copied Agent instructions and integration coverage for active permission toggles, preview setting apply, metadata default denial, metadata opt-in apply, combined source/preview/metadata apply, whole-change rejection, invalid value handling, Checkpoint capping, and combined rollback.

Verification completed with `npm run typecheck`, `npm run test -- --run`, `npm run build`, and a browser smoke/console check at `http://127.0.0.1:5177/aksel-arcade/`.

Follow-up note: #36 should add diagnostics as a read-only bridge command over preview status/errors/console history. #37 can now rely on preview settings being safely agent-changeable while building sanitized Preview evidence.

### 2026-05-26 - PR #45 code review follow-up

Fixed after reviewing PR #45:

- Found that rapid sequential Agent changes could create a later Checkpoint from stale pre-render state, causing human rollback to jump back too far.
- Synchronized the Agent session's current project/preview refs immediately after accepted Agent changes and human rollback, so subsequent bridge calls capture the latest accepted state even before React flushes a render.
- Added regression coverage that applies two Agent changes in the same React act batch, restores the second Checkpoint back to the first change, then restores the first Checkpoint back to the original source.

Verification completed with `npm run typecheck`, `npm run test -- --run`, `npm run build`, and a browser smoke/console check at `http://127.0.0.1:5177/aksel-arcade/`.

### 2026-05-26 - #36 Return diagnostics with bounded sandbox console history

Selected #36 as the highest-priority `ready-for-agent` feature from #36-#38 because #35 is complete, diagnostics are the next PRD feedback-loop capability, and agents need compile/runtime/console visibility before Preview evidence and share/export hardening. Worked only on #36.

Completed in this branch:

- Added bounded sandbox console history to preview state and normalized console payloads into stable strings with capped history length, argument count, and argument length.
- Added the active Agent bridge command `getDiagnostics()` returning only Arcade-scoped preview status, compile error, runtime error, and recent sandbox console messages.
- Updated Agent command discovery and copied instructions to include `getDiagnostics()` as the post-change preview feedback command.
- Marked accepted source changes as `transpiling` immediately in diagnostics while the debounced preview pipeline reprocesses, avoiding stale pre-change diagnostics in the post-apply gap.
- Kept schema-valid Agent source changes apply-then-review: compile/render failures are still not blocked by apply, and diagnostics reports the resulting normal preview errors afterward.
- Added unit and integration coverage for diagnostics normalization, bounded console history, command shape, revoked-session errors, read activity status, runtime errors, and compile errors.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run tests/unit/services/previewDiagnostics.test.ts tests/integration/project-controls-layout.test.tsx`, the full required `CI=1 npm run typecheck && CI=1 npm run test` check, and a browser smoke check of the active Agent `getDiagnostics()` command.

Follow-up note: #37 should reuse the same active-session/read-activity bridge pattern for permission-gated Preview evidence. Keep diagnostics scoped to preview state only; do not add share/export, browser storage, cookies, clipboard, or unrelated page state to diagnostics.

### 2026-05-26 - #37 Return sanitized Preview evidence from the sandbox

Selected #37 as the only feature for this branch because #35 and #36 are complete, so the Agent bridge already has permission state, active-session reads, diagnostics, and preview-setting foundations.

Completed in this branch:

- Added `src/services/previewEvidence.ts`, a deterministic serializer for the sandboxed Preview iframe root that returns frame viewport/scroll metadata plus a sanitized element tree.
- Included useful visual reasoning facts: tag names, direct text, allowed `id`/`role`/`title`/`aria-*`/`data-*` attributes, sorted class names, bounding boxes, and selected computed layout/style values.
- Excluded scripts, style elements, handler attributes, React internals, full CSS text, arbitrary object props, browser storage/cookie/clipboard references, and DOM outside the Preview iframe.
- Added the active Agent bridge command `getPreviewEvidence()`, gated by the existing `previewEvidence` permission and returning structured `permission-denied`, `preview-unavailable`, and `session-revoked` failures.
- Shared the Preview iframe ref through `AppProvider` so the Agent menu reads evidence only from the sandboxed frame, not from unrelated Arcade chrome.
- Updated copied Agent instructions and command discovery to include `getPreviewEvidence()`.
- Added unit serializer coverage plus integration coverage for successful evidence reads, permission denial without evidence, stale bridge revocation, deterministic output, included facts, excluded unsafe data, and read activity status.

Verification completed with `npm run typecheck`, `npm run test -- --run`, targeted `npm run test -- --run tests/integration/project-controls-layout.test.tsx`, `git diff --check`, and a headless browser smoke check of the active `getPreviewEvidence()` bridge command at the local Vite URL.

Follow-up note: #38 should verify share/export artifacts still exclude Agent session state, permissions, activity, bridge metadata, Preview evidence, and Checkpoints. Keep Preview evidence as a live, permission-gated bridge read rather than adding it to share/export payloads.

### 2026-05-26 - #38 Keep Agent artifacts out of share/export fallback flows

Selected #38 as the only feature for this branch because #37 is complete and #38 is the PRD follow-up that protects fallback handoff artifacts after Agent changes and Checkpoints.

Completed in this branch:

- Kept Share URL generation and Export JSON as UI fallback flows only; the active Agent bridge command list still exposes no share/export commands or properties.
- Changed Share URL snapshot generation and the header share refresh fingerprint to use the current project viewport, so accepted Agent preview-size changes are packaged instead of the stale `previewState.currentViewport` default.
- Added regression coverage that starts an Agent session, accepts source, Hooks, viewport, theme, and name changes that create a Checkpoint, then verifies Share URL snapshot and Export JSON output include the current project/preview state while excluding the Checkpoint id, Checkpoint summary, bridge global, Agent session state, permissions, activity, rollback history, diagnostics, and Preview evidence.
- Preserved existing share fallback behavior, including oversize/export fallback coverage and explicit regeneration when the share button is clicked while the popover is already open.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, targeted share/Agent integration coverage, and a headless browser smoke check of Agent change -> preview render -> Share URL copy -> Export JSON download.

Follow-up note: keep share/export as human-triggered fallback flows. If future Agent artifacts move into project-level state, extend the share/export exclusion regression checks before exposing those artifacts anywhere near fallback payloads.

### 2026-05-26 - PR #49 build follow-up

Fixed after the post-merge master build failed:

- Reproduced the GitHub Actions failure from run 26449614261: `tsc -b` rejected the share popover retry button because `generateShareLink` now accepts an optional `forceRegeneration` boolean, but React `onClick` passes a mouse event.
- Wrapped the retry action in a zero-argument click handler so retry keeps normal generation semantics without passing the event into share-link generation.
- Updated the Agent sessions PRD history with the #38 fallback export/share implementation and the PR #49 build follow-up.

Verification completed with `npm run build`.

### 2026-05-26 - #51 Changes to the new Agent ActionMenu

Selected #51 as the only feature for this branch. Used the issue description and the provided screenshot because the Figma link was not accessible from this environment.

Completed in this branch:

- Simplified the Agent ActionMenu to match the new design: Norwegian section labels, a static `Agent-tilgang` checkbox, `Status: aktiv` / `Status: inaktiv`, contextual copy guidance, and `Kopier instruksjoner` as an `ActionMenu.Item`.
- Removed the separate permission controls from the UI.
- Changed the default Agent permission state to all-on while Agent access is active, including project metadata changes.
- Kept the active browser bridge, scoped read commands, apply/change validation, Checkpoints, rollback, diagnostics, Preview evidence, and share/export artifact exclusions intact.
- Updated integration coverage for the simplified menu, static checkbox label, all-on permission state, ActionMenu copy item, simplified status, metadata replacement default, and share/export fallback helper.

Next note: #51 intentionally changes the earlier Agent session PRD assumption that project metadata is opt-in and permission toggles are user-visible. Future Agent ActionMenu work should treat the menu as a simple access/context surface unless a new issue reintroduces granular controls.

### 2026-05-27 - #55 Capability-gate Web and Desktop Arcade

Selected #55 as the highest-priority `ready-for-agent` feature from #55-#68 because it is unblocked and establishes the capability foundation that #56-#67 depend on. Worked only on #55.

Completed in this branch:

- Added `src/services/shellCapabilities.ts` with reusable Web Arcade and Desktop Arcade capability sets for Share URL, Agent sessions, and Arcade project package affordances.
- Wired `App` and `AppHeader` to consume the capability model, defaulting to Web Arcade and supporting Desktop capability preview through `VITE_AKSEL_ARCADE_SURFACE=desktop`.
- Kept Web Arcade Share URL available while hiding Agent access and avoiding a published browser-global Agent bridge.
- Kept Desktop capability mode Agent access available while hiding the Share URL surface.
- Added unit coverage for the capability matrix and integration coverage for Web-vs-Desktop header behavior; moved existing Agent bridge integration coverage onto Desktop capabilities.

Verification completed with `npm run typecheck`, `npm run test -- --run`, `npm run type-check`, `npm run lint` (existing hook dependency warnings only), and browser smoke checks for Web default capabilities plus `VITE_AKSEL_ARCADE_SURFACE=desktop` Desktop capabilities.

Next note: #56 should continue from the capability model instead of adding platform checks. Start with `src/services/shellCapabilities.ts` and `AppHeader` when proving Web Arcade has no Agent runtime, then keep Desktop transport work separate from Share URL behavior.

### 2026-05-27 - #56 Gate Web Arcade Agent and Share surfaces

Selected #56 as the highest-priority `ready-for-agent` feature from #56-#68 because #55 is closed, #56 is the earliest unblocked feature in the Desktop Agent transport dependency chain, and it unlocks the shared Agent bridge router work in #60. Worked only on #56.

Completed in this branch:

- Kept Web Arcade free of Agent controls and the browser-global Agent bridge while preserving the Web Share URL path.
- Moved `AgentSessionMenu` behind a Desktop-capability-only lazy boundary in `AppHeader`, so Web Arcade does not mount or load the Agent session runtime path during normal rendering.
- Strengthened integration coverage to prove Web Arcade can open the Share URL popover without publishing `window.__AKSEL_ARCADE_AGENT_BRIDGE__`, while Desktop capability tests still cover Agent access with Share URL absent.

Verification completed with `npm run typecheck` and `npm run test`.

Next note: #60 can now extract the bridge command router without needing to preserve a Web Arcade browser-global Agent surface. Keep the lazy/Desktop-only Agent menu boundary unless a later PRD explicitly reintroduces Web Agent access.

### 2026-05-27 - #60 Extract the Agent bridge command router

Selected #60 as the highest-priority `ready-for-agent` feature from #57-#68 because #56 is complete, #60 is now unblocked, and the router seam unlocks the Desktop Agent transport work without taking on package export/import or the desktop shell. Worked only on #60.

Completed in this branch:

- Extracted `createAgentBridgeCommandRouter` from the browser-global Agent bridge publishing path, keeping the command contract reusable without `window.__AKSEL_ARCADE_AGENT_BRIDGE__`.
- Preserved the supported command names and response shapes for `getProject`, `getPreviewContext`, `getDiagnostics`, `getPreviewEvidence`, `getSessionState`, and `applySourceChange`.
- Added a structured `unsupported-command` error for non-bridge command routing while preserving existing session-revoked, permission-denied, preview-unavailable, validation, and apply-source result semantics.
- Kept `createAgentBridge` as a compatibility wrapper over the router so Desktop capability tests and existing browser-global bridge behavior do not regress.
- Added unit coverage for router read commands, diagnostics cloning, Preview evidence permission/unavailable failures, apply-source routing, unsupported commands, revoked sessions, and wrapper compatibility.

Verification completed with `npm run typecheck` and `CI=1 npm run test -- --run`.

Next note: #61 can build the Desktop Agent session coordinator on top of this router. Transport adapters should call `routeCommand(commandName, payload)` rather than reaching through the browser-global bridge wrapper, and should continue rejecting file-system, shell, Share URL, export/import, package, and window-management commands as unsupported.

### 2026-05-27 - PR #71 build failure follow-up

Fixed after the merged PR #71 broke the GitHub Pages build:

- Reproduced the failing workflow from run 26495723013 with `npm run build`; `tsc -b` rejected the routed `getSessionState` result because the inline object widened `status` and `readScope` to `string`.
- Annotated the `getSessionState` routed read as `AgentSessionReadState`, preserving the exact structured result shape while keeping the browser-global bridge wrapper unchanged.
- Confirmed the original build failure no longer reproduces.

Verification completed with `npm run build`, `npm run typecheck`, and `CI=1 npm run test -- --run`.

Follow-up note: Agent bridge router changes should run `npm run build`, not only `npm run typecheck`, because the build-mode referenced TypeScript config catches stricter routed-result inference.

### 2026-05-27 - #59 Boot Desktop Arcade with narrow preload capabilities

Selected #59 as the highest-priority `ready-for-agent` feature from #57-#59 and #61-#68 because #55 and #60 are complete, while #59 is the remaining foundation before the Desktop Agent session coordinator and transport chain (#61-#66). Worked only on #59.

Completed in this branch:

- Added a macOS-first Electron development shell (`desktop/main.cjs` and `desktop/preload.cjs`) around the shared renderer.
- Added `npm run desktop:dev`, which starts the Vite renderer on `127.0.0.1:5173` and launches Desktop Arcade against it.
- Kept the renderer browser-like by disabling Node integration, enabling context isolation and sandboxing, and exposing only `getShellCapabilities()` through preload IPC.
- Updated renderer bootstrap so Desktop Arcade capabilities come from the narrow preload bridge when present, while Web Arcade keeps the existing default capability path.
- Added unit coverage for preload capability resolution, fallback behavior, and malformed preload payload rejection.
- Documented local Desktop Arcade development in the README and updated the Desktop Agent transport PRD progress.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run build`, `npm run lint` (existing hook dependency warnings only), syntax checks for the desktop scripts, and a Desktop Arcade smoke launch through `npm run desktop:dev`.

Next note: #61 can build the Desktop Agent session coordinator on top of this shell. Keep future desktop-only capabilities behind preload/main-process IPC and do not import Electron, Node, socket, process, or filesystem APIs into React components.

### 2026-05-27 - #61 Add Desktop Agent session coordinator

Selected #61 as the highest-priority `ready-for-agent` feature from #57-#58 and #61-#68 because #59 and #60 are complete, #61 is now unblocked, and it is the next foundation for the Desktop transport chain (#62-#66). Worked only on #61.

Completed in this branch:

- Added `src/services/desktopAgentSessionCoordinator.ts` as the Desktop Agent session coordinator boundary. It owns active/inactive state, one active session, all-on v1 permissions, session identifiers, hidden short-lived pairing credentials, and start/stop hooks for the later transport adapter.
- Wired `useAgentSession` to start and stop through the coordinator while preserving the existing Desktop Agent menu status text and browser bridge compatibility wrapper.
- Added cleanup on explicit stop, reload/pagehide, beforeunload, and renderer unmount so stale session state, permissions, Checkpoints, credentials, and bridge publication are invalidated.
- Added coordinator unit coverage for lifecycle, one-session behavior, stop/reload/quit cleanup reasons, credential invalidation, Web Crypto credential generation, all-on permissions, and adapter startup rollback.
- Strengthened Desktop Agent menu integration coverage to prove the published bridge still excludes the pairing credential.

Verification completed with `npm run typecheck` and `CI=1 npm run test`.

Next note: #62 can attach the loopback JSON-RPC lifecycle/auth adapter to `DesktopAgentTransportAdapter` and should use `getActiveTransportSession()` only inside the transport boundary. Keep the renderer-facing bridge command contract routed through `createAgentBridgeCommandRouter`; do not expose filesystem, shell, Share URL, export/import, package, rollback, or window-management commands.

### 2026-05-27 - #62 Implement loopback JSON-RPC lifecycle and authentication

Selected #62 as the highest-priority `ready-for-agent` feature from #57-#58 and #62-#68 because #61 is complete and #62 is the first concrete Desktop Agent transport, unblocking the read-command, copy-instructions, mutation, Checkpoint, and validation chain. Worked only on #62.

Completed in this branch:

- Added `desktop/agentLoopbackTransport.cjs`, a short-lived main-process HTTP JSON-RPC transport bound to `127.0.0.1` on a random available port for the active Desktop Agent session.
- Wired Desktop preload IPC and the renderer `DesktopAgentTransportAdapter` so starting Agent access starts the loopback endpoint with the hidden pairing credential, and stopping/reload/quit tears it down.
- Required the pairing credential as a `Bearer` Authorization header; query-string credentials, missing/unsupported/invalid Authorization headers, malformed JSON, invalid JSON-RPC envelopes, unsupported methods, and inactive sessions return structured JSON-RPC errors when a request reaches the transport.
- Preserved the existing renderer Agent bridge/router command contract for #63 by leaving transport methods unsupported until the read-only routing feature is implemented.
- Updated async coordinator and integration coverage so bridge publication waits for transport startup and rolls back active session state if transport startup fails.
- Added loopback transport unit coverage for localhost endpoint metadata, Authorization-header auth, query credential rejection, malformed JSON-RPC, unsupported methods, and shutdown behavior.
- Updated PRD #53 implementation progress with the #62 transport lifecycle/auth work.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run build`, `npm run lint` (existing warnings only), desktop script syntax checks, and a browser smoke/console check at the local Vite URL.

Next note: #63 should route authenticated JSON-RPC method names through `createAgentBridgeCommandRouter` rather than adding a second command contract. Keep the main-process transport as lifecycle/auth only, continue rejecting filesystem, shell, Share URL, export/import, package, rollback, and window-management methods, and keep credentials in the Authorization header only.

### 2026-05-27 - #62 follow-up: expose copied loopback auth details

Fixed after manual Desktop Agent testing showed copied instructions still contained only the older browser-global bridge text:

- Carried the active `DesktopAgentTransportEndpoint` returned by preload IPC through the coordinator and `useAgentSession`.
- Added a minimal hidden transport section to copied Agent instructions with the loopback endpoint, `Authorization: Bearer ...` header, JSON-RPC POST guidance, and an example request.
- Kept the visible Agent menu from rendering the endpoint or credential; these details are only in the copied instructions while Agent access is active.
- Left actual JSON-RPC command routing for #63 and copied-instruction product polish for #64.
- Added regression coverage for active Desktop copied instructions containing endpoint/Auth details without visible UI leakage.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run build`, `npm run lint` (existing warnings only), and a Desktop Arcade dev smoke test. The desktop smoke test copied instructions from the running app, sent a JSON-RPC request with the copied Authorization header and got a structured `unsupported-method` response, verified missing/wrong auth failures, rejected query-token usage, rejected a non-loopback interface request, and confirmed the endpoint shut down after stopping Agent access.

Next note: #63 should now be able to reuse the copied endpoint/Auth instructions and focus only on routing supported read methods through `createAgentBridgeCommandRouter`.

### 2026-05-27 - PR #75 review follow-up: transport lifecycle races

Fixed scoped issues found during PR #75 review before merge:

- Serialized loopback transport `startSession` calls so concurrent starts cannot orphan an earlier random-port HTTP server.
- Marked the pending loopback session active before the server finishes listening and made server shutdown force-close idle/active HTTP connections where Node supports it.
- Made coordinator startup cancellable: stopping/reloading while preload IPC is still starting no longer resurrects the Agent session, and any endpoint returned after cancellation is stopped again with the original cleanup reason.
- Rejected preload IPC endpoints with an empty `Bearer ` Authorization value instead of accepting endpoint metadata that can only fail later.
- Added regression coverage for concurrent loopback starts, cancelled coordinator startup, and empty Bearer endpoint validation.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing warnings only), `npm run build`, desktop script syntax checks, and the Desktop Arcade dev smoke path.

Next note: #63 can still focus on routing authenticated JSON-RPC read methods through `createAgentBridgeCommandRouter`; the #75 review fixes only harden #62 lifecycle/auth behavior.

### 2026-05-27 - #63 Expose read-only Agent bridge commands over transport

Selected #63 as the highest-priority `ready-for-agent` feature from #57-#58 and #63-#68 because #62 is complete, #63 is the next unblocked Desktop Agent transport step, and it unlocks the copied-instruction, mutation, and checkpoint follow-ups. Worked only on #63.

Completed in this branch:

- Routed authenticated loopback JSON-RPC requests from the main-process transport to the active renderer through narrow preload IPC.
- Added a Desktop Agent transport protocol helper that maps read-only method names one-to-one to `createAgentBridgeCommandRouter` for `getProject`, `getPreviewContext`, `getDiagnostics`, `getPreviewEvidence`, and `getSessionState`.
- Kept the transport read-only for this feature: `applySourceChange`, rollback, filesystem, shell, Share URL, export/import/package, window-management, and other unsupported methods return structured JSON-RPC `unsupported-method` errors.
- Kept the hidden pairing credential out of the renderer route boundary; the main-process loopback still authenticates with the Bearer header before forwarding sanitized requests.
- Updated copied transport instructions to name the supported read methods while keeping endpoint and credential details hidden from the visible Agent menu.
- Added unit coverage for protocol routing, stale/revoked session errors, loopback route forwarding without credential leakage, and preload route-handler registration.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run build`, desktop script syntax checks, and a headless browser smoke check against the local Vite app.

Next note: #64 should now polish the copied provider-neutral instructions around the working read transport. #65 should extend the same protocol path for `applySourceChange` instead of adding a second command contract, and should keep the non-project desktop methods rejected.

### 2026-05-27 - #64 Copy hidden provider-neutral Agent instructions

Selected #64 as the highest-priority `ready-for-agent` feature from #57-#58 and #64-#68 because #63 is complete, #64 is the next unblocked Desktop Agent transport step, and it sharpens the active-session instructions needed before mutation and human validation work. Worked only on #64.

Completed in this branch:

- Polished the hidden copied Desktop Agent instructions with endpoint and `Authorization` header guidance, supported read method names, the full Agent bridge command list, JSON-RPC request shape, query-credential rejection guidance, and provider-neutral examples for GitHub Copilot app, Copilot CLI, Copilot in VS Code, and other same-device External agents.
- Kept endpoint, credential, local server, and transport details out of the visible Desktop Agent menu while adding visible copy success feedback, copy failure feedback, and a retry affordance.
- Added prompt unit coverage for provider-neutral contents and inactive secret exclusion.
- Added Agent menu integration coverage for success feedback, hidden active endpoint/Auth details, provider-neutral examples, failure feedback, retry, and visible UI secret non-disclosure.
- Updated PRD #53 implementation progress with the #64 copied-instruction work.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and a desktop-surface browser smoke check for Agent menu render, start access, copy success feedback, hidden instruction copying, visible secret non-disclosure, and clean console output.

Next note: #65 should extend the same Desktop Agent transport protocol path for authenticated `applySourceChange` instead of adding a second command contract. Keep filesystem, shell, Share URL, export/import/package, rollback, and window-management methods rejected, and keep copied instructions/provider examples provider-neutral.

### 2026-05-27 - #65 Apply Agent changes through Desktop transport

Selected #65 as the highest-priority `ready-for-agent` feature from #57-#58 and #65-#67 because #63/#64 are complete, #65 is the next unblocked Desktop Agent transport step, and it unlocks the human Checkpoint restoration work in #66 and package-cleanliness follow-up in #67. Worked only on #65.

Completed in this branch:

- Widened the renderer Desktop Agent transport protocol route from read-only bridge methods to all supported Agent bridge command names.
- Routed authenticated JSON-RPC `applySourceChange` requests through `createAgentBridgeCommandRouter` with the request `params`, reusing the existing mutation validator, project update flow, preview setting update flow, size checks, structured bridge errors, pending diagnostics behavior, and Checkpoint creation.
- Kept filesystem, shell, Share URL, export/import/package, rollback, window-management, and other non-bridge methods rejected with structured `unsupported-method` JSON-RPC errors.
- Updated hidden copied transport instructions to list all supported JSON-RPC methods and include an authenticated `applySourceChange` example without exposing endpoint or credential details in the visible Agent menu.
- Added unit coverage for transport `applySourceChange` success, bridge validation failure mapping, unsupported methods, stale renderer sessions, and revoked sessions.
- Added integration coverage that simulates the Desktop preload route handler applying source, preview, and metadata changes through the transport, verifies invalid transport changes leave state unchanged, and keeps unsupported/stale-session failures structured.

Verification completed with `npm run typecheck`, targeted Agent transport Vitest coverage, and the full `CI=1 npm run test -- --run` suite.

Next note: #66 can build directly on the Checkpoint id returned by transport `applySourceChange`, but the transport must remain human-rollback-only. Do not add an agent-triggered restore/delete Checkpoint command when exposing Checkpoints in the menu.

### 2026-05-27 - #57 Export Arcade project packages

Selected #57 as the highest-priority `ready-for-agent` feature from #57-#58 and #66-#67 because #55 is closed, #57 is now unblocked, and it gates both package import compatibility (#58) and package-cleanliness-after-Agent-session work (#67). Worked only on #57.

Completed in this branch:

- Changed the default project export from a loose JSON artifact to an Arcade project package download with the `.akselarcade` extension.
- Added a typed package envelope with `aksel-arcade/project-package` format metadata, export timestamp, portable project content, and optional AI enrichment metadata.
- Kept package contents scoped to portable Arcade project state by explicitly copying only identity, editable source, viewport, panel layout, timestamps, and Aksel metadata; Agent sessions, pairing credentials, endpoints, permissions, Checkpoints, diagnostics, Preview evidence, and transport metadata are excluded.
- Kept Web Arcade Share URL as a separate web-only sharing path and updated oversized Share URL copy to point users to package export instead of JSON export.
- Added storage unit coverage for filename/MIME/shape, portable content, Aksel metadata, and Agent artifact exclusion.
- Updated Desktop export fallback integration coverage to assert the new package envelope still carries current Agent-applied project content without local Agent artifacts.
- Updated the Desktop Agent transport PRD implementation progress with the #57 package export work.
- Tightened build-mode test type assertions encountered during validation so `npm run build` type-checks the package export test and existing Desktop transport test harness cleanly.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (existing warnings only), `npm run build`, targeted package/export Vitest coverage, and a browser smoke check that rendered the default preview and downloaded an `.akselarcade` package.

Next note: #58 should add import support for this package envelope while preserving legacy `.json` import compatibility. Reuse `createArcadeProjectPackage`/the package constants from `src/services/storage.ts` instead of inventing a second package shape, and keep Agent session/transport fields ignored on import.

### 2026-05-27 - #58 Import Arcade project packages and legacy JSON

Selected #58 as the highest-priority `ready-for-agent` feature from #58, #66, and #67 because #57 is closed and #58 is the next unblocked package-sharing step. #66 remains the next Desktop transport Checkpoint feature, and #67 is blocked by both package import and Checkpoint rollback cleanliness. Worked only on #58.

Completed in this branch:

- Added `.akselarcade` package import support for the `aksel-arcade/project-package` envelope created by package export.
- Kept legacy flat `.json` imports and pre-package nested JSON imports compatible with the same validation path.
- Normalized imported projects to portable Arcade project fields only, assigning a fresh local id and timestamp on import.
- Explicitly ignored Agent session, pairing credential, endpoint, permissions, Checkpoint, diagnostics, Preview evidence, and transport fields from package and legacy JSON payloads.
- Updated the shared Import file picker to accept `.akselarcade`, the package MIME type, `.json`, and `application/json`.
- Updated the Desktop Agent transport PRD implementation progress with the #58 package import work.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, targeted storage/header Vitest coverage, and a browser smoke check that exported a `.akselarcade` package and imported a `.akselarcade` package back into the workspace with no console errors.

PR #80 review follow-up: tightened package-envelope detection from partial-field matching to requiring the full `format` + `formatVersion` + `project` envelope, so legacy nested JSON with unrelated `project` metadata still imports normally. Added storage coverage for that legacy metadata case and updated the PRD history in #53.

Next note: #66 can proceed with human-controlled Checkpoints now that #65 is closed. Keep rollback human-only and do not let the Agent transport restore or delete Checkpoints. #67 should wait until #66 lands so package cleanliness after Agent sessions can cover read, change, stop, and human rollback cases.

### 2026-05-27 - #66 Restore human Checkpoints after transport changes

Selected #66 as the highest-priority `ready-for-agent` feature from #66 and #67 because #65 is closed, #66 is the next unblocked Desktop transport rollback feature, and #67 remains blocked by #66. Worked only on #66.

Completed in this branch:

- Added Desktop transport integration coverage proving an authenticated `applySourceChange` creates a visible Agent menu Checkpoint and that the human menu restore action rolls back source, Hooks, preview settings, and project metadata.
- Added coverage that `restoreCheckpoint` and `deleteCheckpoint` stay unsupported JSON-RPC methods, so External agents can receive Checkpoint identifiers in change results but cannot trigger rollback or delete rollback history.
- Added coverage that stopping Agent access clears the local Checkpoint entries from the Agent menu.
- Updated the Desktop Agent transport PRD implementation progress with the #66 human-controlled rollback work.

Verification note: attempted `npm run typecheck` and `CI=1 npm run test -- --run`, but this environment does not have `npm` on PATH (`bash: npm: command not found`). The next person should run those two commands in a Node-enabled checkout before merging.

Next note: #67 is now the next Desktop Agent transport follow-up. It should prove `.akselarcade` packages remain clean after Agent transport reads, Agent changes, stop, and human rollback; do not include Agent session identifiers, pairing credentials, endpoints, permissions, Checkpoints, diagnostics, Preview evidence, or transport metadata in package exports/imports.

### 2026-05-27 - #67 Keep packages clean after Agent sessions

Selected #67 as the only feature for this branch because #57, #58, #65, and #66 are complete, making package cleanliness after Desktop Agent transport activity fully unblocked. Worked only on #67.

Completed in this branch:

- Added Desktop transport integration coverage that starts Agent access, performs an authenticated transport read, applies source/Hooks/preview/theme/name changes through `applySourceChange`, exports a `.akselarcade` package while Agent access is active, restores the human Checkpoint, stops Agent access, exports again, and imports the active-session package into a fresh Desktop surface.
- Compared the active and stopped package envelope shape with a normal package export, while verifying package payloads include only portable Arcade project content plus normal Aksel metadata.
- Proved package exports exclude Agent session ids, pairing credentials, loopback endpoints, permissions, Checkpoints, diagnostics, Preview evidence, transport metadata, rollback/activity/bridge keys, the bridge global, and confidential Checkpoint summaries.
- Proved importing a package exported after Agent access recreates only the portable project content and does not recreate Agent access or stale Checkpoints. Desktop capability coverage still keeps Export/Import package controls present and Share URL absent.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run tests/integration/project-controls-layout.test.tsx tests/unit/services/storage.test.ts`, and the required full `CI=1 npm run test -- --run` suite after installing dependencies with `npm ci` because this worktree initially had no local `node_modules`.

Next note: package cleanliness is now covered at the Desktop transport integration boundary. If future Agent artifacts move into project state or export metadata, extend `tests/integration/project-controls-layout.test.tsx` and `tests/unit/services/storage.test.ts` before exposing those artifacts anywhere near `.akselarcade` import/export.

### 2026-05-27 - PR #82 review follow-up

Reviewed PR #82 before merge and found one scoped clarity issue in the new package-cleanliness integration test:

- Investigated whether the extra `crypto.randomUUID().mockReturnValueOnce(...)` was dead code; targeted validation showed it intentionally keeps the default project id distinct from the deterministic Desktop transport session id, so package-leak assertions do not fail on fixture UUID reuse.
- Kept that behavior and named the override `deterministicProjectId` so the test makes the project-id/session-id separation explicit while still verifying the active Desktop transport session id from the preload fixture is excluded from exported package JSON.

Verification completed with `npm run typecheck`, `CI=1 npm run test -- --run tests/integration/project-controls-layout.test.tsx tests/unit/services/storage.test.ts`, and the full `CI=1 npm run test -- --run` suite.

### 2026-05-27 - #68 Validate copied instructions with Copilot surfaces

Selected #68 as the only feature for this branch because #64 and #66 are closed, making the copied Desktop Agent instructions and human rollback flow ready for human-in-the-loop validation. Worked only on #68.

Completed in this branch:

- Ran a human-in-the-loop validation pass for the copied Desktop Agent instructions with GitHub Copilot app on macOS, Copilot CLI, and Copilot in VS Code.
- For each surface, Desktop Arcade Agent access was started, hidden instructions were copied from the Agent menu, the surface read Arcade project state, read diagnostics or Preview evidence, applied a small visible Agent change, the human reviewed the live preview, and Agent access was stopped.
- Kept the protocol provider-neutral; no Copilot-specific integration behavior was added.
- Filed follow-up issue #83 for the post-change preview settling guidance gap observed during CLI/VS Code validation: agents should wait for diagnostics to settle after `applySourceChange` before treating Preview evidence as final.
- Filed follow-up issue #84 for the sandbox render diagnostics gap observed during CLI validation: a React render failure could leave Preview evidence empty while `getDiagnostics` still reported no structured runtime error.
- Updated the Desktop Agent transport PRD implementation progress with the #68 validation result.

Verification completed with `npm run typecheck` and `CI=1 npm run test`.

Next note: #68 is validation-only. Follow-up #83 should clarify copied instructions around waiting for post-change diagnostics/evidence after `applySourceChange`, and #84 should investigate structured diagnostics for React render failures that currently only show up in sandbox console output. Both follow-ups should avoid changing the provider-neutral protocol or adding Copilot-specific behavior.

### 2026-05-28 - #88 Add getAgentInstructions Agent bridge command

Selected #88 as the highest-priority `ready-for-agent` feature from #88-#91 because #89, #90, and #91 are all blocked by #88. Worked only on #88.

Completed in this branch:

- Added read-only `getAgentInstructions` to the Agent bridge command names, read command names, browser bridge wrapper, Desktop transport protocol routing, and `getSessionState` command metadata.
- Added a versioned `getAgentInstructions` payload with English instruction markdown plus machine-readable session id, endpoint, Authorization header, permissions, read scope, supported command names, JSON-RPC protocol metadata, and an Arcade authoring contract summary.
- Kept the payload scoped to active-session guidance only: no Arcade project name/source, diagnostics, Preview evidence, Checkpoints, package data, storage data, or Aksel Design System tutorial content.
- Preserved existing inactive, revoked, stale-session, and unauthenticated transport failure paths, with explicit coverage for endpoint-less bridge calls and revoked instruction reads.
- Added unit and integration coverage for command-list exposure, successful Desktop transport routing, excluded project content, and existing stale/revoked behavior.

Verification completed with `npm run typecheck` and `CI=1 npm run test -- --run` after installing dependencies with `npm ci` and using `/opt/homebrew/bin` for the local Node/npm toolchain.

Next note: #89 is now unblocked. It should replace the long copied instructions with a one-line `curl` bootstrap command that calls `getAgentInstructions`, keeps credentials in the Authorization header only, and relies on the same stop/reload invalidation boundary.

### 2026-05-28 - #89 Copy one-line Agent pairing handoff command

Selected #89 as the highest-priority `ready-for-agent` feature from #89-#91 because #90 and #91 are both blocked by #89, while #88 is already closed. Worked only on #89.

Completed in this branch:

- Replaced the Agent menu clipboard payload with a single-line pure `curl` bootstrap command for the active Desktop Agent session.
- The copied command sends a JSON-RPC `getAgentInstructions` request to the loopback endpoint with `Content-Type: application/json` and the Agent pairing credential only in the Authorization header.
- Inactive Agent access, or a session without a Desktop transport endpoint, now refuses to copy a handoff command and keeps the existing copy error feedback path.
- Kept endpoint, Authorization header, and command text hidden in the menu UI while still allowing the short-lived command to be copied after active Desktop Agent access starts.
- Added regression coverage for exact clipboard output, inactive no-copy behavior, no query credentials, no helper/jq/MCP dependency, active getAgentInstructions routing, and stop invalidation for the copied command path.

Verification completed with `npm run typecheck`, `npm run test -- --run`, and a browser smoke check via `npm run test:e2e -- --project=chromium tests/e2e/agent-diagnostics.spec.ts --reporter=line` after installing dependencies with `npm ci` and using `/opt/homebrew/bin` for the local Node/npm toolchain.

Next note: #90 can now update the Norwegian Agent menu wording and safety cues on top of the short command behavior. #91 should keep focusing on preventing the command, endpoint, Authorization header, pairing credential, and fetched instructions from becoming durable project/package/telemetry/log data.

### 2026-05-28 - PR #95 review follow-up

Reviewed PR #95 before merge and fixed one scoped test gap:

- Added regression coverage proving the copied Agent pairing handoff command POSIX-shell-quotes single quotes and shell metacharacters in the endpoint and Authorization header while remaining one line.

Verification completed with `npm run typecheck` and targeted `npm run test -- --run tests/unit/services/agentBridge.test.ts`; full-suite verification was rerun before merge.

### 2026-05-28 - #91 Prevent Agent pairing handoff secrets from durable artifacts

Selected #91 as the highest-priority `ready-for-agent` feature from #90-#91 because it protects the secret-bearing Agent pairing handoff from durable storage/logging. Worked only on #91.

Completed in this branch:

- Added shared renderer redaction for Agent pairing handoff logs so copied commands, loopback endpoints, Authorization bearer values, pairing credentials, and fetched instruction markdown are replaced before console logging.
- Applied the redaction to Agent menu copy/start failures and Desktop Agent transport stop failures, avoiding raw Error object logging for these paths.
- Redacted secret-bearing route failure messages in Desktop loopback/preload transport responses so failed instruction fetches do not echo endpoint, Authorization header, credential, or copied command text.
- Added regression coverage for redaction helpers, copy failure logging, async transport stop logging, instruction fetch failure responses, and the existing package export / rollback / import flow after an active Agent session.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and `npm run lint` (passes with the existing React hook dependency warnings in `InspectionPopover` and `PreviewPane`).

### 2026-05-28 - PR #96 review follow-up

Fixed one scoped review finding before merge:

- Brought the Desktop CJS Agent handoff redactor to parity with the renderer redactor for stringified `pairingCredential` and `instructionsMarkdown` fields.
- Added regression coverage proving Desktop-side redaction marks redacted content and removes stringified pairing credentials, fetched instruction markdown, endpoint, and Authorization header content.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and `npm run lint` (passes with the existing React hook dependency warnings in `InspectionPopover` and `PreviewPane`).

### 2026-05-28 - PR #96 build failure follow-up

Fixed the post-merge `npm run build` failure from PR #96:

- Root cause: `tsc -b` selected the generic `Array.reduce` overload from the nullable `knownSecrets` item type in `src/services/agentHandoffRedaction.ts`, so the accumulator was inferred as `string | null | undefined` even though runtime always seeded it with a string.
- Fixed by making the accumulator type explicit with `reduce<string>`, preserving the redaction behavior while satisfying the build tsconfig.

Verification completed with `npm run build`, `npm run typecheck`, `CI=1 npm run test`, and `npm run lint` (passes with the existing React hook dependency warnings in `InspectionPopover` and `PreviewPane`).

### 2026-05-28 - #90 Agent pairing handoff menu wording

Worked only on #90.

Completed in this branch:

- Updated the Desktop Arcade Agent menu to use the `Koble til agent` section label, `Kopier agentkommando` copy action, helper text that says the command grants access to the active Arcade project while Agent access is active, and the visible warning `Del bare med agenten du vil gi tilgang.`.
- Kept endpoint, Authorization header, Agent pairing credential, and copied command text clipboard-only/not visibly rendered in the menu; the fetched agent-facing instruction payload remains English.
- Localized the same Agent menu checkpoint restore labels and changed integration coverage so copy success, failure, retry, safety copy, and hidden handoff details stay covered.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, and a Desktop Arcade browser smoke test against the dev server with a preload/clipboard stub.

Next note: future Agent pairing work should keep the secret-bearing command, endpoint, Authorization header, and Agent pairing credential out of visible UI and durable artifacts; treat the copied command as clipboard-only handoff material.

### 2026-05-28 - #90 Desktop dev surface follow-up

Fixed a follow-up bug in the same #90 feature:

- Root cause: `npm run desktop:dev` spawned Vite without `VITE_AKSEL_ARCADE_SURFACE=desktop`, so the dev renderer fell back to Web Arcade if viewed without the Electron preload bridge. That hid the Agent menu button and exposed the Share URL button.
- Set the desktop dev Vite process to `VITE_AKSEL_ARCADE_SURFACE=desktop`, documented the behavior, and added a regression test for the desktop dev script.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, targeted shell capability/script tests, and a browser smoke test against a Desktop-surface dev server confirming the Agent menu is visible and Share URL is absent.

### 2026-05-28 - #90 Desktop Agent command copy follow-up

Fixed another follow-up bug in the same #90 feature:

- Root cause: the sandboxed Electron preload script tried to `require('./agentHandoffRedaction.cjs')`, but Electron's sandboxed preload context cannot load project-local modules. The preload failed before exposing `__AKSEL_ARCADE_DESKTOP__`, so Agent access had no Desktop Agent transport endpoint and `Kopier agentkommando` had no command to copy.
- Inlined the small preload redaction helper to keep the preload self-contained while leaving the main-process CJS redactor in place for non-preload code.
- Added a regression test that prevents project-local `require('./...')` imports from returning to `desktop/preload.cjs`.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, targeted preload/script tests, and a Playwright Electron smoke test that starts Agent access and reaches `Agentkommando kopiert.`.

### 2026-05-28 - #90 Web/Desktop surface separation follow-up

Fixed a surface-mixing bug in the same #90 feature:

- Removed the env-based Desktop capability escape hatch. Desktop Arcade is now selected only through the Electron preload bridge; normal browser pages always resolve Web Arcade even if a Desktop-looking global is present.
- Electron now fails closed if the Desktop preload bridge is missing instead of silently rendering Web Arcade inside the Electron app.
- Updated tests and docs to encode the separation: browser pages show Share URL without Agent access, while Electron shows Agent access without Share URL.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, targeted shell/script/preload tests, and a shared-renderer smoke test proving the same Vite URL renders Web Arcade in Chromium and Desktop Arcade in Electron.

### 2026-05-28 - Desktop Agent menu gating

Worked only on the Desktop Arcade Agent action menu.

Completed in this branch:

- Gated the Desktop Agent action menu so the inactive state only shows `Koble til agent`, `Agent-tilgang`, and `Status: inaktiv`.
- Kept the helper copy, safety warning, copy action, feedback, and checkpoint restore controls hidden until `Agent-tilgang` is checked.
- Updated the Desktop Agent menu regression coverage to assert the inactive menu is minimal and the active menu restores the handoff content.

Verification completed with `npm run typecheck`, `CI=1 npm run test`, `npm run lint` (same existing hook dependency warnings), and a real Electron Desktop Arcade smoke test against the current dev renderer on port 5174.

Next note: keep future Agent pairing handoff UI Desktop-scoped and avoid showing the secret-bearing command before the user explicitly enables Agent access.

### 2026-05-28 - Desktop Agent menu width follow-up

Followed up on the Desktop Agent menu gating PR:

- Changed the Agent action menu from `max-width` only to a fixed reserved width capped by the viewport, so the inactive menu keeps the same width as the expanded menu.
- Verified in real Electron Desktop Arcade that the settled menu width is 360px both before and after `Agent-tilgang` is checked.

Next note: if more content is added to the Agent action menu, adjust the reserved `.agent-menu` width together with the full-content design to avoid reintroducing checkbox-triggered layout shift.

### 2026-05-29 - Issue #101: Remove Desktop Agent Checkpoints and rollback

Selected #101 as the highest-priority `ready-for-agent` issue from #100-#103 because ADR 0005 centers on removing Agent Checkpoints/rollback and #103 is explicitly blocked by this cleanup.

Implemented:

- Removed Desktop Agent Checkpoint state, rollback restore logic, and Agent menu restore controls.
- Changed accepted `applySourceChange` results to return only `changedFields`; accepted changes now apply immediately as ordinary Arcade project edits.
- Removed Checkpoint/rollback language from Agent instructions while keeping the existing `applySourceChange` command name for #103.
- Updated bridge, Desktop transport, share/export, and integration tests to assert no checkpoint identifiers or rollback UI are created or exported.

Verification:

- npm run typecheck
- CI=true npm run test
- npm run lint (passes with the existing hook dependency warnings)
- Browser smoke check at http://127.0.0.1:5173/aksel-arcade/ for app render and console output.

Next note: #103 can now handle the command rename/versioning separately; avoid reintroducing Agent-specific recovery state when updating the bridge contract.

### 2026-05-29 - Issue #102: Revoke Agent access on project replacement

Selected #102 as the highest-priority `ready-for-agent` issue from #100, #102, and #103 because it is unblocked and closes the Desktop Agent consent boundary called out in ADR 0005. #103 remains blocked by #101, and #100 is separate Web/Desktop surface cleanup.

Implemented:

- Added a Desktop Agent project-replacement lifecycle signal so replacement actions revoke any active Agent session before the new Arcade project is exposed.
- Routed import/open package, shared snapshot load, reset-to-intro, and built-in template/demo loads through replacement revocation with the internal `project-replaced` stop reason.
- Kept normal edits, project renames, viewport/theme changes, panel layout changes, and agent-applied changes from stopping Agent access.
- Added regression coverage for replacement revocation, stale bridge failures without stop-reason leakage, and non-replacement edits staying active.

Verification:

- npm run lint (passes with the existing hook dependency warnings)
- npm run typecheck
- CI=1 npm run test
- Desktop-mode browser smoke check at http://127.0.0.1:5173/aksel-arcade/ confirmed reset revokes the Agent session with `project-replaced`, removes the bridge/transport handler, returns the UI to inactive, and leaves no console errors.

Next note: keep future project replacement entry points wired through `replaceProject` or `notifyAgentSessionProjectReplaced()` before exposing replacement state; do not expose `project-replaced` over Agent protocol responses.

### 2026-05-29 - Issue #100: Remove Web Arcade Agent access surface

Selected #100 as the highest-priority `ready-for-agent` feature from #100 and #103 because it removes the remaining Web-facing Agent access surface, while #103 is a later bridge contract rename/versioning follow-up.

Implemented:

- Removed the legacy browser-global Agent bridge publication path (`window.__AKSEL_ARCADE_AGENT_BRIDGE__`) and its window typing.
- Kept Desktop Arcade Agent access routed only through the Desktop Agent transport handler while preserving the existing command router contract.
- Updated copied/inactive Agent instructions and current docs so Web Arcade is documented as having no Agent UI, Agent runtime, browser-global bridge, or Agent pairing handoff.
- Refactored Agent integration, share fallback, unit, and diagnostics coverage to exercise Desktop transport instead of the removed browser-global bridge.
- Added Web/Desktop regression assertions that Web Arcade keeps Share URL available without exposing any Agent access surface or legacy global.

Verification:

- npm run typecheck
- CI=1 npm run test -- --run
- npm run lint (passes with the existing hook dependency warnings)
- Browser smoke check at http://127.0.0.1:5173/aksel-arcade/ confirmed Web Arcade renders, has no console errors, no Agent access text, and no `window.__AKSEL_ARCADE_AGENT_BRIDGE__`.

Next note: #103 can rename/version the command contract separately. Do not reintroduce a Web Arcade Agent global or Web-facing pairing handoff while doing that follow-up; keep Agent access Desktop transport-only unless a new ADR reverses this boundary.

### 2026-05-29 - PR #107 review follow-up

Fixed one scoped build-mode regression found during PR review:

- Reproduced `npm run build` failing in `tsc -b` because the Desktop transport test clients returned broad routed result unions after the browser-global bridge removal.
- Tightened the transport test client method result types for project, preview, diagnostics, evidence, session state, instructions, and source changes.
- Replaced build-mode-unsafe dynamic `window` casts in the legacy-global absence assertions with `Reflect.get` / `Reflect.deleteProperty`.
- Swapped one nullable `window.__AKSEL_ARCADE_DESKTOP__` assertion for the existing Desktop preload fixture reference.

Verification completed with `npm run typecheck`, `npm run build`, `CI=1 npm run test -- --run`, and `npm run lint` (existing hook dependency warnings only).

### 2026-05-29 - Issue #103: Rename and version the Agent change command

Selected #103 because it was the single `ready-for-agent` feature requested for this branch.

Implemented:

- Renamed the Desktop Agent mutation command from `applySourceChange` to `applyAgentChange` across the Agent bridge, Desktop transport routing, copied instructions, and transport-backed tests.
- Bumped the Agent bridge protocol/instructions version to 2 and exposed that version from both `getAgentInstructions` and `getSessionState`.
- Kept successful Agent change responses scoped to `changedFields` only, with no checkpoint, rollback, summary echo, or change-record identifier.
- Left stale `applySourceChange` calls unsupported instead of shimming them to the new command.

Verification:

- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing hook dependency warnings)

Next note: future Agent protocol changes should keep Desktop transport as the only Agent access surface and should bump `AGENT_BRIDGE_PROTOCOL_VERSION` whenever command names or result shapes change.

### 2026-05-29 - Issue #109: Export clean Arcade project packages

Selected #109 as the highest-priority `ready-for-agent` feature from #109, #110, and #111 because #110 is blocked by #109 and #111 is blocked by both #109 and #110.

Implemented:

- Changed new `.akselarcade` exports to `formatVersion: 2` clean Arcade project packages containing only `project.name`, `project.source.jsx`, `project.source.hooks`, and `project.preview.viewport`.
- Removed AI metadata, setup/docs guidance, exported timestamps, local identity, project timestamps, diagnostics, preview evidence, workspace preferences, and legacy portable fields from new package exports.
- Kept empty Hooks source as an explicit empty string in the clean package shape.
- Added a minimal additive v2 package reader so newly exported packages remain importable; strict import rejection of legacy/noisy packages is still intentionally left for #110.
- Updated export/import regression tests and Desktop export fallback coverage for the clean `source`/`preview` package shape.

Verification:

- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5174/aksel-arcade/ confirmed the app renders, the Export button produces the clean v2 package shape, and the console is clear after reload.

Next note: #110 should replace the current permissive import path with strict clean-package validation, reject v1/noisy/legacy JSON packages and unknown extras, and keep imported workspace preferences sourced from the importing surface defaults.

### 2026-05-29 - PR #113 review follow-up

Addressed one scoped review finding before merge:

- Added explicit regression coverage for importing legacy `formatVersion: 1` Arcade project packages while #109 keeps the v1 reader as a compatibility bridge until #110 tightens import validation.

Verification:

- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test

### 2026-05-29 - Issue #110: Strict clean package import

Selected #110 as the highest-priority `ready-for-agent` feature from #110 and #111 because #111 is blocked by #110, so strict import validation has to land before UI/copy/test alignment.

Implemented:

- Restricted Web Arcade import to `.akselarcade` Arcade project packages only; `.json` and generic `application/json` file picker entries are no longer advertised.
- Replaced permissive legacy import handling with strict v2 clean package validation for the shared renderer import path used by Web Arcade and Desktop Arcade.
- Rejected raw legacy JSON projects, legacy `formatVersion: 1` packages, packages with `meta`/`exportedAt`, identity/timestamp fields, workspace preferences, diagnostics, preview extras, and any other unknown fields.
- Kept successful imports creating a fresh local Arcade project identity while restoring package name, JSX source, Hooks source, and viewport preview preference; panel layout comes from the local defaults.
- Updated Web/Desktop shell capabilities so `legacyJsonImport` is now false.
- Added storage and shell capability regression coverage for the strict contract, extension handling, fresh identity, and rejection paths.

Verification:

- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing hook dependency warnings)
- Browser smoke check at http://127.0.0.1:5176/aksel-arcade/ imported a clean `.akselarcade` package, restored name/source/Hooks/viewport, kept default panel layout, and had no console errors.

Next note: #111 can now align the visible import/export copy and broader validation tests with this strict clean package contract. Keep `.akselarcade` described as an Arcade project package, not a legacy JSON file or AI export bundle.

### 2026-05-29 - Issue #111: Align clean package UI and validation tests

Implemented:

- Kept the visible Export and Import controls as button-only affordances while aligning import accessibility/warning copy with Arcade project package terminology.
- Tightened import rejection messages so legacy/noisy/invalid package content clearly says it is not a clean `.akselarcade` Arcade project package.
- Updated stale Aksel metadata comments so copied-out AI/setup guidance is no longer described as part of Arcade project package exports.
- Updated the migration regression e2e flow away from legacy `.json` imports and metadata-rich exports; it now verifies the strict v2 clean package shape and `.akselarcade` import path.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5177/aksel-arcade/ confirmed Export emits the strict clean v2 package shape, Import loads a clean `.akselarcade` package with fresh local project state, and the console is clear after reload.

Next note: future package work should keep visible Export/Import UI minimal, keep clean packages limited to name/source/viewport, and update both unit/integration and e2e coverage if the package contract changes.

### 2026-05-29 - Issue #111 follow-up: Import confirmation Dialog

Implemented:

- Replaced the native import `confirm()` prompt with an Aksel `Dialog` so the destructive import confirmation can use the requested Norwegian message and a custom `Importer` action label.
- Added integration coverage that the Dialog appears, native `window.confirm` is not used, and the file picker is opened only from the `Importer` action.

Verification:

- npm run typecheck
- CI=1 npm run test -- tests/integration/project-controls-layout.test.tsx
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5178/aksel-arcade/ confirmed the Aksel Dialog text, `Avbryt` button, `Importer` button, and clear console.

### 2026-05-29 - Issue #116: Minimal Web share URL payloads

Implemented:

- Selected #116 as the highest-priority ready-for-agent issue in #116-#120 because it is unblocked and the dependency for the other Web share URL rollout slices.
- Changed new Web share URL generation to emit format v3 from a share-specific payload containing only Arcade project source (`jsx`, `hooks`) and shareable preview preferences (`viewport`, `theme`).
- Kept URL length, warning/oversize, strategy, and clipboard behavior locally computed while removing warning threshold and character limit metadata from v3 token metadata.
- Left legacy v1/v2/full-snapshot decoding in place for rollout compatibility, while v3 decode verifies checksums against the minimal serialized payload.
- Added focused coverage for v3 payload minimality, v3 token metadata shape, v3 decode, and legacy packed-token decode.

Verification:

- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5179/aksel-arcade/ generated a v3 share URL, decoded metadata as `{ v: 1, s: "brotli-wasm-b64url" }`, verified the decoded payload checksum, and confirmed no console errors after reload.

Next note: #117 should replace the current ProjectSnapshot compatibility adapter with deliberate fresh-local-project load semantics, keeping sender identity, timestamps, settings, active tab, zoom, and sandbox flags out of recipient state.

### 2026-05-29 - PR #122 follow-up: Fix production build

Fixed:

- Reproduced the merged-PR build failure with `npm run build`; `tsc -b` rejected the assertion helper in `src/utils/sharePayload.ts` even though `npm run typecheck` had passed.
- Converted the share payload record assertion helper from an inferred arrow function to a function declaration so TypeScript can apply the assertion signature under the production build project references.

Verification:

- npm run build
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing hook dependency warnings)

Next note: keep `npm run build` in the validation loop for share URL changes because it exercises `tsc -b`, which can catch declaration/assertion issues that `tsc --noEmit` misses.

### 2026-05-29 - Issue #117: Fresh v3 Web share URL loads

Implemented:

- Changed shared Web URL loading so applying a decoded share creates a fresh local Untitled Project identity instead of merging into the previous local project.
- Kept only the shared JSX, Hooks, viewport, and theme from the v3 URL; local project id, name, timestamps, schema version, panel layout, and active editor tab now come from fresh defaults.
- Added integration coverage for loading a v3 share over an existing local project, including new identity, default project name/layout/tab, shared source and preview state, and share query removal.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5175/aksel-arcade/ loaded a v3 share as a fresh Untitled Project with a new UUID, shared source, LG/light preview, default panel layout, stripped share query, and no console errors.

Next note: keep Web share URL loads constrained to shared source plus shareable preview preferences; do not reintroduce ProjectSnapshot identity, settings, workspace preferences, or active-tab restoration.

### 2026-05-29 - Issue #118: Normalize legacy v2 share links

Implemented:

- Selected #118 as the highest-priority ready-for-agent issue from #118-#120 because it is the compatibility slice that #120 depends on.
- Kept temporary v2 full-snapshot share-link decoding available, but normalized decoded v2 snapshots through the Web share URL boundary before application.
- Preserved only canonical JSX/Hooks source plus shareable preview viewport/theme from v2 links; sender schema version, file metadata, settings, timestamp, preview zoom, sandbox flags, and active tab are discarded.
- Rejected legacy v2 links that are missing either canonical source part instead of falling back to recipient/default code.
- Marked the v2 path as temporary in code/tests with a revisit note after one release cycle.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- npm run test -- --run
- Browser smoke check at http://127.0.0.1:5176/aksel-arcade/ confirmed the app renders and the console is clear.

Next note: #120 can now add minimality/URL-size proof coverage on top of both v3 minimal payloads and the v2 normalization/rejection path.

### 2026-05-29 - Issue #119: Clean share-load UI wording and metadata

Implemented:

- Selected #119 as the highest-priority ready-for-agent issue from #119-#120 because #119 unblocks #120, while #120 depends on this recipient-facing wording cleanup.
- Updated the share-load warning to describe loading a shared project instead of a shared snapshot.
- Removed the sender-facing `Last updated` timestamp from the warning so Web share URL loads do not expose metadata outside the agreed share boundary.
- Kept the warning that loading the shared project replaces current work, and kept both load and dismiss actions wired to the existing share hydration handlers.
- Added focused App-level regression coverage for the warning copy, absence of snapshot/timestamp metadata, and the load/dismiss actions.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with the existing hook dependency warnings)
- CI=1 npm run test
- Browser smoke check at http://127.0.0.1:5177/aksel-arcade/ confirmed the updated warning wording, no `Last updated` metadata, a working load action, and no console errors.

Next note: #120 can now add minimality and URL-size proof coverage without carrying share-load UI wording debt; keep recipient-facing Web share copy aligned with "shared project" and avoid reintroducing sender timestamps or metadata.

### 2026-05-29 - Issue #120: Prove Web share URL payload minimality

Implemented:

- Added focused unit coverage proving newly generated v3 Web share URL payloads contain only source JSX/Hooks plus shareable preview viewport/theme.
- Expanded the v3 minimality assertions to reject settings, timestamps, file metadata, project identity/name/version values, active snapshot/editor file state, preview zoom, sandbox flags, warning thresholds, and character limits.
- Added representative URL-size comparisons showing v3 share URLs are shorter than equivalent legacy v2 full-snapshot URLs.
- Extended the UI-generated share-link E2E hydration test to assert the copied token is v3 and that loading it creates a fresh Untitled Project rather than preserving the sender project name.

Verification:

- npm install
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with the existing hook dependency warnings)
- npm run test:e2e -- share-link.spec.ts -g "hydrates a shared snapshot"

Next note: the full `share-link.spec.ts` Playwright file still has an older large-template scenario that can enter the share-generation error state locally; this change only adds the v3/fresh-project assertions to the hydrate path and does not modify that size-limit scenario.

### 2026-05-30 - Issue #128: Persist Web Arcade working copies per tab

Implemented:

- Selected #128 as the highest-priority ready-for-agent issue from #128-#131 because #129, #130, and #131 are explicitly blocked by the tab-scoped working-copy model.
- Moved Web Arcade autosave/load/reset persistence from the legacy browser-wide `localStorage` last-project key to tab-scoped `sessionStorage` under a versioned Web Arcade working-copy envelope.
- Stored the full current working-copy surface needed for reload/duplicate semantics: project name, Arcade project source, viewport Preview preference, theme Preview preference, and panel-order Workspace preference.
- Kept legacy `localStorage` project data ignored rather than migrated, so ordinary new Web Arcade URLs start from the default Untitled Project while same-tab reloads restore the tab working copy.
- Added storage and integration coverage for tab reload restore, legacy browser-wide data ignoring, sessionStorage duplicate-tab fork modeling, and independent divergence after a copied working copy is edited.
- Updated browser/E2E setup and README wording to reflect tab-scoped reload safety instead of a durable browser-wide project library.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

- Browser smoke check at http://127.0.0.1:5173/aksel-arcade/ confirmed sessionStorage reload restore, ordinary new-tab default behavior, and no runtime console errors.

Next note: #129 can now scope explicit replacement flows (Web share URL, import, Reset editor) to the current Web Arcade working copy. Keep #130's UI copy cleanup separate; the Clear storage & reload affordance still exists pending that issue.

### 2026-05-30 - PR #133 review follow-up

Fixed one scoped ADR 0008 issue before merge:

- Removed normal autosave `saving`/`saved`/idle status from the Web UI, leaving only an autosave failure indicator because save failure means same-tab reload safety is broken.
- Added focused component coverage so normal autosave statuses render nothing while failure status remains visible.

### 2026-05-30 - Issue #129: Scope Web Arcade replacement actions

Implemented:

- Selected #129 as the highest-priority ready-for-agent issue from #129-#131 because #131 is blocked by #129/#130 and #129 is the foundational correctness fix for explicit replacement flows.
- Centralized Web Arcade working-copy replacement so import, Web share URL load, and Reset editor replace the current working copy with fresh local project state, reset editor/preview state, and avoid touching other open tabs.
- Reset editor now creates a fresh default Untitled Project identity instead of preserving the previous project id/name/source wrapper.
- Web share URL loads carry shareable preview theme/viewport while resetting workspace preferences; Arcade project package imports reset workspace preferences because packages do not carry them.
- Updated user-facing replacement copy to use Web share URL, Arcade project package, Reset editor, and Web Arcade working copy terms.
- Added regression coverage for Reset editor fresh identity/default preferences and a two-tab Web share URL replacement path proving the original tab working copy and preferences stay unchanged.

Verification:

- npm install
- npm run typecheck
- npm run test -- --run
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- Browser smoke check at http://127.0.0.1:5174/aksel-arcade/ confirmed app render, Web share URL copy, Reset editor confirmation, and no runtime console errors.

Next note: #130 should remove the remaining Clear storage & reload affordance and finish the Web Arcade storage/autosave status UI cleanup; this branch intentionally leaves that separate feature untouched.

### 2026-05-30 - Issue #130: Remove misleading Web Arcade storage UI

Implemented:

- Selected #130 as the highest-priority ready-for-agent issue from #130-#131 because #131 is explicitly blocked by #130 and should wait for this UI cleanup.
- Removed the Web Arcade Settings > Editor "Clear storage & reload" action so the UI no longer suggests a browser-wide saved project or durable storage reset workflow.
- Kept Reset editor available as the explicit action for replacing only the current Web Arcade working copy with the default Untitled Project.
- Removed the now-unused clearStorage UI handler and its direct unit coverage; tab-scoped persistence remains owned by save/load and reset replacement flows.
- Added integration coverage that Reset editor remains visible while Clear storage & reload is absent.
- Confirmed the existing SaveStatusIndicator behavior still hides normal idle/saving/saved autosave states and only surfaces autosave failure feedback.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- npm run test
- Browser smoke check at http://127.0.0.1:5174/aksel-arcade/ confirmed the app renders, Settings > Editor shows Reset editor only, Clear storage & reload is absent, and there are no runtime console errors.

Next note: #131 can now add multi-tab regression coverage across edit, reload, duplicate, share/import/reset, and autosave-error paths with #130's UI cleanup in place.

### 2026-05-30 - Issue #131: Multi-tab Web Arcade regression coverage

Implemented:

- Added Playwright regression coverage dedicated to Web Arcade tab-scoped working copies.
- Covered two ordinary Web Arcade tabs starting from independent default working copies, then diverging through edits without cross-tab overwrite.
- Covered same-tab reload restoring only that tab's saved working copy while another tab keeps its own source.
- Covered browser tab duplication by opening from the current tab, verifying the visible working copy is forked, and proving later edits diverge independently.
- Covered Web share URL load, Arcade project package import, and Reset editor replacement in one tab while another tab remains unchanged.
- Covered autosave failure feedback when tab-scoped `sessionStorage` persistence throws, preserving the #130 rule that normal autosave status stays hidden but failures remain visible.

Verification:

- npm install
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

- CI=1 npm run test:e2e -- web-working-copy-tabs.spec.ts

Next note: #131 is implemented as regression coverage only. The Playwright config reuses any existing dev server locally, so if a stale server is already listening on port 5173, stop that specific process or run in CI mode before re-running this spec.

### 2026-05-30 - Issue #144: Unsigned local Desktop Arcade packaging

Implemented:

- Selected #144 as the highest-priority ready-for-agent issue from #143-#150 because #147, #148, and #149 explicitly depend on #144, while #143 is duplicate text and later issues are blocked.
- Added `desktop:build`, `desktop:package`, `desktop:package:mac`, `desktop:package:win`, and `desktop:icons` npm scripts.
- Added a Desktop-specific Vite config that emits relative asset URLs into `dist-desktop`, separate from the Web Arcade GitHub Pages `dist` build.
- Added electron-builder config for the unsigned local target matrix: Windows x64 NSIS, macOS Apple Silicon DMG, and macOS Intel DMG.
- Added Desktop app metadata for `Aksel Arcade` with `no.nav.aksel.arcade`, plus Desktop version injection via `AKSEL_ARCADE_DESKTOP_VERSION` without changing committed package versions.
- Added icon generation from `public/aksel-favicon.svg` into local `build/desktop` `.ico` and `.icns` resources.
- Updated docs for local unsigned packaging commands, artifact names, host prerequisites, and no-publish behavior.
- Added unit coverage for Desktop build profile, artifact naming, metadata, target matrix, version injection, icon wiring, and unsigned publish settings.

Verification:

- npm run desktop:build
- npm run typecheck
- npm run test -- --run
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- npx electron-builder --config electron-builder.config.cjs --dir --publish never
- AKSEL_ARCADE_DESKTOP_VERSION=0.1.0 npm run desktop:package:mac

Next note: Windows installer generation is configured as `desktop:package:win`; running it from macOS or Linux may require Wine, as documented.

### 2026-05-31 - Issue #145: Desktop Arcade release planning

Implemented:

- Selected #145 as the highest-priority ready-for-agent issue from #143-#150 because #143 duplicates the already-present local packaging slice, while #147-#150 are blocked by earlier Desktop release automation work.
- Added `npm run desktop:release-plan` backed by `scripts/desktop-release-plan.cjs`.
- Added release/no-release classification for Desktop shell files, release automation, release-specific docs, generated Desktop resources, public/shared renderer assets, and Web-only or unrelated changes.
- Added Desktop release tag planning with `desktop-vX.Y.Z` parsing, malformed/unrelated tag filtering, first-release `0.1.0`, and later patch bumps from the highest valid Desktop tag.
- Added manual recovery dispatch validation that rejects refs not already verified on protected `master`.
- Modeled Desktop version injection as `AKSEL_ARCADE_DESKTOP_VERSION` workspace state with no committed `package.json` version bump.
- Documented the release planning helper and environment inputs in `docs/desktop-release.md`.
- Added unit coverage for no-release changes, Desktop-impacting changes, first release, patch bumping, malformed tags, shared renderer changes, and accepted/rejected manual recovery refs.

Verification:

- npm run typecheck
- npm run test -- --run
- CI=1 npm run test
- npm run type-check
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- DESKTOP_RELEASE_CHANGED_FILES=$'src/App.tsx\n.github/workflows/deploy.yml' DESKTOP_RELEASE_TAGS=$'desktop-v0.1.0\ndesktop-v0.1.bad\nv1.0.0' npm run desktop:release-plan
- Browser smoke check at `http://127.0.0.1:5174/aksel-arcade/`

### 2026-05-31 - PR #152 review follow-up

Fixed:

- Marked `vite.config.ts` as Desktop-impacting because `vite.desktop.config.ts` merges the shared Vite config into Desktop builds.
- Added unit coverage so shared Vite config changes require a Desktop release, and removed `vite.config.ts` from the no-release test case.

Verification:

- npm run typecheck
- CI=1 npm run test
- npm run type-check
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- DESKTOP_RELEASE_CHANGED_FILES='vite.config.ts' DESKTOP_RELEASE_TAGS='desktop-v0.2.6' npm run desktop:release-plan
- Browser smoke check at `http://127.0.0.1:5174/aksel-arcade/`

### 2026-06-12 - Issue #214: Fix menu, overlay, and help component insertions

Implemented:

- Selected #214 as the highest-priority open `ready-for-agent` feature from #209-#217 after checking #205, `CONTEXT.md`, and the existing `progress.txt` notes; it was the most user-blocking remaining authoring slice because `ActionMenu`, `Dropdown`, `Popover`, `Tooltip`, and `HelpText` insertion paths were still incomplete.
- Added shared catalog-backed insertion coverage for `ActionMenu`, `Dropdown`, `HelpText`, `Popover`, and `Tooltip` in `src/data/akselCatalog.ts`, including shared placement/strategy value sets for the overlay/help entries.
- Kept `Popover` composable by making it a Hooks-backed helper insertion (`<DeadlinePopover{{popoverSuffix}} />`) instead of inserting stateful wrapper JSX directly into the authoring surface.
- Added regression coverage across autocomplete, catalog, component insertion, and Add-menu boundaries in `tests/unit/akselAutocomplete.test.ts`, `tests/unit/akselCatalog.test.ts`, `tests/unit/services/componentInsertion.test.ts`, and `tests/integration/component-palette.test.tsx`.
- Fixed one leftover contextual autocomplete mismatch before closing the issue: `Dropdown.Menu` children now suggest `Dropdown.Menu.Divider` with the correct inserted JSX even though the upstream docs dataset still exposes the older `Dropdown.Divider` entry name.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4321/aksel-arcade/` verified Add-menu insertion plus working preview interactions for `ActionMenu`, `Dropdown`, `Popover`, `Tooltip`, and `HelpText`; clean runtime interactions had no console errors after insertion

Next note:

- The docs-derived autocomplete dataset still contains `Dropdown.Divider`, so the runtime now aliases that docs entry to the author-facing `Dropdown.Menu.Divider` name. If the generator is ever refreshed against corrected upstream docs, keep the alias path or remove it only after confirming contextual autocomplete still surfaces the runtime name and insertion.

### 2026-06-12 - PR #228 follow-up for Issue #214

Fixed after user feedback:

- The first `Popover` insertion shipped too much pre-baked behavior by hiding the actual Aksel structure behind `<DeadlinePopover{{popoverSuffix}} />`.
- Replaced that helper-component insertion with the direct `Button` + `Popover` JSX structure the user asked for, keeping only the required `anchorEl`, `openState`, and `popoverId` bindings in the Hooks tab.
- Taught the preview transpiler to lift bare top-level hook bindings like `const [openState, setOpenState] = useState(false)` and `const popoverId = useId()` from page Hooks into the generated page component, so the direct Popover snippet renders without wrapper components or extra editor mutations.
- Added follow-up regression coverage in `tests/integration/transpiler.test.ts` alongside the catalog/autocomplete/palette/component-insertion assertions to lock down the new direct Popover insertion shape.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4321/aksel-arcade/`: after clearing the saved JSX/Hooks state, confirmed Add -> `Popover` inserts the direct `Button` + `Popover` JSX plus raw hook bindings, a reload preserves that shape, clicking `Åpne popover` reveals `Innhold her!`, and the clean reloaded session shows no console errors beyond the pre-existing form-field issue

### 2026-06-12 - PR #228 review follow-up for Issue #214

Fixed before merge:

- Replaced the first line-based page-Hooks lifting pass in `src/services/transpiler.ts` with top-level statement extraction backed by Babel's parser so multiline hook bindings also move into the generated page component instead of remaining at module scope.
- Kept the lifting scope narrow to direct top-level hook-backed `const`/`let` declarations, preserving the earlier direct Popover insertion behavior while fixing the review-reported invalid-hook-call edge case.
- Added a regression in `tests/integration/transpiler.test.ts` that exercises multiline top-level hook bindings in the Hooks tab, and confirmed the direct Popover snippet still renders correctly when those bindings span multiple lines.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4321/aksel-arcade/`: replaced the editor content with the direct Popover JSX plus multiline `useState` / `useId` hook bindings, confirmed the preview renders `Åpne popover`, and confirmed clicking it still shows `Innhold her!` with no new Popover/transpiler runtime errors

### 2026-05-31 - Issue #147: Unsigned PR Desktop packaging checks

Implemented:

- Selected #147 as the single ready-for-agent feature from #143-#150 because #143/#144 local unsigned packaging and #145 release planning were already present, while #147 is the next unblocked release-safety check before signed publication work.
- Added `npm run desktop:pr-package-plan` backed by `scripts/desktop-pr-packaging-plan.cjs` to reuse Desktop-impacting change detection for pull requests without reading Desktop release credentials or tags.
- Added `.github/workflows/desktop-pr-packaging.yml` on `pull_request` only, with read-only permissions, concurrency cancellation, changed-file collection via the PR files API, and conditional packaging only when Desktop-impacting files changed.
- Added unsigned macOS and Windows packaging matrix legs that inject `AKSEL_ARCADE_DESKTOP_VERSION`, disable code-signing discovery, avoid Desktop release secrets/environments, and assert the expected DMG/NSIS files exist in `release/desktop`.
- Documented the PR packaging check contract in `docs/desktop-release.md`.
- Added unit coverage for the PR packaging plan, skipped non-Desktop PRs, scalar GitHub outputs, workflow trigger safety, unsigned package commands, artifact assertions, and no publish/release behavior.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test
- DESKTOP_RELEASE_CHANGED_FILES='src/App.tsx' npm --silent run desktop:pr-package-plan
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/`

Next note: The PR packaging workflow intentionally does not upload the unsigned installers; it only verifies that electron-builder produced the expected local files. Later signed release issues should keep using the protected `desktop-release` environment instead of extending this PR workflow.

Review follow-up:

- Fixed PR file collection so renamed files pass both the new `filename` and old `previous_filename` into the Desktop packaging planner.
- Added regression coverage for renamed files whose previous path was Desktop-impacting, plus workflow coverage that the PR files API keeps `previous_filename`.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

### 2026-05-31 - Issue #143: Unsigned local Desktop install artifacts

Verified:

- Checked `CONTEXT.md` Desktop language and `docs/desktop-release.md` local unsigned packaging contract before touching the feature.
- Confirmed the current branch already contains the #143 local packaging path: `npm run desktop:build` emits `dist-desktop` with relative asset URLs, Electron Builder targets Windows x64 NSIS plus macOS arm64/x64 DMGs without publishing, artifact names include product/version/OS/architecture, installed metadata uses `Aksel Arcade` and `no.nav.aksel.arcade`, and desktop icons are generated from `public/aksel-favicon.svg`.
- Confirmed `tests/unit/desktopPackagingConfig.test.ts` covers the Desktop build profile, app metadata, target matrix, artifact naming, icon wiring, version injection, and local unsigned packaging commands.

- No acceptance gap required a code change in this pass; kept the work scoped to the single #143 feature.

Verification:

- npm run typecheck
- npm run lint
- npm test -- --run

Next note: #143 appears implemented by the existing Desktop packaging work on this branch. Later Desktop work should stay in separate signed-release or CI packaging slices rather than expanding this local unsigned installer feature.

### 2026-05-31 - Issue #146: Protected Desktop release credentials

Implemented:

- Selected #146 as the single feature for this pass and kept the work scoped to Desktop release credential provisioning.
- Created/configured the GitHub environment `desktop-release` with no required reviewers or wait timer.
- Restricted `desktop-release` to the custom environment branch policy `master`; repository branch protection now reports `master` as protected.
- Confirmed the six required environment secret names are present: `MAC_CERTIFICATE_P12_BASE64`, `MAC_CERTIFICATE_PASSWORD`, `APPLE_API_KEY_BASE64`, `APPLE_API_KEY_ID`, `APPLE_API_ISSUER_ID`, and `APPLE_TEAM_ID`.
- Added `npm run desktop:release-env-check` backed by `scripts/desktop-release-environment-check.cjs` so maintainers can verify environment metadata and secret names without reading or printing secret values.
- Documented maintainer-only environment setup, secret-name setup, and the no-secret-value handling rule in `docs/desktop-release.md`.
- Added unit coverage for the readiness helper: missing environment, manual approval, broad branch policies, non-master policies, unprotected master, missing secret names, insufficient secret-name permissions, and the ready state.

Verification:

- npm run desktop:release-env-check
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=true npm run test

Desktop release workflow (#149) review follow-up:

- Code review found that `workflow_dispatch` recovery accepted runs launched from non-`master` workflow refs as long as the requested recovery SHA was already on protected `master`.
- Tightened Desktop release planning so manual recovery now requires both the workflow ref and the target commit to be on protected `master`.
- Added regression coverage for rejecting non-`master` recovery workflow refs separately from rejecting recovery commits outside protected `master`.

Verification:

- CI=1 npm run test -- --run tests/unit/desktopReleasePlan.test.ts tests/unit/desktopReleaseWorkflowPlan.test.ts tests/unit/desktopReleaseWorkflow.test.ts
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)

Desktop release workflow (#149):

- Selected #149 over #150 because #150 depends on the Desktop release workflow.
- Added `.github/workflows/desktop-release.yml` for protected `master` pushes and guarded `workflow_dispatch` recovery releases from commits already reachable from protected `master`.
- Release planning now prevents duplicate public Desktop releases for the same release commit, injects `AKSEL_ARCADE_DESKTOP_VERSION` without committing version bumps, and publishes only after signed macOS DMGs and the Windows x64 installer are built and validated.
- The publish job creates a draft release, uploads exactly the three required installer assets, validates the asset set, then publishes as non-draft/non-prerelease/latest; cleanup removes draft/tag state before publication if a publish step fails.
- Added workflow-plan and workflow regression tests for triggers, artifact aggregation, release-note/download-guide handling, duplicate-release prevention, and no-checksum publication.

Verification:

- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)

### 2026-05-31 - Issue #148: Signed and notarized macOS Desktop artifacts

Implemented:

- Selected #148 as the highest-priority `ready-for-agent` feature from #148-#150 because #149 depends on signed/notarized macOS artifacts and #150 depends on the release workflow after that.
- Kept the work scoped to macOS signing/notarization only; did not add the public GitHub Release publishing workflow from #149.
- Added release signing mode to the Electron Builder config while preserving unsigned local/PR packaging defaults.
- Added minimal macOS hardened-runtime entitlements in `desktop/entitlements.mac.plist`.
- Added `npm run desktop:package:mac:release`, which validates Desktop release credentials without printing values, imports the Developer ID Application certificate into a temporary keychain, builds both macOS DMGs, notarizes with the App Store Connect API key flow, staples, locally validates, and removes the temporary keychain/files before exit.
- Added unit coverage for secret-safe credential validation, invalid base64 setup errors, release entitlements/config, expected arm64/x64 DMG names, API-key notarization arguments, validation failure propagation, and cleanup after packaging failure.
- Documented the signed macOS release packaging contract in `docs/desktop-release.md`.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

Next note: #149 can now wire the full Desktop release workflow around `npm run desktop:package:mac:release` for macOS and the existing Windows packaging path. Keep GitHub Release creation, artifact upload, version tagging, and partial-release prevention in #149 rather than expanding this signing slice.

Next note: The Desktop release credentials are now configured as GitHub environment secrets, but their values must remain maintainer-only. Future signed/notarized release workflow work should reference only the `desktop-release` environment and these secret names; do not copy values into source, logs, release assets, runtime state, or Arcade project data.

Review follow-up:

- Fixed readiness output so failed manual-approval, unprotected-master, and branch-policy checks describe the actual misconfiguration instead of reusing pass-state wording.
- Reserved exit code 2 for the explicit insufficient-permission readiness path; unexpected CLI/API failures now exit 1 instead of looking like permission failures.
- Added regression coverage for the clearer failure messages and unexpected CLI failure exit behavior.

Verification:

- npm test -- --run tests/unit/desktopReleaseEnvironmentCheck.test.ts
- npm run desktop:release-env-check
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=true npm run test

Desktop release build failure follow-up:

- Investigated failed run https://github.com/navikt/aksel-arcade/actions/runs/26716777751. The Desktop release planner and Windows package job passed; the signed macOS packaging job failed in `npm run desktop:package:mac:release`.
- The macOS release wrapper previously captured child process output and only printed the failing command label/exit code, so the CI log hid the underlying electron-builder/codesign/notarytool error.
- Tightened the signing path by disabling electron-builder's automatic app notarization in release signing mode; the documented release contract notarizes and staples the final DMGs in `desktop-macos-release-signing.cjs`.
- Added the temporary signing keychain to the user keychain search list before importing/signing, persisted the previous search list in signing state, and restored it before deleting the temporary keychain. This matches electron-builder's own certificate-chain workaround and helps codesign resolve the Developer ID chain.
- Release packaging command failures now include non-secret stdout/stderr so future CI failures expose the actual failing tool output.
- Added regression coverage for release config notarization settings, keychain search-list parsing/update/restore, and surfaced packaging command output.
- Review follow-up: hardened cleanup so a failed keychain search-list restore no longer skips temporary keychain deletion or temp directory removal.

Verification:

- CI=1 npm run test -- --run tests/unit/desktopMacosReleaseSigning.test.ts tests/unit/desktopPackagingConfig.test.ts
- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- AKSEL_ARCADE_DESKTOP_VERSION=0.1.0 npm run desktop:package:mac

Desktop release publish failure follow-up:

- Follow-up run https://github.com/navikt/aksel-arcade/actions/runs/26718523535 confirmed the macOS signing/package fix worked: both signed macOS installers and the Windows installer built and uploaded successfully.
- The publish job then failed because it intentionally has no repository checkout, but `gh release upload/view/delete` was called without `--repo`, causing `gh` to try and fail to infer a repo from `.git`.
- Updated all `gh release` calls in the publish and failure-cleanup steps to pass `--repo "$GITHUB_REPOSITORY"` explicitly.
- Added workflow regression coverage so future release publish commands keep an explicit repository context.

Verification:

- CI=1 npm run test -- --run tests/unit/desktopReleaseWorkflow.test.ts
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

Issue #150 documentation progress:

- Kept the work scoped to documentation for "Document Desktop Arcade download and recovery flow"; did not change release scripts, workflows, packaging code, or unrelated features.
- Read `CONTEXT.md`, `docs/desktop-release.md`, ADR `docs/adr/0009-desktop-arcade-release-pipeline.md`, and the implemented Desktop release workflows before editing.
- Updated `README.md` with user-facing Desktop Arcade download guidance: GitHub Releases are the supported download channel, Web Arcade remains browser-hosted, and Windows x64, Mac Apple Silicon, and Mac Intel users are mapped to the expected install artifact names.
- Expanded `docs/desktop-release.md` with the same download channel/artifact mapping plus maintainer recovery dispatch rules, failure expectations, the Desktop release credential security boundary, and explicit initial out-of-scope items: no Web Arcade download UI, no in-app updates, no package-manager distribution, no initial Windows signing, and no checksum artifacts.

Verification:

- npm run typecheck
- CI=1 npm run test

Desktop release duplicate draft follow-up:

- Replacement run https://github.com/navikt/aksel-arcade/actions/runs/26718889270 completed successfully, but GitHub release state showed two `desktop-v0.1.0` releases: a stale draft from the previous failed run containing the three installer assets, and a public release from the rerun with zero assets.
- Root cause: `gh release upload/view "$DESKTOP_TAG"` can resolve a stale draft release when duplicate draft state exists for the same tag, while the workflow publishes the newly-created release by ID.
- Removed the broken public release, stale draft release, and tag so the fixed workflow can republish `desktop-v0.1.0` cleanly.
- Updated the publish step to delete stale draft releases for the target Desktop tag, and to remove the stale tag when no public release owns it, before creating a new draft release.
- Added workflow regression coverage for stale draft cleanup before publish.

Verification:

- CI=1 npm run test -- --run tests/unit/desktopReleaseWorkflow.test.ts
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

### 2026-06-01 - Issue #163 compact Agent operating instructions

Completed for #163:

- Kept the work scoped to `getAgentInstructions` and the Desktop Arcade Agent pairing flow.
- Replaced the fetched `instructionsMarkdown` body with a compact 10-line Agent operating guide; the copied pairing handoff remains only the short `curl` bootstrap command.
- The guide tells external agents to treat the returned guidance as authoritative for the active Agent session, call `getProject` first, use import-free Arcade JSX and Hooks, use preview context/diagnostics/Preview evidence for feedback, submit full-field `applyAgentChange({ summary, jsxCode?, hooksCode?, viewportSize?, theme?, name? })` replacements, and validate after diagnostics settle.
- Kept existing structured session, endpoint, Authorization, permission, protocol, command-name, and Arcade authoring contract fields available without adding a new machine-readable workflow field.
- Kept the response content-free: no active Arcade project source, diagnostics content, share/package data, repository documentation content, provider-specific behavior, or Aksel Design System training is included in the fetched guide.
- Updated `tests/unit/services/agentBridge.test.ts` with behavioral assertions for the operating-guide requirements instead of an exact full Markdown snapshot.

Verification:

- npm run typecheck
- CI=1 npm run test
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` for app render, preview render, and no console warnings/errors.

### 2026-06-01 - Issue #164 remove obsolete long-form Agent instruction helpers

Completed for #164:

- Kept the work scoped to the Desktop Arcade Agent pairing flow and did not add or change unrelated features.
- Removed the obsolete exported `createAgentInstructions` helper that built the old long copied prompt with duplicated transport prose, inline read/change curl examples, and provider-specific Copilot usage examples.
- Kept the canonical copied Agent pairing handoff as the short one-line authenticated `curl` bootstrap request for `getAgentInstructions`.
- Updated `tests/unit/services/agentBridge.test.ts` so it no longer asserts old long copied examples or provider-specific prompt content.
- Strengthened the `getAgentInstructions` test to verify the compact Agent operating guide can be fetched repeatedly during an active Agent session without including Arcade project content.
- Preserved the supported Agent bridge command list and structured protocol/session fields on the bridge-returned instructions payload.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` for app render, preview render, and no console warnings/errors.

### 2026-06-01 - Issue #165 keep Agent operating instructions hidden and redacted

Completed for #165:

- Kept the work scoped to the Desktop Arcade Agent menu/redaction feature.
- Split redaction terminology so copied Agent pairing handoffs and pairing credentials still redact as Agent pairing handoff data, while `instructionsMarkdown` content now redacts as Agent operating instructions.
- Tightened the copied handoff redactor so it stops at the `getAgentInstructions` bootstrap `--data` argument instead of swallowing later log payloads on the same line.
- Made `instructionsMarkdown` redaction quote-aware so commas, braces, and `applyAgentChange({ ... })` examples inside the Agent operating instructions cannot leak tail content.
- Applied the same redaction behavior in renderer code, the Desktop loopback helper, and the sandboxed preload copy.
- Strengthened tests for copy failure/retry feedback, hidden menu content, Web Arcade Agent absence, and redaction coverage for copied handoffs, endpoint/Auth data, pairing credentials, and Agent operating instructions.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` for app render, preview render, Web Arcade Agent absence, and no console warnings/errors.

### 2026-06-01 - Issue #166 validate Agent operating instructions happy path

Completed for #166:

- Kept the work scoped to the Desktop Arcade Agent pairing and bridge happy path.
- Added an E2E scenario that starts Desktop Arcade Agent access, copies the hidden Agent pairing handoff, parses the one-line `getAgentInstructions` bootstrap request, follows the returned Agent operating instructions, calls `getProject` first, applies a full-field `applyAgentChange`, polls `getDiagnostics` until the preview settles, and reads Preview evidence when permission is available.
- Fixed the real-browser Preview evidence path so Agent bridge reads capture evidence through the sandbox message channel instead of parent-side iframe document access, preserving the existing `sandbox="allow-scripts"` isolation.
- Shared the existing `previewEvidence` serializer into the sandbox bundle as `preview-evidence-global.js` to avoid duplicated sanitization logic.
- Kept non-evidence Agent bridge commands synchronous while allowing `getPreviewEvidence` and Desktop transport routing to await async evidence capture.
- Updated affected unit and integration coverage for async Preview evidence routing and handler registration.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- npm run test -- --run
- npx playwright test tests/e2e/agent-diagnostics.spec.ts

### 2026-06-01 - PR #173 review follow-up

Reviewed and fixed one PR-scoped build issue before merge:

- Production build type-checking caught the new Agent diagnostics E2E helper narrowing JSON-RPC request IDs to `string`, even though Desktop transport IDs can be `string | number | null`.
- Widened the test transport helper to the same JSON-RPC ID shape and fixed the typed `page.waitForFunction` call that passes a global-name argument.
- Kept the change scoped to the issue #166 E2E test; no product behavior changed.

Verification:

- npm run build
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- npm run test -- --run
- npx playwright test tests/e2e/agent-diagnostics.spec.ts

### 2026-06-01 - PR #174 review follow-up

Reviewed and fixed one PR-scoped test coverage issue before merge:

- The Desktop renderer protocol smoke test verified preview rendering through `aksel-arcade://app/index.html`, but it launched Electron in unpackaged mode and therefore did not prove the packaged `app.isPackaged` startup branch selects that protocol.
- Added a focused Desktop main-process unit harness that runs `desktop/main.cjs` with `app.isPackaged = true` and verifies the packaged startup path loads `aksel-arcade://app/index.html`, registers the privileged protocol, and installs the protocol handler.
- Renamed the Electron smoke test wording to describe the Desktop renderer protocol coverage it actually provides while keeping the real preview-rendering assertion.

Verification:

- npx vitest run tests/unit/desktopMain.test.ts tests/unit/desktopPackagingConfig.test.ts
- npx playwright test tests/e2e/desktop-packaged-preview.spec.ts --reporter=line
- npm run type-check
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- npm test -- --run
- npm run build
- Browser smoke check at `http://127.0.0.1:5174/aksel-arcade/` for app render, preview render, and no console errors.

### 2026-06-01 - `bg-default` background autocomplete

Fixed a background-token autocomplete gap for Aksel primitives:

- Reproduced that `default` existed as the valid Box background fragment, but typing the full Aksel token name prefix `bg-` did not surface `bg-default` in primitive background prop completions.
- Added background-token alias matching in `src/services/akselAutocomplete.ts`, so `bg-*` queries show full token-name labels such as `bg-default` while still applying the valid primitive prop fragment (`default`) to avoid invalid `background="bg-default"` output.
- Added unit coverage for empty background suggestions, `bg-default` visibility, and `bg-default` applying as `default`.
- Added browser autocomplete coverage proving the real CodeMirror tooltip includes `bg-default`.

Verification:

- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test
- npx playwright test `tests/e2e/autocomplete.spec.ts` on an isolated local Vite port
- Browser smoke check at `http://127.0.0.1:5175/aksel-arcade/` for app render, preview render, `bg-default` autocomplete visibility, and no console errors after resetting local/session storage.

Next note:

- Keep Aksel primitive background prop values as token fragments in source. If future users search by CSS variable/token names (`bg-*`), extend the alias-label path rather than changing emitted prop values to include the `bg-` prefix.

### 2026-06-01 - Desktop follow-up for preview/autocomplete fixes

Extended the same fixes to the Desktop Arcade packaged renderer path:

- Confirmed the Desktop app builds from the shared React/Codemirror source, so the `bg-default` alias and general prop-value filtering fixes are included in Desktop builds.
- Added Desktop renderer protocol coverage in `tests/e2e/desktop-packaged-preview.spec.ts` for the packaged `aksel-arcade://app/index.html` path.
- The Desktop test now verifies the preview surface, live preview wrapper, and sandbox iframe all use `--ax-bg-default`, and that prop-value autocomplete filters after opening a value list (`size=""` -> typing `s` leaves `small` without stale `medium`/`xsmall` entries).

Verification:

- npm run typecheck
- npx playwright test tests/e2e/desktop-packaged-preview.spec.ts --reporter=line

### 2026-06-01 - Desktop dev stale renderer fix

Fixed the Desktop dev launcher after testing showed it could open stale code:

- Reproduced that `npm run desktop:dev` could launch Electron against an already-running Vite server on `127.0.0.1:5173` from another worktree before this worktree's Vite process failed on the occupied port.
- Changed `scripts/desktop-dev.mjs` to choose the first available local renderer port from `5173`, pass that exact port to Vite with `--strictPort`, and pass the matching `AKSEL_ARCADE_RENDERER_URL` to Electron.
- Updated README/PRD wording and `tests/unit/desktopDevScript.test.ts` so future agents do not assume Desktop dev is hardcoded to port `5173`.
- Smoke-started `npm run desktop:dev` while stale 5173/5174 servers were still running; it selected `http://127.0.0.1:5175/aksel-arcade/`, rendered this worktree, showed `--ax-bg-default` preview backgrounds, and filtered prop-value autocomplete after typing.

Verification:

- npm test -- --run tests/unit/desktopDevScript.test.ts
- npm run typecheck
- npm run lint (passes with existing hook dependency warnings in `InspectionPopover` and `PreviewPane`)
- CI=1 npm run test

### 2026-06-09 - Issue #183: Multi-page canonical project source and workspace preference

Implemented:

- Selected #183 as the highest-priority `ready-for-agent` feature from #183-#191 because it is the foundational multi-page slice that later ready issues depend on.
- Read issue #181, issue #183, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Replaced the flat project model with a canonical `project.source` shape containing `globalConfig`, ordered `pages`, `startPageId`, `nextPageNumber`, and top-level `activePageId`, while keeping the flag-off UX behaviorally single-page.
- Added `src/services/projectSource.ts` to own canonical source creation, active/start page access, page lifecycle operations, selection normalization, and frozen monotonic page ids.
- Bumped storage schema to `2.0.0` and added lossless migration from legacy single-page projects to `page01` / `Page 1` with empty global config.
- Added the experimental working-copy-only `multiPageEnabled` preference, defaulted older saved preferences safely, and ensured autosave persists it without leaking it into shared/exported artifacts.
- Kept current share/export/import behavior on the start-page source so portable artifacts remain single-page compatible while the stored working copy now uses the canonical source model.
- Updated the editor, preview, and Desktop Agent bridge compatibility paths to read/write the active page source while preserving the current JSX/Hooks authoring surface.
- Fixed a preview re-render loop by memoizing `updatePreviewState`, and used `about:blank` as the iframe source in test mode to keep preview tests deterministic.
- Added canonical-model coverage in `tests/unit/services/projectSource.test.ts` and updated affected storage/share/integration tests for the new model and current Settings/Agent UI labels.

Verification:

- npm run typecheck
- CI=1 npm run test -- --run

- Browser check at `http://127.0.0.1:43183/aksel-arcade/` on an isolated local Vite port for app render, preview render, Settings menu visibility of `Enable experimental multi-page authoring`, successful toggle to `Disable experimental multi-page authoring`, and no console warnings/errors

Next note:

- Later multi-page issues should build on `src/services/projectSource.ts` and the canonical `project.source` model rather than reintroducing flat `jsxCode` / `hooksCode` storage. Keep `multiPageEnabled` local to working-copy preferences and keep portable share/export artifacts free of workspace-only flags.

### 2026-06-09 - PR #192 review follow-up

Fixed one PR-scoped review issue before merge:

- `useAutoSave` had started depending on the whole `preferences` object, while `App` passed a fresh inline object on every render. Because the hook also updates `saveStatus`, that could retrigger the effect and schedule repeated autosaves even without project changes.
- Narrowed the autosave effect dependencies to the primitive preference fields (`theme`, `panelOrder`, `multiPageEnabled`) so rerenders from save-status updates do not reschedule saves.
- Added `tests/unit/useAutoSave.test.tsx` to prove inline preference-object identity churn does not cause repeated saves.

Verification:

- npm run typecheck
- CI=1 npm run test -- --run

### 2026-06-09 - PR #196 follow-up: Multi-page preview bug fixes after Issue #190

Implemented:

- Fixed page-scoped preview errors leaking into unrelated pages by keeping the underlying `compileError` / `runtimeError` attached to their originating `pageId`, while only rendering the visible preview overlay when the active view is that page or the error is bundle-wide (`pageId: null`).
- Kept the multi-page iframe mounted when `Global config` is selected and turned the no-preview message into an overlay instead of unmounting `LivePreview`, which preserves the running sandbox page and prevents the start-page reload/flicker on the way back to a non-start page.
- Added explicit editor-focus tracking through `EditorState`, `EditorPane`, and `CodeEditor`, then used it in `PreviewPane` to suppress visible compile errors while the author is actively typing. Pending compile errors are now surfaced only after focus leaves the editor instead of interrupting the writing flow.
- Expanded page error badges from a single page id to a distinct set of page ids so the page panel can keep the right row marked even when compile/runtime errors overlap.
- Added focused regressions in `tests/integration/multi-page-preview-sync.test.tsx` covering the kept-mounted Global config placeholder, hidden cross-page error overlays, and no visible compile error during the first edit step.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- npm test -- --run
- Browser check at `http://127.0.0.1:4179/aksel-arcade/` for:
  - enabling experimental multi-page authoring
  - adding Page 2 and keeping Page 2 active through the Global config placeholder round-trip
  - typing a lone `<` on Page 2 without showing a visible compile error while the editor stayed focused
  - clicking back to Page 1 and seeing the Page 2 error stay scoped to the Page 2 row instead of taking over Page 1
  - no browser console errors/warnings during the clean verification path

Next note:

- The preview now distinguishes between page-scoped and bundle-wide failures in the overlay layer, but diagnostics/agent surfaces still only expose the latest compile/runtime payloads. If later work needs a full per-page error history, build it on top of the new editor-focus suppression and `errorPageIds` wiring instead of reintroducing a single global page-error flag.

### 2026-06-09 - PR #196 review follow-up

Fixed one PR-scoped review issue before merge:

- Replaced `Array.prototype.at(-1)` in `tests/integration/multi-page-preview-sync.test.tsx` with ES2020-compatible indexing so the new regression test still works under the repo's current TypeScript/lib target and `npm run build` no longer fails during `tsc -b`.

Verification:

- npm run build
- npm run typecheck
- npm test -- --run

### 2026-06-09 - Issue #191: Multi-page stale page reference diagnostics

Implemented:

- Selected #191 as the highest-priority `ready-for-agent` feature from #185-#191 because it closes the safety loop around multi-page authoring after #189 page lifecycle actions and #190 active-page sync, and it is more blocking to real authoring than export, autocomplete, or Agent-bridge follow-ups.
- Read issue #181, issue #191, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Added a pure page-reference analyzer in `src/services/pageReferences.ts` that finds `goToPage(...)` and page-id `href`/`to` references with source positions, classifies them as valid vs stale, and computes delete-impact counts across surviving pages plus `Global config`.
- Wired `src/components/Editor/CodeEditor.tsx` to surface stale references as warning diagnostics during multi-page authoring, using the analyzer so the current page or `Global config` shows clear missing-page warnings while typing.
- Updated `src/components/Editor/EditorPane.tsx`, `src/components/Editor/PagePanel.tsx`, and `src/components/Editor/PagePanel.css` so the page panel now shows a broken-navigation indicator, keeps preview errors higher-priority than stale references, and shows delete-confirmation impact counts before a page is removed.
- Added regression coverage in `tests/unit/services/pageReferences.test.ts`, `tests/integration/multi-page-page-panel.test.tsx`, and `tests/integration/multi-page-preview-sync.test.tsx` for source-position detection, delete-impact counting, live broken-navigation indicator updates from page and `Global config` edits, and the error-over-stale priority rule.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test
- npm run build
- Browser check at `http://127.0.0.1:4176/aksel-arcade/` for enabling experimental multi-page authoring, deleting a referenced Page 2 and seeing the stale-reference impact count in the confirmation dialog, seeing Page 1 switch to the broken-navigation state after delete, and reloading into a cleaned-up Page 1 state that rendered `Recovered` in the preview without runtime console errors.

Next note:

- If later work wants a dedicated `Global config` broken-navigation badge, `analyzeProjectPageReferences()` already exposes `globalConfigStaleReferences`; the current UI intentionally fans those shared stale refs out to all page rows instead, because the broken navigation is reachable from every page that renders shared config code.

### 2026-06-09 - PR #197 review follow-up

Fixed one PR-scoped review issue before merge:

- The page panel only rendered its status container for rows with errors or lifecycle labels, so an inactive, non-start page with stale references never showed the broken-navigation badge even though `hasBrokenNavigation` was already computed.
- Expanded the `PagePanel` render guard to include `hasBrokenNavigation`, then added an integration regression proving the badge still appears when the affected page is neither active nor start page.

Verification:

- npm run typecheck
- npm run lint (existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test

### 2026-06-09 - Issue #185: Multi-page experimental single-page sharing and export

Implemented:

- Selected #185 as the highest-priority `ready-for-agent` feature from #185-#187 because it closes the experimental multi-page safety gap: sharing/export were already start-page-only under the hood, but without clear warnings they could silently drop extra pages and `Global config`, which is a more urgent product risk than autocomplete polish (#186) or the Agent bridge extension (#187).
- Read issue #181, issue #185, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Added a portable-artifact warning helper in `src/services/storage.ts` so Web share URLs and `.akselarcade` exports reuse one warning message whenever a multi-page project would be reduced to the Start page.
- Updated `src/components/Header/AppHeader.tsx` so the share popover shows that warning for experimental multi-page projects, the Export button advertises dialog behavior in that state, and exporting now requires a confirmation dialog before proceeding with a Start-page-only package.
- Kept the portable artifact payloads themselves backward-compatible: Web share serialization still carries only the Start page source, and `.akselarcade` packages still export/import as clean single-page projects.
- Added regression coverage in `tests/integration/share-popover.test.tsx`, `tests/integration/share-decode.test.tsx`, and `tests/unit/services/storage.test.ts` for share warning UI, export confirmation, start-page-only multi-page share payloads, and multi-page package import collapsing back to a valid single-page project.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4317/aksel-arcade/` with a multi-page working copy injected through `sessionStorage`: the page panel rendered, the share popover showed the lossy-warning copy, Export opened the confirmation dialog, and there were no runtime console errors during the checked flow.

Next note:

- #186 can build on the existing `validPageIds` plumbing in `EditorPane` / `CodeEditor`, while #187 can stay focused on protocol v3 and targeted page/global-config edits; neither needs to revisit the portable artifact contract unless the experiment stops being Start-page-only.

### 2026-06-09 - PR #198 review follow-up

Fixed one PR-scoped issue before merge:

- The new multi-page share/export warning was incorrectly gated on the experimental multi-page preference, even though a stored project can still contain multiple pages and `Global config` after that UI toggle is turned off.
- Removed the preference gate in `src/components/Header/AppHeader.tsx` so lossy Web share/export warnings depend on the actual project shape, then extended `tests/integration/share-popover.test.tsx` with a regression covering export from a hidden multi-page project while the experiment is disabled.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4317/aksel-arcade/` with a stored multi-page project and `multiPageEnabled: false`: the share popover still showed the lossy-warning copy, Export still opened the confirmation dialog, and the console only showed the existing form-field `id`/`name` issue.

### 2026-06-09 - Issue #186: Multi-page page-id autocomplete for page navigation

Implemented:

- Selected #186 as the highest-priority `ready-for-agent` feature from #186-#187 because it closes the remaining human-facing multi-page authoring gap after the page/runtime/share work already landed, while the pages-aware Agent bridge in #187 can build afterward on the now-complete authoring surface.
- Read issue #181, issue #186, `docs/prd/0030-multi-page-support.md`, and the existing `progress.txt` notes before changing code.
- Extended `src/services/akselAutocomplete.ts` with page-navigation completion contexts for `goToPage('…')`, `href="…"`, and `to={"…"}` so completions show the page display name as the label, the stable `pageNN` id as the detail, and insert the id while still allowing free typing.
- Kept page-id matching ergonomic by filtering page navigation suggestions on either the display name or the typed page-id prefix, then returning the options in project page order instead of introducing a second page-sorting rule.
- Updated `src/components/Editor/CodeEditor.tsx` so CodeMirror now gets the current page targets, uses them in the autocomplete override, and auto-triggers completions inside page-navigation string contexts in addition to the existing catalog-backed prop-value contexts.
- Updated `src/components/Editor/EditorPane.tsx` to thread `{ id, name }` page metadata into `CodeEditor` while keeping stale-reference linting on the lighter `validPageIds` list.
- Added focused regression coverage in `tests/unit/akselAutocomplete.test.ts` for `goToPage(...)`, `href`/`to`, name-vs-id filtering, inserted ids, and out-of-scope cursor positions.

Verification:

- npm install
- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- CI=1 npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4191/aksel-arcade/` for enabling experimental multi-page authoring, adding Page 2, seeing the live CodeMirror autocomplete tooltip inside `goToPage('…')` with `Page 1` / `Page 2` labels and `page01` / `page02` details, confirming the same tooltip inside Global config `href="…"` authoring, and reloading into the default project with no console errors or warnings.

Next note:

- #187 can stay focused on protocol v3, page lifecycle bridge commands, and targeted page/global-config edits; the page-navigation suggestion source now lives in `EditorPane` → `CodeEditor` → `akselAutocomplete` and does not need to be duplicated in the Agent bridge layer.
- For browser verification, prefer a fresh Vite port if a reused port appears to serve stale source from another worktree; `4191` was the clean port used for this branch’s verification.

### 2026-06-09 - Issue #187: Multi-page pages-aware Agent bridge

Implemented:

- Selected #187 as the single requested feature and kept the work scoped to the Desktop Arcade Agent bridge follow-up from #181/#186.
- Read issue #181, issue #187, `docs/prd/0030-multi-page-support.md`, `docs/adr/0014-pages-aware-agent-bridge.md`, and the existing `progress.txt` notes before changing code.
- Bumped the Agent bridge contract to protocol v3 in `src/services/agentBridge.ts`, expanded `getProject` to return page-aware project state (`pageMode`, `pages`, `globalConfig`, `startPageId`, `activePageId`), deep-cloned nested read state, and made the session command list/instructions dynamic between single-page and multi-page authoring.
- Added explicit lifecycle commands (`createPage`, `renamePage`, `deletePage`, `setStartPage`, `selectActivePage`) and wired their routing/gating so multi-page-disabled sessions surface `unsupported-command` guidance instead of silently accepting unsupported mutations.
- Added reusable source targeting in `src/types/project.ts`, `src/services/projectSource.ts`, and `src/hooks/useProject.tsx` so Agent edits can safely target either `{ type: 'page', pageId }` or `{ type: 'global-config' }` without mutating the human editor selection first.
- Updated `src/hooks/useAgentSession.ts` and `src/components/Header/AgentSessionMenu.tsx` so the live Desktop Agent session reads current multi-page state, validates `applyAgentChange.target`, applies lifecycle mutations through the canonical project helpers, and preserves the single-page compatibility path when experimental multi-page authoring is off.
- Fixed `src/services/desktopAgentTransportProtocol.ts` so Desktop transport forwards `params` for every non-read command, not just `applyAgentChange`; without that fix, lifecycle commands reached the bridge with `undefined` params.
- Refreshed regression coverage in `tests/unit/services/agentBridge.test.ts`, `tests/unit/services/desktopAgentTransportProtocol.test.ts`, `tests/integration/project-controls-layout.test.tsx`, and `tests/integration/share-popover.test.tsx` for protocol v3, dynamic command exposure, single-page gating, lifecycle routing, and targeted page/global-config edits.

Verification:

- npm install
- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run build
- npm run test -- --run
- Browser check at `http://127.0.0.1:5176/aksel-arcade/` with a Desktop preload shim: verified single-page Agent sessions expose only `applyAgentChange` plus read commands and reject `createPage` / other-page / global-config edits with the expected guidance; then enabled experimental multi-page authoring and verified `createPage`, `renamePage`, `setStartPage`, `selectActivePage`, `deletePage`, page-targeted edits, and global-config-targeted edits all updated the human-visible page panel + preview with no console errors or warnings.

Next note:

- The single-page bridge path intentionally exposes the current active page plus empty `globalConfig`, matching the human-visible editor/preview surface and the earlier #183 compatibility behavior. If product wants strict "always expose page01/first page" semantics instead, clarify that before changing the bridge again.
- Local Vite ports may already be occupied by other worktrees; `5173` in this environment was serving stale source from another session, and `5176` was the clean port used for the verified browser flow on this branch.

### 2026-06-09 - PR #200 review follow-up

Fixed one PR-scoped issue before merge:

- Guarded `deletePage` in `src/hooks/useAgentSession.ts` so an Agent request that targets the last remaining page now returns a structured `invalid-request` bridge failure instead of throwing through the Desktop transport layer and breaking the JSON-RPC contract.
- Added a regression in `tests/integration/project-controls-layout.test.tsx` that exercises the multi-page-enabled single-page state and proves the last-page delete attempt fails cleanly before any destructive mutation runs.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run build
- npm run test -- --run
- Browser check at `http://127.0.0.1:5176/aksel-arcade/` with the Desktop preload shim: after deleting `page02`, `deletePage({ pageId: 'page01' })` returned the expected `invalid-request` response, the project stayed on `page01`, and there were no console errors or warnings.

### 2026-06-10 - Issue #206: Establish shared Aksel insertion catalog tracer

Implemented:

- Selected #206 as the highest-priority `ready-for-agent` feature from #206-#217 because it is the shared **Aksel insertion catalog** tracer that every later authoring issue in this batch is explicitly blocked on.
- Read issue #205, `CONTEXT.md`, and the existing `progress.txt` notes before changing code so the slice stayed aligned with the PRD language and the current authoring roadmap.
- Added current catalog entries for the tracer components `BodyShort`, `Heading`, and `Tag` in `src/data/akselCatalog.ts`, including docs links, keywords, curated visible snippets, and the current Tag variant/data-color guidance instead of the stale `variant="info"` path.
- Extended `getCatalogPaletteComponents()` / `src/data/akselComponents.ts` so active Add-menu data can carry catalog keywords and search against them, which keeps the catalog query layer usable for both discovery and insertion without leaking storage details into the modal UI.
- Updated `src/services/akselAutocomplete.ts` so top-level component completion now prefers shared catalog entries whenever a component exists in the catalog, while generated docs data remains the fallback source for uncataloged top-level choices and still powers prop / prop-value intelligence.
- Added focused regression coverage in `tests/unit/akselCatalog.test.ts` and `tests/unit/akselAutocomplete.test.ts` proving the tracer components now come from the shared catalog for Add-menu data, keyword search, autocomplete detail text, and autocomplete insertion payloads.

Verification:

- npm install
- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4181/aksel-arcade/`: confirmed the Add menu shows `BodyShort`, `Heading`, and `Tag`; confirmed Add inserts the new Tag catalog snippet (`<Tag variant="moderate" data-color="info">In progress</Tag>`) and the preview renders it; confirmed the browser-loaded autocomplete service returns the catalog apply strings for `BodyShort` and `Heading`, then loaded those exact snippets into the editor and saw both render in preview with no console errors.

Next note:

- #207 can now focus narrowly on the author-facing `Combobox` / `UNSAFE_Combobox` alias cleanup, because the top-level autocomplete path now has an explicit catalog-first merge point instead of only the docs-derived list.
- When browser-verifying this worktree, `4173`-`4180` were already serving other sessions; `4181` was the clean Vite port for this branch.

### 2026-06-11 - Issue #207: Normalize Combobox authoring alias behavior

Implemented:

- Selected #207 as the highest-priority `ready-for-agent` feature from #207-#217 because the fresh #206 handoff explicitly identified it as the next narrow, unblocked fix now that top-level autocomplete has a catalog-first merge point.
- Read issue #205, `CONTEXT.md`, and the existing `progress.txt` notes before changing code so the slice stayed aligned with the shared Aksel authoring PRD and recent catalog work.
- Removed the autocomplete-only `UNSAFE_Combobox` alias generation in `src/services/akselAutocomplete.ts`, so top-level completion options now stay on the public `Combobox` name instead of synthesizing an internal runtime-only option.
- Removed the autocomplete apply rewrite that previously turned a selected `Combobox` completion into `UNSAFE_Combobox`, keeping the public name in inserted editor source while leaving the runtime aliasing untouched in `src/sandboxAksel.ts`.
- Tightened component prop lookup in `src/services/akselAutocomplete.ts` so prop and prop-value completions only resolve against the actual author-facing component name; `Combobox` keeps working, while `UNSAFE_Combobox` is no longer treated as a completion target.
- Added focused regression coverage in `tests/unit/akselAutocomplete.test.ts` proving top-level autocomplete excludes `UNSAFE_Combobox`, selecting `Combobox` applies `Combobox`, `Combobox` still gets its prop suggestions, and `UNSAFE_Combobox` no longer gets prop or prop-value completions.

Verification:

- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run build
- npm run typecheck
- npm run test -- --run
- Browser check at `http://127.0.0.1:4182/aksel-arcade/`: confirmed the Add menu still shows `Combobox`; confirmed Add inserts `<Combobox label="Select option" options={[]}>` and the preview renders a real combobox without exposing `UNSAFE_Combobox`; confirmed typing `<Comb` in the editor shows only the `Combobox` completion option and accepting it inserts `<Combobox` rather than the unsafe runtime alias.

Next note:

- Keep `Combobox -> UNSAFE_Combobox` mapping inside `src/sandboxAksel.ts` only; this issue intentionally removed the alias from authoring surfaces, not the runtime compatibility layer.
- #208 can centralize replacement/deprecation filtering next without needing to touch the Combobox alias path again; the browser Add menu still exposes deprecated `Alert` and replaced `Modal`, which matches the open follow-up.

### 2026-06-11 - PR #220 review follow-up for Issue #207

Fixed before merge:

- A focused PR review caught one real regression in the first #207 patch: stored projects that still contain `<UNSAFE_Combobox ...>` lost Combobox prop and prop-value autocomplete even though the runtime alias still lets those pages render.
- Restored alias-aware autocomplete lookup in `src/services/akselAutocomplete.ts` only for metadata resolution by normalizing `UNSAFE_Combobox` back to `Combobox` inside `getComponentProps()`. This keeps the unsafe name hidden from top-level authoring suggestions and insertions while preserving editor help for legacy stored source.
- Updated `tests/unit/akselAutocomplete.test.ts` so the regression coverage now proves both sides of the contract together: top-level autocomplete still exposes only `Combobox`, and legacy `<UNSAFE_Combobox ...>` source still gets prop/value completions through the compatibility lookup path.

Verification:

- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run build
- npm run typecheck
- npm run test -- --run
- Browser check at `http://127.0.0.1:4183/aksel-arcade/`: confirmed typing `<Comb` in the editor still shows only the public `Combobox` completion with no unsafe alias surfaced in the UI; the legacy `UNSAFE_Combobox` compatibility path is now covered by the focused unit regression because the browser editor only surfaces the public-name flow for new authoring.

Next note:

- Do not remove the metadata-side `UNSAFE_Combobox -> Combobox` normalization until stored Arcade project source is migrated off the unsafe name. Runtime compatibility alone is not enough for legacy editor autocomplete.

### 2026-06-11 - Issue #208: Remove deprecated and replaced authoring entries

Implemented:

- Selected #208 as the highest-priority `ready-for-agent` feature from #208-#217 because the fresh #207 handoff identified deprecated `Alert` / replaced `Modal` exposure as the next unblocked authoring problem still steering new work toward the wrong APIs, and fixing that shared policy also clears the way for the later Dialog/form and audit follow-ups.
- Read issue #205, `CONTEXT.md`, and the existing `progress.txt` notes before changing code so the replacement/deprecation filter stayed aligned with the shared Aksel authoring PRD and recent catalog-first handoffs.
- Added `src/data/akselAuthoringPolicy.ts` as the shared new-authoring filter for deprecated/replaced roots, and wired it through catalog discovery plus autocomplete so `Alert`/`Modal` no longer appear in Add-menu data, top-level autocomplete, contextual subcomponent autocomplete, prop completions, or prop-value completions.
- Marked the catalog `Alert` entry as legacy and filtered manual palette/snippet metadata in `src/data/akselComponents.ts` and `src/services/componentLibrary.ts`, so the hidden-policy behavior now stays consistent across both the active Add dialog and the older snippet search helpers.
- Kept runtime compatibility intact by leaving the raw catalog/manual metadata and sandbox runtime support in place; old/manual `Alert` and `Modal` source can still render, but new authoring aids stop suggesting them.
- Replaced deprecated built-in `Alert` usage in `src/utils/projectDefaults.ts` with current `InlineMessage` examples, including the intro template and the hooks demo confirmation/details blocks, so fresh projects no longer teach deprecated feedback APIs.
- Migrated the active Add-component chrome in `src/components/ComponentPalette/ComponentPalette.tsx` from `Modal` to `Dialog`, keeping the same search/tabs/card workflow while removing the local authoring-UX use of the replaced overlay component.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, `tests/unit/projectDefaults.test.ts`, and `tests/integration/component-palette.test.tsx` for the shared hidden-entry policy, Add/autocomplete filtering, built-in template updates, and the Dialog-backed palette portal DOM.

Verification:

- npm install
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run typecheck
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4183/aksel-arcade/`: confirmed the default project now renders `InlineMessage` instead of `Alert`, the Add dialog opens as a dialog and shows no `Alert` or `Modal` entry, searching `modal` reports no matching component, and there were no browser console errors during the authoring flow.

Next note:

- #215 can now add Dialog and the current feedback/form-shell replacements without reopening this filtering work; keep using `src/data/akselAuthoringPolicy.ts` if future deprecations/replacements need to disappear from new authoring surfaces.

### 2026-06-11 - Issue #211: Multi-part insertion into JSX and Hooks

Implemented:

- Selected #211 as the highest-priority `ready-for-agent` feature from #209-#217 because #206-#208 are already complete in this file, and #211 is the shared insertion foundation that unblocks the later stateful component slices more directly than another one-off catalog entry.
- Re-read issue #205, `CONTEXT.md`, and the recent `progress.txt` notes before changing code so the fix stayed aligned with the Aksel authoring PRD and the catalog/autocomplete work already landed.
- Added `src/services/componentInsertion.ts` plus a shared `ComponentInsertion` type so insertion can update JSX only or JSX plus Hooks together, with separate Add-menu vs autocomplete placement handling.
- Added collision-safe suffix replacement for support helpers such as `usePaginationState`, scanning both JSX and Hooks source together so repeated insertions become `usePaginationState2`, `usePaginationState3`, and so on instead of clobbering earlier support code.
- Extended the catalog/component metadata path to preserve insertion metadata end-to-end instead of flattening everything to a single snippet string, so the active Add dialog can now drive shared multi-part insertion.
- Updated `src/components/Editor/CodeEditor.tsx` and `src/services/akselAutocomplete.ts` so catalog-backed completions with support code go through a stable callback bridge into React state without recreating CodeMirror extensions or reintroducing the known autocomplete focus bug.
- Added `Pagination` as the stateful tracer entry. Its JSX insertion now uses a fragment-wrapped IIFE, and its Hooks insertion exports `usePaginationState{{paginationSuffix}}(...)`, matching the existing import-free Arcade custom-hook pattern instead of using invalid top-level hook calls.
- Added focused regression coverage in `tests/unit/services/componentInsertion.test.ts`, `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, and `tests/integration/component-palette.test.tsx` for JSX-only insertion, JSX-plus-Hooks insertion, repeated tracer insertion, autocomplete apply shape, and Add-menu metadata wiring.

Verification:

- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run typecheck
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4311/aksel-arcade/`: after clearing the persisted working copy, confirmed Add -> `Pagination` inserts the fragment-wrapped Pagination JSX plus exported hook support, the preview renders pagination controls and advances from page 1 to page 2, typing `<Pagi` and accepting `Pagination` inserts the same multi-part structure with collision-safe `usePaginationState2`, and a clean reload of the saved state shows no console errors.

Next note:

- Future stateful catalog entries should reuse `src/services/componentInsertion.ts` and the catalog `snippet.code` + `snippet.hooksCode` contract instead of reintroducing one-off editor mutations. The first browser check accidentally hit another worktree on port `4191`; the verified current worktree server for this branch was `4311`, so confirm the serving cwd before trusting browser results if multiple Vite sessions are running.

### 2026-06-11 - PR #222 review follow-up for Issue #211

Fixed before merge:

- A focused PR review found one real regression in the first #211 patch: `Pagination` autocomplete still appeared in the Hooks editor, but accepting that multi-part completion would target JSX ranges and corrupt the wrong buffer.
- Limited `EditorPane` to pass the multi-part catalog insertion callback only on the JSX tab, so the Hooks editor no longer advertises completions it cannot apply safely.
- Filtered catalog entries with `snippet.hooksCode` out of autocomplete result sets whenever the editor cannot apply the shared insertion callback, keeping normal JSX component discovery intact while preventing unsafe Hooks-tab multi-part insertions.
- Added regression coverage in `tests/unit/akselAutocomplete.test.ts` proving `Pagination` is still available when the JSX insertion callback is present and disappears when the editor cannot apply multi-part insertions safely.

Verification:

- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run typecheck
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4311/aksel-arcade/`: confirmed typing `<Pagi` in the Hooks tab shows no `Pagination` completion, typing `<Pagi` in JSX still offers `Pagination`, accepting it still renders the preview pagination controls, the hook support code lands in the Hooks tab, and the clean verified session had no browser console errors.

### 2026-06-11 - Post-#211 insertion contract update from user feedback

Implemented:

- Fresh user feedback on the new `Pagination` / `Tabs` authoring flow clarified a stricter system contract for Add-menu and autocomplete insertions: JSX must stay composable inside the surrounding layout tree, and helper logic must live in Hooks instead of being injected into the JSX buffer as wrapper fragments or IIFEs.
- Updated the shared `Pagination` catalog insertion so the JSX payload is now a plain placeable `<Pagination ... />` tag while the reusable state helper stays in Hooks as `usePaginationState{{paginationSuffix}}(...)`; the JSX now consumes that helper through prop spread instead of a fragment-wrapped local IIFE.
- Updated `Tabs` starter snippets to use uncontrolled `defaultValue="tab1"` instead of a controlled `value="tab1"` without `onChange`, so Add-menu insertion yields a clickable preview without requiring extra support code.
- Added a catalog-level regression guard proving every Hooks-backed insertion keeps a plain JSX root and does not inject wrapper IIFEs/fragments, so future stateful snippet work fails fast in tests if it violates the composable-layout contract again.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run build
- npm run test -- --run

Next note:

- This contract applies directly to #209, #214, #215, #216, and #217 because those remaining slices still touch author-facing insertions for interactive/stateful components; do not introduce wrapper JSX around the inserted node when fixing those issues.
- #210 should preserve the same contract for autocomplete-triggered compound insertions: if a completion needs helper code, the JSX apply string still has to remain composable at the insertion point.
- #213 should explicitly audit for this rule when comparing docs/runtime/catalog snippets, because wrapper-based JSX insertions are now a known system-level regression class.
- #212 looks mostly static (`content`, `typography`, `link`) and probably does not need special handling beyond the normal composable-snippet expectation, unless one of its target components later grows Hooks-backed support code.

### 2026-06-11 - Issue #210: Add contextual autocomplete for compound children

Implemented:

- Selected #210 as the highest-priority `ready-for-agent` feature from #209-#217 because it is the only remaining unblocked autocomplete foundation and it directly gates the later menu/overlay, form/input, and workflow/navigation insertion fixes in #214, #216, and #217.
- Re-read issue #205, `CONTEXT.md`, and the latest `progress.txt` notes before changing code so the autocomplete behavior stayed aligned with the shared **Aksel insertion catalog** contract and the newer composable multi-part insertion rules.
- Added narrow contextual autocomplete metadata in `src/data/akselCatalog.ts` for the representative compound/parent-bound structures needed by this slice: Accordion, ActionMenu, Dropdown, Tabs, RadioGroup, and CheckboxGroup. The metadata now distinguishes contextual-only children from top-level entries that can also be suggested contextually, which keeps `Page.Block` available as a curated top-level choice while hiding compound children such as `Accordion.Item` or `Dropdown.Menu.List` from global discovery.
- Reworked `src/services/akselAutocomplete.ts` so top-level tag suggestions are limited to real top-level JSX authoring choices: catalog-backed entries plus non-dotted documented component tags. Raw docs-only subcomponents and lowercase helper exports no longer leak into top-level autocomplete.
- Added a lightweight JSX ancestor scanner in `src/services/akselAutocomplete.ts` that computes the open component stack at the current tag position, then uses the new catalog metadata to offer context-specific children only inside the matching parent ancestry.
- Made contextual child matching work on both full and relative names, so typing `<Accordion>` then `<H` inside `Accordion.Item` now surfaces `Accordion.Header`, while constrained parents suppress generic child suggestions instead of mixing in unrelated primitives or icons.
- Removed the noisy generic docs fallback detail text from autocomplete options. Catalog-backed entries still show meaningful descriptions, but raw docs-backed entries no longer render placeholder text like `Documented Aksel component.`.
- Added focused regression coverage in `tests/unit/akselAutocomplete.test.ts` for top-level subcomponent suppression, `Page.Block` staying top-level, contextual child suggestions inside representative parents, parent-bound Radio/Checkbox suggestions, constrained-context suppression, relative child-name matching, and the detail-text cleanup. Updated `tests/e2e/autocomplete.spec.ts` so the browser coverage now checks the same user-facing contract.

Verification:

- npm run typecheck
- npm test -- --run
- npm run build
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- Browser check at `http://127.0.0.1:4321/aksel-arcade/`: confirmed top-level `<Page.` still offers `Page.Block`; confirmed top-level `<Accordion.` shows no autocomplete list; confirmed typing `<` inside `<Accordion>` offers only `Accordion.Item`; confirmed the constrained contextual list does not leak unrelated suggestions such as `Button`; confirmed no browser console errors or warnings during the verified flow

Next note:

- Extend `AKSEL_CONTEXTUAL_AUTOCOMPLETE_RULES` in `src/data/akselCatalog.ts` for later compound slices instead of adding one-off autocomplete branches. The `exclusive` flag now controls whether a parent should suppress generic child suggestions completely, and the child `discovery` flag preserves intentional top-level exceptions such as Radio/Checkbox-style catalog choices.
- Keep the top-level autocomplete filter strict: catalog exceptions like `Page.Block` stay allowed, but docs-only dotted subcomponents and lowercase helper exports should remain hidden from global discovery unless a future catalog decision makes them an intentional top-level insertion choice.

### 2026-06-11 - Issue #216: Fix form, picker, and input insertions

Implemented:

- Selected #216 as the highest-priority `ready-for-agent` feature from #209-#217 because #210 and #211 had already landed, and this slice fixes the broadest set of high-frequency authoring controls still missing catalog-backed, behavior-complete defaults - including the explicitly broken Radio insertion path.
- Re-read issue #205, `CONTEXT.md`, and the latest `progress.txt` notes before changing code so the implementation stayed aligned with the shared **Aksel insertion catalog** contract, the composable JSX rule from post-#211 feedback, and the existing contextual-child behavior from #210.
- Expanded `src/data/akselCatalog.ts` with catalog-backed entries for Checkbox, Radio, DatePicker, MonthPicker, Search, Select, Switch, Textarea, and ToggleGroup. The new snippets now carry current docs links, searchable keywords, visible labels/content, and explicit form-field names where the example renders a real input.
- Replaced the broken top-level Radio snippet with a valid `RadioGroup` example, so Add/autocomplete no longer emit the old `Radio.Group` runtime error path while still keeping child `Radio` suggestions scoped to `RadioGroup` from the earlier contextual-autocomplete work.
- Added multi-part insertion support for the remaining stateful controls in this slice: DatePicker and MonthPicker now insert Hook-tab helper components that own the `useDatepicker` / `useMonthpicker` prop-pair wiring, while ToggleGroup follows the Pagination-style prop-spread helper pattern with `useState`-backed `value` / `onChange`.
- Kept the simpler form controls accessible and immediately usable out of the box: Search now inserts inside a `role="search"` form with a stable field name, Select includes a visible placeholder plus options, Switch/Checkbox include descriptive labels, and Textarea gets helper text plus a usable initial size.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, `tests/integration/component-palette.test.tsx`, and `tests/unit/services/componentInsertion.test.ts` for grouped-control snippets, stateful picker/toggle insertions, collision-safe Hook-tab helpers, and Add/autocomplete parity for the #216 component set.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4321/aksel-arcade/`: confirmed RadioGroup selection, DatePicker date changes, MonthPicker month changes, Pagination page changes, Search/Select/Textarea input, ToggleGroup state changes, and no browser console errors or warnings during the clean verified flow

Next note:

- #214 and #217 can reuse the same two multi-part patterns introduced here: prop-spread helpers when one state pair is enough (`Pagination`, `ToggleGroup`), and Hook-tab helper components when the Aksel API returns paired prop bundles (`DatePicker`, `MonthPicker`). Keep the JSX insertion root composable and keep helper logic in Hooks instead of reintroducing inline wrapper fragments or IIFEs.
- Keep adding explicit field names to future form-control snippets when the example renders real inputs. The authoring contract now covers visible defaults and runtime-safe state, but browser autofill/audit hygiene is another easy regression surface for these slices.

### 2026-06-11 - PR #226 review follow-up for issue #216

Implemented:

- PR review found that the contextual `Radio` and `Checkbox` completions added by #210 were still borrowing the wrong insertion payloads after the #216 catalog work: `Radio` inside `RadioGroup` inserted the full top-level `RadioGroup` snippet again, and `Checkbox` inside `CheckboxGroup` reused the standalone checkbox snippet without the grouped-child `value` prop.
- Extended the contextual autocomplete metadata in `src/data/akselCatalog.ts` so parent-bound child suggestions can override their insertion payload without changing the shared top-level catalog entry.
- Updated `src/services/akselAutocomplete.ts` so contextual child completions honor those override insertions both when filtering available options and when applying the selected completion, while leaving the normal top-level catalog apply path unchanged.
- Added regression coverage in `tests/unit/akselAutocomplete.test.ts` that asserts the actual applied completion text for `RadioGroup -> Radio` and `CheckboxGroup -> Checkbox`, not just the presence of the suggestion label.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4321/aksel-arcade/`: confirmed contextual `Radio` completion inside `RadioGroup` inserts a single `<Radio value="option1">...</Radio>` child and renders cleanly in preview; confirmed `CheckboxGroup` still offers only the contextual `Checkbox` child completion, with the inserted text locked by the new regression test.

### 2026-06-11 - Issue #215: Fix feedback, dialog, and form-shell insertions

Implemented:

- Selected #215 as the highest-priority `ready-for-agent` feature from #209-#217 because the earlier ready-for-agent groundwork in this range was already present, and this slice restores the current feedback/dialog/form-shell authoring path after deprecated `Alert` / `Modal` insertions were removed.
- Re-read issue #205, `CONTEXT.md`, and the latest `progress.txt` notes before changing code so the catalog work stayed aligned with the shared **Aksel insertion catalog** contract and the post-#211 composable JSX rule.
- Expanded `src/data/akselCatalog.ts` with catalog-backed entries for `InlineMessage`, `GlobalAlert`, `LocalAlert`, `Dialog`, `Fieldset`, `CheckboxGroup`, `ErrorMessage`, `ErrorSummary`, `FileUpload`, and `FormSummary`, while leaving the already-present `FormProgress` entry untouched.
- Implemented `Dialog` as a Hooks-backed multi-part insertion using a collision-safe `ReviewDialog{{dialogSuffix}}` helper component so the inserted JSX stays a composable `<ReviewDialog />` root while the `useState` dialog wiring lives in Hooks.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, `tests/unit/services/componentInsertion.test.ts`, and `tests/integration/component-palette.test.tsx` for catalog presence, Add/autocomplete parity, collision-safe Dialog helper insertion, and the Add-menu metadata path.
- Checked the new snippets against the pinned 8.11.0 runtime exports instead of copying newer docs examples blindly; `FormProgress` was already implemented and stayed out of scope for this single-feature pass.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4327/aksel-arcade/`: after reloading the saved working copy, confirmed `InlineMessage`, `LocalAlert`, `FileUpload`, and the Hooks-backed `Dialog` render cleanly; confirmed `Review summary` opens the dialog; confirmed both `Go back` and `Confirm` close it; confirmed the verified session had no browser console errors or warnings.
- One unrelated full-suite run of `tests/integration/share-popover.test.tsx` flaked once before passing on immediate rerun; no code was changed for that existing non-#215 failure path.

Next note:

- #214 and #217 should reuse the same composable-root contract here: keep inserted JSX placeable inside surrounding layouts and keep helper logic in Hooks when a component needs state. `Dialog` now provides another example of the helper-component pattern alongside the picker/toggle helpers from #216.

### 2026-06-12 - PR #227 review follow-up for issue #215

Implemented:

- PR review found one real suffix-collision bug in the new `Dialog` insertion flow: `src/services/componentInsertion.ts` was treating the hyphenated popup-id fragment `popup{{dialogSuffix}}` as an identifier template, so existing unrelated `popup` identifiers in Hooks could force the very first dialog insertion to become `ReviewDialog2`.
- Tightened identifier-template extraction so collision suffixing only considers real identifier starts and ignores hyphenated string fragments like the `review-dialog-popup{{dialogSuffix}}` id.
- Added a focused regression in `tests/unit/services/componentInsertion.test.ts` that seeds Hooks with `const popup = true` and proves the first inserted dialog still uses unsuffixed `ReviewDialog`, `dialogOpen`, and `review-dialog-popup`.

Verification:

- npm run test -- --run tests/unit/services/componentInsertion.test.ts
- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4327/aksel-arcade/`: with Hooks set to `const popup = true`, confirmed Add -> `Dialog` inserts unsuffixed `<ReviewDialog />`, keeps the helper as `export const ReviewDialog = () => { ... }`, keeps `id="review-dialog-popup"`, opens in preview, and produces no browser console errors.

### 2026-06-12 - Issue #217: Fix compound workflow and navigation insertions

Implemented:

- Selected #217 as the highest-priority open `ready-for-agent` feature from #209-#217 after checking #205, `CONTEXT.md`, and the existing `progress.txt` notes; it was the most user-blocking remaining authoring slice because compound workflow/navigation examples still had broken or missing insertion paths, especially for `Tabs`, `Process`, and `Timeline`.
- Expanded `src/data/akselCatalog.ts` with catalog-backed top-level entries for `Accordion`, `ExpansionCard`, `Process`, `ReadMore`, `Stepper`, `Tabs`, and `Timeline`, and added matching contextual child rules for `Accordion.*`, `ExpansionCard.*`, `Process.Event`, `Stepper.Step`, `Tabs.*`, and `Timeline.*`.
- Switched `Tabs` from the earlier temporary uncontrolled example to a proper Hooks-backed multi-part insertion using `useTabsState{{tabsSuffix}}`, keeping the inserted JSX composable while moving state into Hooks in line with the post-#211 insertion contract.
- Kept `Process`, `Timeline`, `Accordion`, `ExpansionCard`, `ReadMore`, and `Stepper` as valid JSX-first insertions with behavior-complete default structures, including real `Process.Event` children and a dated `Timeline` example that renders rows, periods, and a pin against the pinned 8.11.0 runtime.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, `tests/integration/component-palette.test.tsx`, and the new `tests/integration/compound-insertions-runtime.test.tsx` for Add/autocomplete parity, contextual subcomponent suggestions, Tabs hook insertion, and runtime rendering/switching for the highest-risk snippets.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4182/aksel-arcade/`: confirmed Add -> `Tabs` inserts `<Tabs {...useTabsState()}>` plus a matching Hooks helper, confirmed preview tab clicks switch panels, confirmed Add -> `Process` renders the inserted event list, confirmed Add -> `Timeline` renders rows/periods/pin cleanly, and confirmed the verified session had no browser console errors.

Next note:

- The remaining open ready-for-agent issues in this range are narrower follow-ups (`#209`, `#212`, and `#213`). If a later slice needs more interactive workflow/navigation examples, reuse the #211/#215/#217 composable-root rule: keep authored JSX insertable inside surrounding layouts and move supporting state into Hooks instead of wrapper fragments or inline IIFEs.

### 2026-06-12 - PR #229 review follow-up for issue #217

Implemented:

- PR review found one real correctness bug in the new contextual `Tabs` child insertions: repeated `Tabs.Tab` / `Tabs.Panel` autocomplete insertions reused the same `value` key, which can break Aksel tab-panel pairing and generate duplicate ids within a single `Tabs` instance.
- Changed the contextual `Tabs.List`, `Tabs.Tab`, and `Tabs.Panel` insertion templates to route through the editor-apply path instead of raw string insertion when they need source-aware transformation.
- Added `Tabs`-specific autocomplete insertion resolution in `src/services/componentInsertion.ts` so contextual `Tabs` children now reuse an unpaired sibling value when possible and otherwise generate the next sequential `tabN` value within the current `Tabs` block.
- Added regression coverage in `tests/unit/services/componentInsertion.test.ts` for list/tab/panel pairing and next-value generation, and extended `tests/unit/akselAutocomplete.test.ts` so the contextual `Tabs` child completions stay callback-backed instead of falling back to unsafe direct string insertion.

Verification:

- npm run test -- --run tests/unit/akselAutocomplete.test.ts tests/unit/services/componentInsertion.test.ts
- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- npm run build
- Browser check at `http://127.0.0.1:4184/aksel-arcade/`: confirmed Add -> `Tabs` still renders and switches panels in preview, confirmed the browser-loaded autocomplete/component-insertion modules now resolve a contextual `Tabs.Panel` insertion to the next matching `timeline` value instead of duplicating `overview`, and confirmed the verified session had no browser console errors.

### 2026-06-12 - Issue #209 catalog-backed status/data-display insertions

Implemented:

- Picked #209 as the highest-priority still-open `ready-for-agent` issue in the requested `#209`-`#213` range after confirming `#210` and `#211` were already closed and `#212` / `#213` were narrower follow-ups.
- Moved the remaining issue-209 authoring targets onto the shared Aksel insertion catalog: separate `Chips Toggle` and `Chips Removable` multi-part insertions, plus visible default snippets for `Loader`, `ProgressBar`, `Skeleton`, and `Table`, while keeping the existing catalog-backed `Tag`.
- Removed the matching legacy palette/snippet duplicates from `src/data/akselComponents.ts` and `src/services/componentLibrary.ts` so the Add menu and snippet search stay sourced from the shared catalog instead of diverging manual copies.
- Updated top-level autocomplete matching so catalog entries with spaced labels still resolve from component-like prefixes (`<Chips`, `<ChipsT`, `<ChipsR`) and the generic docs fallback `Chips` suggestion no longer competes with the curated split variants.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, and `tests/integration/component-palette.test.tsx` for catalog visibility, Add-menu rendering, and callback-backed insertions for the new `Chips` variants.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation warning and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with existing Vite/browser-compatibility and chunk-size warnings only)
- Browser check at `http://127.0.0.1:5178/aksel-arcade/`: confirmed Add shows `Chips Toggle` and `Chips Removable` instead of a generic `Chips`, confirmed both entries insert JSX plus matching Hooks helpers, confirmed Add inserts the visible `Loader`, `ProgressBar`, `Skeleton`, and `Table` snippets from the catalog, confirmed the preview renders the expected loading/progress/table text alongside both chips examples, and confirmed the verified session had no browser console warnings or errors.

Next note:

- The remaining open issues from this original range are `#212` and `#213`. If a follow-up keeps migrating palette/search/autocomplete surfaces, treat the shared catalog as the source of truth and remove matching legacy palette/snippet entries in the same slice to avoid duplicate authoring choices.

### 2026-06-12 - PR #230 follow-up for issue #209 Chips visibility

Implemented:

- Followed up on review feedback that the first `Chips Toggle` / `Chips Removable` version hid the actual JSX behind helper component tags such as `<ChipsToggleExample />`, which made the editor harder to read and broke the intended insertion contract.
- Changed both shared-catalog `Chips` insertions so the JSX editor now gets the real visible markup (`<Chips>`, `<Chips.Toggle>`, `<Chips.Removable>`) while the Hooks tab only receives the supporting `options` and `useState` declarations.
- Kept collision safety for repeated insertions: when both examples coexist, the second insertion now suffixes its support identifiers (`options2`, `filter2`, `setFilter2`) instead of reintroducing helper wrappers.
- Added a focused regression at the editor-apply seam in `tests/unit/services/componentInsertion.test.ts` and updated the catalog/autocomplete/Add-menu assertions so they now fail if a future change collapses the Chips JSX back into helper tags.

Verification:

- npm run test -- --run tests/unit/services/componentInsertion.test.ts tests/unit/akselCatalog.test.ts tests/unit/akselAutocomplete.test.ts tests/integration/component-palette.test.tsx
- npm run typecheck
- npm run test -- --run
- Browser check at `http://127.0.0.1:5182/aksel-arcade/`: confirmed Add -> `Chips Toggle` inserts visible `<Chips.Toggle>` JSX into the JSX tab, confirmed Add -> `Chips Removable` inserts visible `<Chips.Removable>` JSX and suffixes the second example’s support identifiers in the Hooks tab, and confirmed the preview renders both chips examples without runtime console errors or warnings.

### 2026-06-12 - PR #230 review follow-up for issue #209 insertion polish

Fixed before merge:

- Moved the `neutral` color contract onto `Chips.Toggle` and `Chips.Removable` themselves instead of the outer `<Chips>` wrapper so the generated examples match Aksel’s supported API.
- Made the shared `ProgressBar` snippet use collision-safe label ids (`applicationProgressLabel`, `applicationProgressLabel2`, ...) so repeated insertions do not create duplicate `aria-labelledby` targets.
- Updated `insertionNeedsEditorApply` so catalog snippets with collision tokens also route through the editor-apply path even when they do not need Hooks-tab support, which keeps repeated pure-JSX insertions safe.
- Added/updated regression coverage across `tests/unit/services/componentInsertion.test.ts`, `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, and `tests/integration/component-palette.test.tsx` for the Chips child `data-color` contract plus repeated `ProgressBar` id deconfliction.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- Browser check at `http://127.0.0.1:5183/aksel-arcade/`: confirmed the JSX tab shows direct `Chips.Toggle` / `Chips.Removable` markup with `data-color="neutral"` on each child, the Hooks tab still contains support state only, repeated Add -> `ProgressBar` insertions render two labeled progress bars in the preview with distinct ids, and the verified session showed no browser console errors (only the pre-existing form-field accessibility issue).

### 2026-06-12 - Issue #212 catalog-backed content, typography, and link insertions

Implemented:

- Picked `#212` as the highest-priority remaining `ready-for-agent` issue in the requested `#212`-`#213` range because it is the user-facing insertion slice, while `#213` is still the broader maintainer workflow/audit follow-up.
- Moved the remaining issue-212 authoring targets onto the shared Aksel insertion catalog: `BodyLong`, `BodyShort`, `Chat`, `CopyButton`, `Detail`, `GuidePanel`, `Heading`, `InternalHeader`, `Label`, `Link`, `LinkCard`, and `List`, with visible composable snippets that can be inserted inside existing layouts without wrapper fragments or helper components.
- Removed the matching legacy manual palette/snippet duplicates from `src/data/akselComponents.ts` and `src/services/componentLibrary.ts` so the Add menu, autocomplete, and snippet fallback all stay sourced from the shared catalog instead of diverging copies.
- Kept the new snippets on supported runtime props (`data-color`, `textColor`, `position`, etc.) and added the missing preview iframe `allow="clipboard-write"` permission so sandboxed `CopyButton` examples can use the clipboard API without widening the iframe sandbox origin.
- Added regression coverage in `tests/unit/akselCatalog.test.ts`, `tests/unit/akselAutocomplete.test.ts`, `tests/integration/component-palette.test.tsx`, and `tests/unit/sandboxSecurity.test.ts` for catalog presence, top-level autocomplete/apply strings, Add-menu insertion behavior, and the clipboard-write iframe permission.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with the existing Vite/browser-compatibility and chunk-size warnings only)
- Browser check at `http://127.0.0.1:4387/aksel-arcade/`: confirmed Add inserts the catalog-backed `BodyLong`, `Chat`, `CopyButton`, `GuidePanel`, `InternalHeader`, `LinkCard`, and `List` examples into the JSX editor, confirmed `CopyButton` changes to `Case number copied` after click when the verification browser context is granted clipboard permission, and confirmed the verified session showed no browser console warnings or errors.

Next note:

- The remaining open issue from the original `#212` / `#213` pair is `#213`. If a future slice needs more preview support for sandboxed interactive components, keep `sandbox="allow-scripts"` and add narrowly scoped iframe `allow` capabilities instead of broadening the sandbox origin or enabling modals.

### 2026-06-12 - PR #231 follow-up for issue #212 InternalHeader snippet

Implemented:

- Replaced the simple issue-212 `InternalHeader` catalog example with the richer user-menu structure requested in follow-up: `InternalHeader.Title`, `Spacer`, `ActionMenu`, `InternalHeader.UserButton`, themed `ActionMenu.Content`, user identity details, and the trailing `LeaveIcon` logout action.
- Updated the catalog import guidance to include the full snippet surface (`ActionMenu`, `BodyShort`, `Detail`, `InternalHeader`, `Spacer`, `Theme`, and `LeaveIcon`) and added a Hooks-tab `resolvedTheme` helper so the inserted example renders immediately in the sandbox without requiring manual setup.
- Kept the insertion composable and repeat-safe by suffixing the `resolvedTheme` helper through the shared collision-token path, then updated the catalog/autocomplete/Add-menu regressions so `InternalHeader` now behaves like the other callback-backed Hooks snippets.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with the existing Vite/browser-compatibility and chunk-size warnings only)
- Browser check at `http://127.0.0.1:4387/aksel-arcade/`: confirmed Add -> `InternalHeader` inserts the updated JSX plus Hooks helper, confirmed the preview renders the `Sykepenger` header and `Ola N.` user button, confirmed opening the user menu shows `Ola Normann`, `D123456`, and `Logg ut`, and confirmed the verified session showed no browser console warnings or errors.

### 2026-06-12 - PR #231 review fix for InternalHeader theme syncing

Implemented:

- Code review found one real PR-scoped regression in the new `InternalHeader` insertion: the ActionMenu content theme stayed locked to the initial preview theme instead of following later light/dark toggles.
- Reworked the Hooks-tab helper so the inserted example now uses a collision-safe `useResolvedTheme{{internalHeaderSuffix}}` custom hook, with a `MutationObserver` that watches the sandbox document class and updates `resolvedTheme{{internalHeaderSuffix}}` when the preview theme changes.
- Kept the requested JSX structure intact while fixing the runtime behavior, and updated the catalog/autocomplete/Add-menu expectations to match the new helper shape.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with the existing Vite/browser-compatibility and chunk-size warnings only)
- Browser check at `http://127.0.0.1:4387/aksel-arcade/`: confirmed the inserted `InternalHeader` menu opens in dark mode with a dark theme wrapper, confirmed switching the preview theme through Settings -> `Switch to light theme` flips the preview root and the visible ActionMenu wrapper to light before reopening the menu, and confirmed the verified session showed no browser console warnings or errors.

### 2026-06-12 - Issue #213: Aksel update and docs/runtime catalog audit workflow

Implemented:

- Worked only on `#213` after re-reading issue `#205`, `CONTEXT.md`, and the latest `progress.txt` notes so the maintainer workflow stayed aligned with the shared **Aksel insertion catalog** contract instead of turning into a second authoring feature slice.
- Added explicit maintainer commands in `package.json`: `npm run aksel:refresh-docs -- --write src/data/akselAutocompleteData.ts` to fetch fresh `https://aksel.nav.no/llm.md` data and rewrite the checked-in docs metadata, and `npm run aksel:audit -- --target X.Y.Z` to run the manual docs/runtime/catalog drift audit against a visible target Aksel version.
- Extracted the docs parser/generator into `scripts/lib/akselDocs.ts`, kept the legacy `scripts/generate-aksel-autocomplete-data.mjs` path as a wrapper to the new TypeScript entrypoint, and added fixture-backed parser coverage in `tests/unit/akselDocs.test.ts` plus `tests/fixtures/aksel-audit/` so normal validation stays network-independent.
- Added `scripts/lib/akselAudit.ts` and `scripts/aksel-audit.ts` to compare fresh docs against pinned package versions, lockfile versions, `AKSEL_METADATA`, runtime exports, icon exports, the curated catalog, and the checked-in docs metadata, while separating accepted local exceptions from new potential drift.
- Added the explicit local exceptions ledger in `src/data/akselAuditExceptions.ts` for the currently intentional Arcade-specific differences that the audit should not silently reclassify: `Combobox` -> `UNSAFE_Combobox`, `Modal` hidden behind replacement policy, `Navpoleonskake` docs noise, and the retained legacy compatibility entries for `Alert`, `BoxNew`, `Stack`, and `Grid`.
- Moved the sandbox compatibility constants into `src/data/akselRuntimeCompatibility.ts`, re-exported them from `src/sandboxAksel.ts`, and updated the sandbox import to a relative path so the same compatibility data works in the app, the audit CLI, and the standalone sandbox build.
- Added a dedicated `tsconfig.scripts.json` so the new TypeScript CLI scripts are checked by `tsc -b` without dragging unrelated experimental scripts through the wrong compiler config, and documented the full maintainer flow in `README.md`: update exact Aksel package pins and lockfile, refresh docs metadata, run the audit, review potential drift, clarify any new/changed exceptions with a human, then rerun normal validation.
- Added fixture-based audit coverage in `tests/unit/akselAudit.test.ts` for comparison/classification/report generation, including the requirement that new or changed potential exceptions stay non-zero and instruct the operator to ask a human before encoding them.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with the existing Vite/browser-compatibility and chunk-size warnings only)
- Browser check at `http://127.0.0.1:4186/aksel-arcade/`: confirmed the app shell and default preview render, confirmed the sandbox iframe shows the default intro content, and confirmed the verified session had no browser console errors or warnings (only the pre-existing form-field accessibility issue reported as a DevTools issue).

Next note:

- `npm run aksel:audit -- --target 8.11.0` now runs successfully and intentionally exits non-zero because it found current potential drift that should be reviewed instead of silently whitelisted. The focused report currently points at three main buckets: top-level catalog coverage gaps (`Bleed`, `Combobox`, `Hide`, `InfoCard`, `Provider`, `RadioGroup`, `Show`), docs-metadata/authoring prop drift around `ActionMenu` and a couple of stale generated props, and deprecated/preview autocomplete exposure (`ConfirmationPanel`, `DataGrid`, `Ingress`, `LinkPanel`, `Panel`).
- Do not add new accepted exceptions for those findings without human confirmation. The issue-213 workflow is now in place specifically so the next maintainer can review that report, decide which items should become intentional Arcade policy, and then update the catalog/docs metadata in a separate follow-up.

### 2026-06-12 - PR #232 review follow-up for issue #213

Implemented:

- PR review found two real audit correctness bugs before merge.
- Tightened accepted-exception matching in `scripts/lib/akselAudit.ts` so accepted entries no longer match on name + match kind alone; the audit now also checks the exception item type, affected surface, and pinned runtime alias target when that metadata is present. This keeps changed alias/runtime conditions from being silently auto-whitelisted.
- Split docs matching into a coverage path and a status path so user-facing variant labels can still map to their parent docs entry where intended, but compound entries such as `Page.Block` no longer inherit status from `Page` through `importName` matching.
- Added focused regressions in `tests/unit/akselAudit.test.ts` for both review findings: changed runtime alias targets must stay potential drift, and compound catalog entries must report their own deprecated/stale docs status instead of borrowing the parent component status.

Verification:

- npm run test -- --run tests/unit/akselAudit.test.ts
- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (passes with the existing Vite/browser-compatibility and chunk-size warnings only)

### 2026-06-14 - Issue #234: Persist Preview fullscreen as tab-local workspace state

Implemented:

- Picked `#234` as the highest-priority open `ready-for-agent` issue from `#234`-`#240` after checking issue `#205`, the parent PRD `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. It is the root dependency for the whole Preview fullscreen stack, and every later ready slice depends on its storage/runtime contract.
- Extended `WebArcadeWorkingCopyPreferences` and `SettingsContext` with `previewFullscreen`, keeping it as tab-local **Workspace preference** state with a backward-compatible default of `false` for older stored working copies.
- Updated `saveProject` with an `updateLastModified` escape hatch so workspace-only persistence can write the working copy envelope without mutating Arcade project data, then taught `useAutoSave` to persist fullscreen changes immediately while keeping normal debounced autosave behavior for ordinary project/settings changes.
- Wired the restored fullscreen preference back through `useProject` / `App` so reloaded working copies and forked duplicated tabs keep the current fullscreen intent in the same session-scoped storage model as the other workspace preferences.
- Added coverage in `tests/unit/services/storage.test.ts`, `tests/unit/useAutoSave.test.tsx`, and `tests/integration/share-decode.test.tsx` for backward-compatible defaults, duplicated-tab persistence, package exclusion, immediate fullscreen persistence, and reload survival.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice and existing `InspectionPopover` hook dependency warning only)
- npm run test -- --run
- Browser check at `http://127.0.0.1:4337/aksel-arcade/`: confirmed the working copy now stores the full preference envelope including `previewFullscreen`, forced `previewFullscreen: true` into the tab-scoped working copy, reloaded the same tab, confirmed the stored preference stayed `true`, and confirmed the verified session had no browser console warnings or errors.

Next note:

- `#235`-`#240` can now build on the persisted `previewFullscreen` workspace preference instead of inventing their own storage path. Keep future enter/exit/share behavior wired through the same `SettingsContext` + working-copy envelope so fullscreen stays out of durable Arcade project/package data.

### 2026-06-14 - Issue #235: Add normal preview-header enter fullscreen path

Implemented:

- Picked `#235` as the highest-priority open `ready-for-agent` issue from `#235`-`#240` after checking the parent PRD `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. It is the first user-facing Preview fullscreen entry point and it blocks the later fullscreen chrome/share slices.
- Added the preview-header fullscreen toggle to `PreviewPane` on the far left, keeping Inspect and viewport controls on the right and exposing pressed-state semantics through the same tab-local `previewFullscreen` setting introduced in `#234`.
- Wired the App shell and split-pane layout to hide the normal Arcade header and the editor panel from both the visual layout and the accessibility tree while Preview fullscreen is active, without swapping out the mounted preview pane.
- Kept the live preview iframe mounted across fullscreen entry/exit by collapsing the editor/layout chrome instead of remounting the preview tree; the preview header button now retains focus across the toggle and provides the temporary safe fullscreen focus target until the later sticky fullscreen chrome slice lands.
- Added regression coverage in `tests/integration/app-preview-fullscreen.test.tsx` and `tests/integration/preview-fullscreen-entry.test.tsx` for the fullscreen enter path, history/address stability, hidden normal chrome, and the workspace-only persistence contract while keeping the existing `#234` last-modified storage coverage intact.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and the existing `InspectionPopover` hook-dependency warning only)
- npm test -- --run
- Browser check at `http://127.0.0.1:5187/aksel-arcade/`: confirmed the normal preview header now shows the fullscreen toggle on the far left, entering fullscreen removes the normal header/editor from the accessibility tree, the same preview iframe survives the round trip, focus lands on the fullscreen toggle, and the URL/history stay unchanged. The checked session showed no runtime console errors; Chrome still reported the pre-existing form-field issue entry.

Next note:

- `#237` can now refine the same preview-header control into the sticky fullscreen-specific chrome (Escape handling, focus restoration, responsive wrapping) instead of introducing a second fullscreen state path. Keep reusing the existing `previewFullscreen` workspace-preference wiring and avoid pushing fullscreen into share/package project data except where `#236`/`#238` explicitly add Web share opening intent.

### 2026-06-14 - PR #243 review follow-up for issue #235

Implemented:

- PR review found that the editor pane remained keyboard-focusable in **Preview fullscreen** because `SplitPane` only used `aria-hidden` plus a CSS class. Added real `hidden` state to the editor panel and divider so fullscreen removes that subtree from both layout interaction and the accessibility tree without remounting the preview pane.
- PR review also caught that the fullscreen preview surface used raw `'0'` padding values on Aksel `Box` props. Replaced them with the valid `space-0` token so the fullscreen path stays compatible with Aksel v8 primitive typing.
- Tightened `tests/integration/app-preview-fullscreen.test.tsx` to assert the hidden editor control now lives under a real `[hidden]` ancestor, and updated `tests/integration/preview-fullscreen-entry.test.tsx` to verify the fullscreen button size through a stable test-owned `data-size` signal instead of Aksel internal class names.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `.eslintignore` deprecation notice and the existing `InspectionPopover` hook-dependency warning only)
- npm run test -- --run
- npm run build (confirmed the PR-scoped `PreviewPane` padding token issue was fixed; the remaining `tests/unit/useAutoSave.test.tsx` build/type error is outside this PR diff and was left untouched in this review follow-up)
- Browser check at `http://127.0.0.1:5187/aksel-arcade/`: confirmed entering fullscreen leaves only preview controls in the accessibility tree, the editor panel and divider both carry real `hidden` state, and the visible controls no longer include the editor/header chrome.

### 2026-06-14 - Issue #236: Encode Preview fullscreen opening intent in Web share URLs

Implemented:

- Picked `#236` as the highest-priority open `ready-for-agent` issue from `#236`-`#240` after checking the parent PRD `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. It is the earliest unblocked fullscreen-share slice and it gates both share hydration (`#238`) and fullscreen share-popover reuse (`#239`).
- Extended the v3 Web share payload contract with optional top-level `previewFullscreen: true`, keeping it strictly as checksum-protected share-opening intent rather than durable **Arcade project** data, a **Preview preference**, or `.akselarcade` package content.
- Updated `useShareLink` / share encoding so normal share generation still omits the field, while future fullscreen-share callers can opt in through the hook with `{ openingIntent: { previewFullscreen: true } }` instead of inventing a second payload path.
- Updated share decoding to expose the optional opening intent separately from the decoded snapshot model, and threaded that intent into `shareHydration` so `#238` can apply it after user confirmation without revisiting the payload boundary.
- Added regression coverage in `tests/unit/utils/shareEncoding.test.ts`, `tests/unit/utils/shareDecoding.test.ts`, `tests/integration/share-popover.test.tsx`, and `tests/integration/share-decode.test.tsx` for fullscreen-intent inclusion, normal-share omission, backward-compatible decoding, checksum/tamper protection, and pre-apply hydration exposure.

Verification:

- Ran `npm install` in this worktree because dependencies were missing (`tsc` was not initially available on PATH through local `node_modules/.bin`).
- `npm run typecheck`
- `CI=1 npm run test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:4189/aksel-arcade/`: confirmed the app loaded without console errors, Preview fullscreen still entered/exited cleanly, the normal Share project popover still generated/copyable Web share URLs, and the verified session had no runtime warnings or page errors.

Next note:

- `#238` can now consume `shareHydration.openingIntent` to apply fullscreen only after “Load Web share URL”, while `#239` can reuse the same `useShareLink({ openingIntent: { previewFullscreen: true } })` path for the fullscreen header share button. Keep normal-header sharing on the omission path; absence of the field now intentionally means “preserve the recipient tab’s current fullscreen state.”

### 2026-06-14 - Issue #238: Apply Preview fullscreen intent during Web share hydration

Implemented:

- Picked `#238` as the highest-priority unblocked `ready-for-agent` issue from `#238`-`#240` after checking issue `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. It completes the recipient-side fullscreen share-opening contract already added in `#236`, and it needs to land before `#239` exposes fullscreen share links in the UI.
- Kept share hydration consent-based: fullscreen-intent Web share URLs still leave the recipient tab’s current `previewFullscreen` state untouched until the user clicks **Load Web share URL**, while dismissing the share leaves the current layout state unchanged.
- Moved App-level warning positioning into a shared `app-shell__notifications` container so fullscreen share confirmations, decode errors, save errors, and size warnings all switch from fixed overlay mode to inline flow mode while **Preview fullscreen** is active. That keeps the sticky fullscreen preview header visible and pushes the preview surface down instead of covering controls.
- Added regression coverage in `tests/integration/share-decode.test.tsx` for the fullscreen-intent accept path (stays out of fullscreen before consent, enters fullscreen after accept, and persists fullscreen for reloads) plus the dismiss path while already fullscreen.
- Added regression coverage in `tests/integration/app-share-load-ui.test.tsx` for the App-level fullscreen notification layout: a fullscreen-intent share confirmation does not force fullscreen on by itself, and once the recipient tab is fullscreen the confirmation moves into the inline fullscreen flow container.

Verification:

- Ran `npm install` in this worktree before verification because local dependencies were missing initially.
- `npm run typecheck`
- `npm run lint` (passes with the existing `.eslintignore` deprecation notice and the existing `InspectionPopover` hook-dependency warning only)
- `npm run test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:5191/aksel-arcade/` against this worktree’s own Vite server (shared-machine ports `5173`-`5190` were already occupied): confirmed a fullscreen-intent share URL keeps the recipient tab fullscreen confirmation inline above the sticky header, **Keep my work** preserves fullscreen, a normal share URL accepted while already fullscreen does not exit fullscreen, a fullscreen-intent share accepted while already fullscreen stays fullscreen, and reload keeps the accepted fullscreen share open in fullscreen. No browser console errors or warnings appeared during the verified flow; DevTools still showed the pre-existing form-field accessibility issue.

Next note:

- `#239` can reuse the new `app-shell__notifications` wrapper without adding fullscreen-specific popover positioning logic. Keep the fullscreen header share button lifecycle tied to the existing `previewFullscreen` enter/exit path so popovers reset cleanly when the layout mode changes.

### 2026-06-14 - Issue #237: Add fullscreen preview-header exit and responsive chrome behavior

Implemented:

- Picked `#237` as the highest-priority open `ready-for-agent` issue from `#237`-`#240` after checking the parent PRD `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. It is the first remaining unblocked fullscreen slice and the later share/capability issues explicitly depend on it.
- Updated `PreviewPane` so **Preview fullscreen** now has its own sticky preview-header state with the icon-only “Exit preview fullscreen” control on the left, while Inspect and the viewport toggle stay available on the right in the same preview chrome.
- Added explicit fullscreen-exit handling instead of a blind toggle path so Escape from preview chrome exits fullscreen and focus reliably returns to the normal preview-header fullscreen toggle after exit.
- Kept the existing preview iframe, inspect-mode state, selected viewport breakpoint, and preview theme intact across fullscreen enter/exit by reusing the existing `previewFullscreen` workspace-preference wiring rather than remounting preview content.
- Added responsive fullscreen-header layout behavior in `PreviewPane.css`: controls can wrap across multiple rows on narrow screens, the viewport toggle can fill available width in fullscreen, and the preview iframe starts below the taller wrapped header instead of being overlapped.
- Extended regression coverage in `tests/integration/preview-fullscreen-entry.test.tsx` and `tests/integration/app-preview-fullscreen.test.tsx` for the fullscreen exit path, Escape handling, focus restoration, preserved inspect/viewport state, sticky fullscreen-header mode, and restoring the normal workspace chrome after exit.

Verification:

- `npm run type-check`
- `npm run lint` (passes with the existing `.eslintignore` deprecation notice and the existing `InspectionPopover` hook-dependency warning only)
- `CI=1 npm test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:4190/aksel-arcade/`: confirmed the preview header shows the enter button in normal mode, entering fullscreen hides the normal app chrome and editor, the fullscreen header becomes sticky with focus on the exit button, narrowing to `360px` wraps controls into multiple rows and keeps the iframe below the header, pressing Escape from preview chrome exits fullscreen and restores focus to the normal toggle, and the verified session showed no browser console errors.

Next note:

- `#239` can now add the fullscreen-only share control onto the same sticky preview-header structure without inventing another fullscreen layout. Keep the share button last in the right-hand control order when the header wraps, and reuse the existing `previewFullscreen` exit path so fullscreen-only popovers can reset cleanly on exit.

### 2026-06-14 - PR #246 review follow-up for issue #237

Implemented:

- Code review of PR `#246` found that the new Escape handler only listened on the fullscreen header, so Escape would miss other fullscreen **Arcade chrome** rendered inside the preview pane, such as the compile/runtime error overlay close button.
- Moved the Escape handler from the preview header to the fullscreen preview-pane section so key events from other fullscreen chrome inside the pane still exit **Preview fullscreen** while preserving the existing iframe behavior.
- Added a regression in `tests/integration/preview-fullscreen-entry.test.tsx` that forces a compile error, enters fullscreen, focuses the fullscreen error chrome, presses Escape, and confirms fullscreen exits with focus restored to the normal toggle.

Verification:

- `npm run type-check`
- `CI=1 npm test -- --run tests/integration/preview-fullscreen-entry.test.tsx`

Next note:

- `#239` can keep relying on pane-level fullscreen Escape handling for any fullscreen-only share popover chrome it adds later, rather than rebinding Escape separately on each fullscreen control surface.

### 2026-06-14 - Post-merge fullscreen header alignment follow-up

Implemented:

- Fixed the post-merge preview-header layout regression that affected both **Web Arcade** and **Desktop Arcade**: the right-side Inspect + breakpoint controls were no longer hugging the right edge, and the breakpoint ToggleGroup stretched across the available header width in both normal and fullscreen states.
- Root cause was the merged flex contract in `PreviewPane.css` plus the fullscreen-only `fill` prop passed into `ViewportToggle`. The right control group and viewport wrapper were both allowed to grow, so the ToggleGroup expanded to consume most of the header and dragged the whole control cluster left.
- Restored the intended layout by making the outer header-controls row explicitly span the full header width, keeping the right-side wrapper hug-content with `margin-inline-start: auto`, and removing the fullscreen `fill` behavior from `ViewportToggle` so the ToggleGroup keeps its intrinsic width in both modes.
- Added a focused Playwright regression in `tests/e2e/preview-header-layout.spec.ts` that checks the header geometry in normal and fullscreen modes: the right-side control group must stay flush to the right edge and both the group and ToggleGroup must remain well under the full header width instead of stretching.
- PR review also found that the repo-default Playwright runner could bind to an unrelated reused `localhost:5173` Vite server from another checkout. Hardened `playwright.config.ts` so `npm run test:e2e` now starts an isolated repo-local Vite server on a stable per-worktree port by default, with explicit reuse still available through `PLAYWRIGHT_REUSE_SERVER=true` and `PLAYWRIGHT_PORT`.
- Follow-up review then caught three older e2e specs that still hardcoded `http://localhost:5173`, bypassing the shared Playwright `baseURL`. Updated those specs to use `/aksel-arcade/` and refreshed their assertions to the current preview/editor/runtime shape so the affected browser suite now runs against the isolated per-worktree server consistently.

Verification:

- `npm run typecheck`
- `npm run lint` (passes with the existing `.eslintignore` deprecation notice and the existing `InspectionPopover` hook-dependency warning only)
- `CI=1 npm run test -- --run`
- `npm run build`
- `npx playwright test tests/e2e/preview-header-layout.spec.ts --project=chromium`
- `npx playwright test tests/e2e/preview-header-layout.spec.ts tests/e2e/aksel-components.spec.ts tests/e2e/alert-theme-verification.spec.ts tests/e2e/theme-wrapper.spec.ts --project=chromium`
- Focused browser regression against the current worktree server on `http://127.0.0.1:4190/aksel-arcade/` confirmed the fix numerically in both modes:
  - normal mode: right controls width `337px`, ToggleGroup width `293px`
  - fullscreen mode: right controls width `337px`, ToggleGroup width `293px`

Next note:

- If later fullscreen-header work adds another control (for example the fullscreen share button in `#239`), preserve the same hug-content layout on the right wrapper; avoid reintroducing `flex: 1` or ToggleGroup `fill` on this header row unless the design explicitly changes.

### 2026-06-14 - PR #245 review follow-up for issue #236

Implemented:

- PR review found that fullscreen-intent share payloads were still emitted as format `3`, which would have made older clients reject the new top-level field as malformed v3 payloads. Added a dedicated format `4` for payloads that carry `previewFullscreen: true` while keeping normal share links on v3 and legacy full snapshots on v2.
- PR review also found that loading a normal Web share URL while the recipient tab was already in **Preview fullscreen** still forced fullscreen off because share apply reset workspace preferences to defaults. Share apply now preserves the current `previewFullscreen` state when the share has no fullscreen opening intent.
- Kept the opening intent separate from the Arcade project snapshot model, and extended decode handling so both minimal Web share formats (v3 normal, v4 fullscreen-intent) go through the same portable payload parser.
- Added regression coverage proving fullscreen-intent payloads use format v4, decoded opening intent is still exposed for fullscreen-intent shares, and accepting a normal share while already fullscreen keeps the recipient tab fullscreen.

Verification:

- `npm run typecheck`
- `CI=1 npm run test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:4189/aksel-arcade/`: generated a normal Web share URL, entered Preview fullscreen, reopened that share URL in the same tab, accepted “Load Web share URL”, and confirmed fullscreen stayed active before and after load with no console errors or page errors.
### 2026-06-14 - Follow-up fixes on Issue #238 / PR #248

Implemented:

- Fixed the reload blank-preview regression by removing the 500ms typing debounce from initial / project-replacement preview transpilation while keeping the debounce for same-project edits that happen with the code editor focused.
- Fixed the page-panel regression after accepting a Web share URL by preserving the recipient tab's `multiPageEnabled` and `pagePanelOpen` working-copy preferences instead of resetting them to the single-page defaults.
- Kept page-scoped runtime errors visible until the sandbox reports a fresh render success, which avoids clearing page-panel error indicators during fast recompiles triggered around the new reload behavior.
- Added regression coverage in `tests/integration/preview-fullscreen-entry.test.tsx` for immediate initial transpilation and in `tests/integration/share-decode.test.tsx` for preserving the recipient multi-page workspace affordance on share load.

Verification:

- npm run typecheck
- npm run lint (passes with the existing `InspectionPopover` hook dependency warning and ESLint `.eslintignore` deprecation warning)
- npm test -- --run
- npm run build
- Browser checks at `http://127.0.0.1:5193/aksel-arcade/` for fast dark-mode reload preview recovery and for keeping the `Show pages` button after accepting a Web share URL into a tab with multi-page enabled

### 2026-06-14 - Issue #239: Reuse Web share popover from the fullscreen preview header

Implemented:

- Picked `#239` as the single highest-priority `ready-for-agent` feature from `#239`-`#240` after checking the parent PRD `#233`, `CONTEXT.md`, the Figma fullscreen nodes (`125:568` / `125:955`), and the latest `progress.txt` notes. `#240` depends on `#239`, so the fullscreen-header share slice was the next unblocked priority.
- Extracted the normal header Web share button/popover behavior into `src/components/Share/SharePopoverButton.tsx` so the normal header and the fullscreen preview header now share the same generation, retry, warning, copy-fallback, oversize, and export-fallback flow instead of drifting in two implementations.
- Wired `PreviewPane` to show a fullscreen-only icon button labelled **Share fullscreen preview** as the last control in the fullscreen preview-header right cluster, matching the Figma order of Inspect → viewport toggle → share and keeping the share button far right / last in tab order when the header wraps.
- Added the fullscreen-only popover note `opens in fullscreen` and passed `openingIntent: { previewFullscreen: true }` only from the fullscreen share path, while normal header sharing still omits fullscreen opening intent.
- Added fullscreen-transition lifecycle cleanup: entering fullscreen closes and resets the normal header share popover, exiting fullscreen closes and resets the fullscreen share popover, and `useShareLink` now invalidates in-flight generations so hidden share flows do not complete into stale UI state.
- Kept capability boundaries and experimental multi-page behavior intact by omitting the fullscreen share button when Web share URLs are unavailable and reusing the existing Start-page-only warning/path for fullscreen sharing.
- Added focused regression coverage in `tests/integration/fullscreen-share-popover.test.tsx` for fullscreen visibility, control order, fullscreen intent, capability gating, multi-page warning behavior, and popover reset across fullscreen enter/exit.

Verification:

- `npm run typecheck`
- `npm run lint` (passes with the existing `InspectionPopover` hook dependency warning and ESLint `.eslintignore` deprecation warning)
- `npm run build`
- `CI=1 npm run test -- --run`
- Browser checks at `http://127.0.0.1:4193/aksel-arcade/`: opened the normal header share popover, entered **Preview fullscreen** and confirmed the normal popover closed; confirmed the fullscreen share button was the last/rightmost preview-header control; opened the fullscreen share popover and confirmed the `opens in fullscreen` note; exited fullscreen with the popover open and confirmed it closed; enabled multi-page authoring, added a second page, shared from fullscreen, and confirmed the existing Start-page-only warning appeared; no browser console errors appeared during the verified flow.

Next note:

- `#240` can now focus on end-to-end capability-boundary validation without reworking share UI plumbing. The reusable share control and `useShareLink` cancellation path are already in place; keep Desktop/non-share-capable verification building on the new `PreviewPane shellCapabilities` wiring instead of adding another fullscreen-specific branch.

### 2026-06-14 - PR #250 review follow-up for issue #239

Implemented:

- PR review found that the new pane-level fullscreen `Escape` handler also caught `Escape` presses from inside the fullscreen share popover, which could kick the user out of **Preview fullscreen** instead of dismissing the popover first.
- Tagged the reusable share popover content with an explicit data boundary and updated `PreviewPane` to ignore fullscreen-exit `Escape` handling when the key event originates inside that share popover subtree.
- Added a regression in `tests/integration/fullscreen-share-popover.test.tsx` proving the first `Escape` closes the fullscreen share popover while leaving fullscreen active, and the next `Escape` still exits fullscreen normally.

Verification:

- `npm run typecheck`
- `npm run build`
- `CI=1 npm run test -- --run tests/integration/fullscreen-share-popover.test.tsx tests/integration/share-popover.test.tsx tests/integration/preview-fullscreen-entry.test.tsx tests/integration/app-preview-fullscreen.test.tsx`
- Browser check at `http://127.0.0.1:4194/aksel-arcade/`: opened the fullscreen share popover, pressed `Escape`, confirmed the popover closed while fullscreen stayed active, then focused the fullscreen exit control and confirmed `Escape` still exited fullscreen; no browser console errors appeared during the verified flow.

### 2026-06-15 - Issue #240: Validate Preview fullscreen across Web and Desktop capability boundaries

Implemented:

- Picked `#240` as the single highest-priority ready-for-agent feature after checking issue `#240`, parent PRD `#233`, `CONTEXT.md`, and the latest `progress.txt` notes. This slice stays strictly on the final Preview fullscreen validation boundary and does not change the shipped fullscreen/share UI behavior.
- Extended `tests/integration/app-preview-fullscreen.test.tsx` so the fullscreen layout regression now runs against both `WEB_ARCADE_CAPABILITIES` and `DESKTOP_ARCADE_CAPABILITIES`, proving the shared App shell still hides/restores the normal chrome and preserves tab-local fullscreen persistence across both product-surface capability sets.
- Fixed the Desktop preview integration harness in `tests/integration/project-controls-layout.test.tsx` to pass the active `shellCapabilities` into `PreviewPane` instead of silently falling back to Web defaults, then added a Desktop-only regression proving a clean `.akselarcade` import exits **Preview fullscreen** and still omits the fullscreen share button in the non-share-capable shell.
- Tightened `tests/unit/services/storage.test.ts` so clean package exports explicitly exclude Web-share-only `openingIntent` metadata and imports reject polluted package/import payloads that try to carry `openingIntent` or `previewFullscreen`, keeping fullscreen opening intent out of `.akselarcade` boundaries entirely.

Verification:

- `npm run typecheck`
- `npm run lint` (existing `InspectionPopover` hook dependency warning only)
- `npm run build`
- `CI=1 npm run test`
- Browser check at `http://127.0.0.1:4193/aksel-arcade/`: entered **Preview fullscreen**, confirmed the fullscreen share button appeared in the Web shell, opened the share popover and saw the `opens in fullscreen` note, exited fullscreen back to the normal layout, and confirmed there were no browser console errors during the verified flow.

Next note:

- Any future Desktop/non-share-capable preview integration test that mounts `PreviewPane` must thread the intended `shellCapabilities` through the harness; otherwise the preview silently uses Web defaults and can hide capability-boundary regressions like the fullscreen share button appearing where it should be omitted.

### 2026-06-15 - Issue #253: Default the Page panel closed and fix the toggle wrapper

Implemented:

- Selected `#253` as the highest-priority `ready-for-agent` feature from `#253`-`#259` because it is unblocked, user-visible, and it directly unblocks the always-on pages work in `#255` plus the later lossless share/package/agent follow-ups.
- Read issue `#233`, `docs/prd/0030-multi-page-support.md`, `CONTEXT.md`, and the existing `progress.txt` notes before changing code.
- Changed the default working-copy `pagePanelOpen` preference to `false` in `src/services/storage.ts` and `src/contexts/SettingsContext.tsx` so fresh Web/Desktop working copies, **Reset editor**, and imported replacement working copies now start from the closed-panel default.
- Kept share hydration recipient-local in `src/hooks/useProject.tsx`: Web share loads still preserve the current tab's `pagePanelOpen` value, while reset/import replacements intentionally snap back to the closed default.
- Replaced the bespoke xsmall icon-only page toggle in `src/components/Editor/EditorPane.tsx` and `src/components/Editor/EditorPane.css` with a neutral small Aksel button that visibly says **Show pages** / **Hide pages** and uses standard button sizing instead of the custom 24px wrapper hack.
- Added regression coverage in `tests/integration/multi-page-page-panel.test.tsx` for the closed-default **Show pages** control, and in `tests/integration/share-decode.test.tsx` for **Reset editor** plus imported replacement working copies restoring the closed page-panel default.

Verification:

- `npm install`
- `npm run typecheck`
- `npm test -- --run`
- `npm run lint` (existing `InspectionPopover` hook dependency warning only)
- Browser check at `http://127.0.0.1:4193/aksel-arcade/`: enabled **Multiple pages** on a fresh working copy, confirmed the visible **Show pages** control with the panel closed, opened it to the `Config` / `Pages` sections plus **Add page**, and saw no browser console warnings/errors during the verified flow

Next note:

- `#255` can now remove the remaining editor/preview multi-page gating without preserving the old open-by-default panel assumption. `pagePanelOpen` is now a closed-by-default workspace preference; share hydration still preserves the recipient-local value, while reset/import replacements deliberately restore the closed default.

### 2026-06-15 - PR #260 review follow-up for issue #253

Fixed after review:

- Tightened `tests/unit/services/storage.test.ts` so the empty-storage `loadProject()` path now asserts the full default working-copy preferences, including `pagePanelOpen: false`, covering the true fresh-working-copy default instead of only stored-preference restores.
- Extended `tests/integration/multi-page-page-panel.test.tsx` with a stale-reference regression that closes the page panel after broken-navigation state exists and confirms the collapsed **Show pages** control stays neutral rather than surfacing warning/error state.

Verification:

- `npm run typecheck`
- `npm test -- --run`

### 2026-06-15 - Issue #255: Make editor and preview page flows always on

Implemented:

- Selected `#255` as the highest-priority `ready-for-agent` feature from `#254`-`#259` because `#253` is already closed, `#255` is the core always-on pages model change, and it directly unblocks the lossless share/package/agent follow-ups in `#256`-`#258`.
- Read issue `#252`, `CONTEXT.md`, and the current `progress.txt` notes before changing code.
- Removed the editor/preview runtime gate from `src/services/projectSource.ts`, `src/hooks/useProject.tsx`, `src/components/Editor/EditorPane.tsx`, and `src/components/Preview/PreviewPane.tsx` so selected edit targets, page-panel visibility, preview transpilation, and preview page navigation always use the canonical pages-based **Arcade project source** model.
- The editor now keeps **Show pages** / **Hide pages**, **Global config**, page navigation targets, stale-page-reference analysis, delete impact counts, and page-level error indicators available even when restored working-copy preferences still carry `multiPageEnabled: false`.
- The preview now always transpiles `project.source` through the pages-aware runtime and always drives the sandbox with `activePageId`, so one-page projects and multi-page projects share the same preview contract and page-scoped diagnostics stay attributed to stable page ids.
- Updated regression coverage in `tests/unit/services/projectSource.test.ts`, `tests/integration/multi-page-page-panel.test.tsx`, `tests/integration/multi-page-preview-sync.test.tsx`, `tests/integration/preview-fullscreen-entry.test.tsx`, and `tests/integration/project-controls-layout.test.tsx` to cover legacy `multiPageEnabled: false` restores plus the page-scoped compile diagnostics emitted from the always-on runtime.

Verification:

- `npm run typecheck`
- `npm run test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:4195/aksel-arcade/`: on a fresh working copy with the legacy flag still off, confirmed **Show pages** is visible, opened the **Page panel**, selected **Global config** and saw the no-preview placeholder while the sandbox stayed mounted, added **Page 2**, switched to it, and saw no browser console warnings/errors during the verified flow.

Next note:

- `#254` can now remove the obsolete Settings checkbox and stop writing `multiPageEnabled` without preserving any editor/preview fallback path; legacy false values already behave like always-on page projects.

### 2026-06-15 - Issue #254: Permanent pages Settings and workspace-preference cleanup

Implemented:

- Selected `#254` as the highest-priority `ready-for-agent` feature from `#254`-`#259` because it is the only unblocked item in the set and it removes the last user-facing experiment chrome that the later portability and agent issues depend on.
- Read issue `#252`, `CONTEXT.md`, and the existing `progress.txt` notes before changing code.
- Removed the obsolete **Multiple pages** checkbox plus the empty **Experiments** section/divider from `src/components/Header/AppHeader.tsx`, leaving the Settings menu with Theme, Templates, and Editor only.
- Stopped persisting `multiPageEnabled` in new Web Arcade working-copy saves by introducing a persisted-preferences shape in `src/services/storage.ts` that still accepts legacy stored values on read but strips the obsolete flag on write.
- Updated regression coverage in `tests/unit/services/storage.test.ts`, `tests/integration/share-decode.test.tsx`, and `tests/integration/project-controls-layout.test.tsx` so tests now assert the new persisted-preference shape, the removed Settings experiment UI, and the remaining recipient-local page-panel behavior without expecting the obsolete flag to round-trip.

Verification:

- `npm run typecheck`
- `npm run test -- --run`
- Browser check at `http://127.0.0.1:4173/aksel-arcade/` confirming the Settings menu no longer shows **Experiments** or **Multiple pages**, and the page loaded without console errors

Next note:

- `multiPageEnabled` still exists as an internal compatibility input for the later agent/runtime rollout, but new working-copy storage no longer writes it. `#255` / `#258` should remove the remaining internal reads instead of reintroducing persisted preference behavior.

### 2026-06-15 - PR #262 review follow-up for Issue #254

Fixed after PR review:

- The first review pass found that removing the Settings checkbox left Desktop agent sessions stranded in the old single-page bridge mode, and that reloading legacy working copies after autosave would silently fall back to `multiPageEnabled: false` because new saves omit the field.
- Changed `src/contexts/SettingsContext.tsx` and `src/services/storage.ts` so permanent pages restore as always-on behavior: fresh defaults now treat `multiPageEnabled` as enabled, legacy stored boolean values are tolerated but ignored on restore, and new saves still omit the obsolete field.
- Updated `tests/integration/project-controls-layout.test.tsx` so the default Desktop agent-session path is pages-aware without any harness-only enable step, command-list assertions include page lifecycle commands, and the stale transport-method assertion no longer hardcodes the pre-pages-aware method list.
- Added/updated regression coverage in `tests/unit/services/storage.test.ts` and `tests/integration/share-decode.test.tsx` to prove old `multiPageEnabled: false` working copies reload as permanent pages-enabled sessions while page-panel workspace state remains local.

Verification:

- `npm run typecheck`
- `npm run test -- --run`
- Browser check at `http://127.0.0.1:4173/aksel-arcade/` confirming the Settings menu no longer shows **Experiments** / **Multiple pages**, the editor still shows a neutral **Show pages** control, and the browser console stayed free of errors

Next note:

- The old `multiPageEnabled` state is now compatibility-only in practice. Follow-up work can delete the remaining internal single-page bridge branch once the dedicated agent-session cleanup issue lands, without needing to preserve legacy stored false values.

### 2026-06-15 - Issue #256: Lossless `.akselarcade` packages for full Arcade source

Implemented:

- Selected `#256` as the highest-priority `ready-for-agent` feature from `#256`-`#259` because `#259` is blocked on the package/share/agent groundwork, `#258` is narrower Desktop-agent follow-up work, and `#256` establishes the portable full-project package boundary needed before the Web-share fallback in `#257`.
- Read issue `#252`, `CONTEXT.md`, and the existing `progress.txt` notes before changing code.
- Updated `src/services/storage.ts` so `.akselarcade` exports now carry the full canonical `project.source` (`globalConfig`, all pages, `startPageId`, `nextPageNumber`) in clean package format `v3`, while imports still mint a fresh local project identity and keep backward compatibility for legacy `v2` single-page packages and `v1` portable packages.
- Kept raw legacy saved-project JSON rejected on import, narrowed the lossy warning surface to Web share URLs only, and removed the misleading package-export confirmation path because package export is now lossless for the full Arcade project source.
- Updated the header/share UI and regression coverage in `src/components/Header/AppHeader.tsx`, `src/components/Share/SharePopoverButton.tsx`, `tests/unit/services/storage.test.ts`, `tests/integration/share-popover.test.tsx`, `tests/integration/fullscreen-share-popover.test.tsx`, and `tests/integration/project-controls-layout.test.tsx` so package export/import, Desktop export fallback, and imported working-copy defaults all match the new full-source contract.
- Cleaned up two unrelated regressions discovered during validation: restored the missing `FIRST_PAGE_ID` import in the legacy storage migration path and moved the `InspectionPopover` position helper into the layout effect so the repo lint pass is clean again.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
- Browser check at `http://127.0.0.1:5190/aksel-arcade/` confirming a two-page `.akselarcade` export captured `startPageId: "page03"` with both pages present, importing that package reset the working copy to the Start page on the **JSX** tab with the page panel closed, and a clean reload completed without console errors

Next note:

- `#257` still owns the lossy Web share fallback. Keep `MULTI_PAGE_WEB_SHARE_WARNING` scoped to share URLs until that path can preserve full project source; package export should stay direct and warning-free.
- Future package work should keep the `v3` clean-package boundary strict: no working-copy preferences, local identity, or Desktop/agent session artifacts belong in `.akselarcade` payloads.

### 2026-06-15 - PR #263 review follow-up for Issue #256

Fixed after code review:

- The PR review found that real historical `formatVersion: 1` `.akselarcade` files were still rejected because the new parser enforced the strict v3 root shape before it branched to the legacy portable-package path.
- Updated `src/services/storage.ts` so genuine v1 package envelopes can carry their historical `exportedAt` and optional `meta` root fields without weakening the strict clean-package boundary for current v3 exports or raw legacy JSON working copies.
- Updated `tests/unit/services/storage.test.ts` so the v1 import regression uses the historical root package shape instead of a synthetic minimal envelope.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run test -- --run`

Next note:

- Keep the parser branch order intentional: v1 compatibility should be handled before the strict clean-package root-key assertion, while v3 and raw JSON rejections should continue to fail closed on extra top-level fields.

### 2026-06-15 - Issue #257: Make Web share URLs lossless for full Arcade project source

Implemented:

- Selected `#257` as the highest-priority `ready-for-agent` feature from `#257`-`#259` because `#259` is explicitly blocked by `#257` and `#258`, and `#257` is the broader user-facing portability dependency while `#258` is the narrower Desktop Agent follow-up.
- Read issue `#252`, `CONTEXT.md`, and the existing `progress.txt` notes before changing code.
- Reworked the Web share payload path in `src/utils/shareEncoding.ts`, `src/utils/sharePayload.ts`, `src/utils/shareDecoding.ts`, `src/hooks/useShareLink.ts`, and `src/hooks/useProject.tsx` so newly generated links now use full-project share formats `v5` / `v6`, carrying the shared project name, full clean `project.source`, Start page metadata, preview viewport, and preview theme while preserving explicit opening intents like preview fullscreen.
- Reused the clean portable project boundary from package export/import by promoting shared helpers in `src/services/storage.ts`, so Web share decode now rebuilds a fresh local project identity from the same strict lossless source contract instead of collapsing back to a single-page snapshot.
- Removed the lossy multi-page share warning from `src/components/Share/SharePopoverButton.tsx`, updated the share-regeneration fingerprint to follow whole-project changes, and kept the oversize Web-share fallback directing users to Export when the URL guardrail is exceeded.
- Refreshed regression coverage in `tests/unit/utils/shareEncoding.test.ts`, `tests/unit/utils/shareDecoding.test.ts`, `tests/integration/share-decode.test.tsx`, `tests/integration/share-popover.test.tsx`, and `tests/integration/fullscreen-share-popover.test.tsx` so the suite now asserts full-source share payloads, recipient-side Start-page loading, shared project-name hydration, page-panel preference preservation, fullscreen opening intent, and backward-compatible decoding of older single-page Web share URLs.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- `npm run build`
- Browser check at `http://127.0.0.1:5192/aksel-arcade/` confirming a two-page shared working copy generated a Web share URL with no lossy warning, and loading that URL in a fresh tab preserved the recipient-local open Page panel while restoring the shared project name, both pages, global config, Start page on the **JSX** tab, and a clean console

Next note:

- `#258` is now the remaining pages-awareness rollout slice for Desktop Agent sessions; it can build on the new full-project Web share/package portability boundary without reintroducing single-page fallback assumptions.
- `#259` should now keep treating Web share compatibility as covered by both the legacy single-page decode assertions and the new full-project recipient-flow regression coverage added here.

### 2026-06-15 - PR #264 review follow-up for Issue #257

Fixed after review:

- The new full-project share work left `SharePopoverButton` caching tied only to project/theme changes, so an already-open share popover could keep a stale link if the share-generation behavior changed without a project edit.
- Updated `src/components/Share/SharePopoverButton.tsx` to invalidate the last generated share fingerprint whenever the `generateShareLink` callback changes, so refreshed share options (including fullscreen opening intent) trigger regeneration instead of reusing stale output.
- Added a regression test in `tests/integration/share-popover.test.tsx` that rerenders an open share popover with `openingIntent.previewFullscreen` enabled and asserts a fresh fullscreen share payload is generated.

Verification:

- `npm run type-check`
- `npm run lint`
- `npm run test -- --run`
- Browser check at `http://127.0.0.1:5193/aksel-arcade/` confirming both the normal share popover and fullscreen share popover still generate links cleanly with no console warnings or errors

### 2026-06-15 - Issue #258: Make Desktop Agent sessions always pages-aware

Implemented:

- Selected `#258` as the highest-priority `ready-for-agent` feature from `#258`-`#259` because `#259` is explicitly blocked by `#258`, while `#253`-`#257` are already closed.
- Read issue `#252`, `CONTEXT.md`, `docs/adr/0014-pages-aware-agent-bridge.md`, and the existing `progress.txt` notes before changing code.
- Removed the remaining single-page Desktop Agent fallback in `src/services/agentBridge.ts`, `src/hooks/useAgentSession.ts`, and `src/components/Header/AgentSessionMenu.tsx` so Agent sessions always expose the full pages-based project model, always advertise page lifecycle commands, and no longer mention experimental multi-page enablement.
- Updated `tests/unit/services/agentBridge.test.ts`, `tests/unit/services/desktopAgentTransportProtocol.test.ts`, and `tests/e2e/agent-diagnostics.spec.ts` so one-page projects still read as one-page pages-based source, transport errors list the full command surface, and the desktop Agent shim expects `getProject` / `getAgentInstructions` / `getSessionState` to stay pages-aware.
- Refreshed `CONTEXT.md` and `docs/adr/0014-pages-aware-agent-bridge.md` to remove the outdated agent-side multi-page gating language and describe the permanent pages-based Agent-session contract.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run test -- --run`
- Browser check at `http://127.0.0.1:5192/aksel-arcade/` with a Desktop preload shim: started an Agent session, confirmed `getProject` returned `pageMode: "multi-page"` with `globalConfig`, `pages`, `startPageId`, and `activePageId`, confirmed `getAgentInstructions` / `getSessionState` included the lifecycle commands with no experimental fallback copy, and saw no new console errors (only the existing form-field `id` / `name` issue).

Next note:

- `#259` can build on the always-pages-aware Desktop Agent contract; the old single-page bridge/instructions path is gone, so future regression coverage should assert the full command list and page-target / global-config targeting even for one-page projects.

### 2026-06-15 - Issue #259: Add end-to-end regression coverage for permanent lossless pages

Implemented:

- Worked only on `#259`, using issue `#252`, `CONTEXT.md`, and the existing `progress.txt` notes to keep the scope on permanent-page regression coverage.
- Added `tests/e2e/permanent-pages-regression.spec.ts` to cover the permanent **Page panel** / Settings UI defaults, full-project Web share recipient behavior with the new `v5` token shape, backward-compatible legacy single-page share loading, full-source `.akselarcade` export/import, and backward-compatible legacy single-page package import.
- Updated `tests/e2e/agent-diagnostics.spec.ts` so the Desktop Agent-session E2E follows the current English Agent menu copy, keeps the runtime-error scenario pages-aware by targeting `page01`, and adds a one-page regression that exercises the full lifecycle command list plus `global-config` and page-targeted edits before deleting the added page again.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- `npm run build`
- `npm run test:e2e -- tests/e2e/permanent-pages-regression.spec.ts tests/e2e/agent-diagnostics.spec.ts`
- Browser check at `http://127.0.0.1:4196/aksel-arcade/` confirming the default Web surface rendered with the **Page panel** closed, a visible neutral **Show pages** control, a working preview, and no browser console warnings/errors

Next note:

- If later rollout work needs deeper Web-share token contract assertions inside Playwright, prefer helper code that can run in the Node-side test process directly rather than importing browser-bundled share modules from the test runner.

### 2026-06-16 - Issue #270: Fixed Desktop Arcade MCP endpoint and Settings configuration

Implemented:

- Selected `#270` as the highest-priority `ready-for-agent` feature from `#270`-`#280` because `#271` is explicitly blocked by `#270`, making this the MCP foundation slice the later issues depend on.
- Read issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes before changing code.
- Added an Electron-owned fixed MCP server in `desktop/mcpServer.cjs` and wired `desktop/main.cjs` / `desktop/preload.cjs` so Desktop Arcade now starts an app-lifetime localhost endpoint at `http://127.0.0.1:3846/mcp`, reports `Available` vs `Unavailable: <reason>`, and never falls back to another port.
- Added `src/services/desktopMcp.ts` and threaded `desktopMcpServerState` through `src/main.tsx`, `src/App.tsx`, and `src/components/Header/AppHeader.tsx` so the Desktop Settings ActionMenu now shows a dedicated **Desktop Arcade MCP** section with the fixed server name, transport, URL, no-token copy, and a last-activity placeholder while keeping this slice focused on the new configuration path.
- Added focused regression coverage in `tests/unit/services/desktopMcpServer.test.ts`, `tests/integration/desktop-mcp-settings.test.tsx`, updated `tests/integration/project-controls-layout.test.tsx` for the extra Settings section, and updated `tests/unit/desktopMain.test.ts` for the new Electron main dependency.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `CI=1 npm run test`
- Browser check at `http://127.0.0.1:4196/aksel-arcade/` with a mocked desktop preload confirming Settings shows **Desktop Arcade MCP**, `Status: Available`, `desktop-arcade`, `HTTP (MCP Streamable HTTP)`, `http://127.0.0.1:3846/mcp`, `No token/header required.`, `Last activity: No MCP activity yet.`, and no console errors

Next note:

- `#271` should build directly on `desktop/mcpServer.cjs` and the `getDesktopMcpServerState` preload bridge instead of reintroducing another discovery/config surface; keep the fixed endpoint and Settings values authoritative while the MCP protocol surface is filled in.
- The old Desktop Agent loopback/pairing UI still exists for now; `#279` is the cleanup slice that should remove the superseded user-facing model once the MCP discovery/resources/tools flow is in place.

### 2026-06-16 - PR #281 review follow-up for Issue #270

Fixed after review:

- Kept the fixed MCP endpoint slice on `#270`, but closed the biggest correctness gap in `desktop/mcpServer.cjs` by teaching the endpoint to accept a minimal MCP `initialize` request and return JSON-RPC `method not implemented` errors for later calls instead of failing every request with HTTP 501.
- Tightened `src/components/Header/DesktopMcpSettingsGroup.tsx` so an unavailable fixed port no longer exposes or copies Arcade’s MCP URL/auth copy while another local process owns `127.0.0.1:3846`; the menu now shows the unavailable reason and a clear hidden-details note instead.
- Updated `tests/unit/services/desktopMcpServer.test.ts` and `tests/integration/desktop-mcp-settings.test.tsx` to cover the new initialize handshake and the hidden-details unavailable state.

Verification:

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `CI=1 npm run test`
- Browser check at `http://127.0.0.1:4196/aksel-arcade/` with mocked desktop preload states confirming the Settings MCP section still shows fixed details when available, hides them when the port is unavailable, and logs no console errors in either state

### 2026-06-16 - Issue #271: MCP protocol foundation and safe discovery contract

Implemented:

- Selected #271 as the highest-priority open `ready-for-agent` feature from #271-#280 after checking issue #269, `CONTEXT.md`, and the existing `progress.txt` notes; it is the MCP foundation that #272-#280 build on, and #270 was already closed.
- Expanded `desktop/mcpServer.cjs` from initialize-only handling into a strict v1 MCP foundation: POST-only JSON-RPC lifecycle/discovery, explicit `initialize` capabilities for `tools` and `resources`, `tools/list`, `resources/list`, `tools/call`, and `resources/read`.
- Kept the discovery surface intentionally narrow: `tools/list` now exposes only `capture_preview_evidence` and `apply_changes`, while `resources/list` exposes only the six agreed stable direct `arcade://desktop/*` and `arcade://project/*` resources.
- Rejected any request carrying a browser `Origin` header, kept origin-less local MCP requests working, and returned a clear 405 for GET/SSE-style entry attempts instead of leaving the endpoint hanging.
- Added strict MCP validation around the protocol layer and placeholder business layer: top-level JSON-RPC envelopes reject unknown fields, `tools/call` and `resources/read` reject unsupported params/argument fields, unknown tool names fail as JSON-RPC invalid-params errors, known tools return structured `isError: true` placeholder results, and known/unknown resources fail with clear JSON-RPC resource errors until #272/#273/#274/#275 implement real behavior.
- Deliberately left `resources/templates/list` and prompts/subscriptions out of the v1 surface for now so later slices can own the dynamic source/capture URI shapes instead of the foundation overcommitting early.
- Replaced the old minimal MCP server unit coverage with focused discovery/protocol tests in `tests/unit/services/desktopMcpServer.test.ts` for initialize capabilities, tools/resources discovery, initialized notifications, known-vs-unknown tool/resource behavior, strict schema rejection, browser-origin rejection, GET/SSE rejection, malformed JSON-RPC envelopes, and the fixed-port availability error path.

Verification:

- npm run typecheck
- npm run lint (existing `.eslintignore` deprecation notice only)
- CI=1 npm run test -- --run
- npm run build
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` confirmed the app still renders and the console stayed free of runtime error/warn messages during the check
- Node MCP smoke against `createDesktopMcpServer({ port: 0 })` confirmed `initialize` advertises only `tools`/`resources`, `tools/list` returns `capture_preview_evidence` + `apply_changes`, and `resources/list` returns only the six stable direct resource URIs

Next note:

- #272 should fill in `resources/read` for project resources through narrow main→renderer IPC and keep the existing URI list stable. #273 should fill in the three Desktop resources and the machine-readable capabilities resource. Keep `resources/templates/list` absent until the later slices are ready to publish the real dynamic source/capture URI templates.

### 2026-06-16 - PR #282 review follow-up for issue #271

Fixed before merge:

- PR review found a meaningful test gap in the MCP foundation coverage: the placeholder `tools/call` path was asserted for `capture_preview_evidence`, but not for the second declared tool, `apply_changes`.
- Added a regression in `tests/unit/services/desktopMcpServer.test.ts` that calls `apply_changes` with a schema-valid operation batch and asserts the same structured `isError: true` placeholder contract used by the other known tool.

Verification:

- npm run typecheck
- CI=1 npm run test -- --run

### 2026-06-16 - PR #283 review follow-up for Issue #272

Fixed before merge:

- Kept the delayed visible compile-error UX in `PreviewPane`, but stopped Desktop MCP/agent diagnostics from missing active compile failures while the code editor stays focused by carrying a separate `pendingCompileError` through preview state and teaching `collectPreviewDiagnostics()` to surface it as an error immediately.
- Cleared stale error state in both directions: a newer runtime error now drops any queued compile error before blur, and a newer focused compile error now clears any stale runtime overlay from the last successful preview.
- Added regression coverage for that focused-editing path in `tests/integration/multi-page-preview-sync.test.tsx` plus a unit guard in `tests/unit/services/previewDiagnostics.test.ts`, so diagnostics stay accurate even when the visible overlay is still intentionally suppressed.
- Replaced compile-error source inference-by-message with explicit source metadata: `CompileError` now carries `resourceUri`, project transpilation attaches that URI for global config, page JSX/Hooks, unsupported imports, and mapped Babel errors, and Desktop MCP diagnostics prefer the explicit URI before falling back to the old label parser.
- Extracted shared Desktop MCP project source URI helpers into `src/services/desktopMcpProjectSourceUris.ts`, reused them from both the transpiler and MCP resource builder, and added regression assertions in `tests/integration/multi-page-preview-runtime.test.tsx` plus `tests/unit/services/desktopMcpProjectResources.test.ts` so page/global compile diagnostics keep stable source links even when error messages are unlabeled.

Verification:

- npm run type-check
- npm run lint
- CI=1 npm run test
- npm run build
- Browser/console smoke check at `http://127.0.0.1:4173/aksel-arcade/` using `npm run preview -- --host 127.0.0.1 --port 4173`, confirming the app shell and preview render and the console stays free of errors/warnings

### 2026-06-16 - Issue #273: Serve compact Desktop MCP guide and capabilities resources

Implemented:

- Selected `#273` as the single highest-priority actionable `ready-for-agent` feature from `#273`-`#280` after checking parent PRD `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#275` is explicitly blocked by `#273`, while `#279` and `#280` depend on later MCP slices, so the Desktop guidance-resource contract was the next unblocked foundation.
- Replaced the `not-yet-implemented` Desktop-resource path in `desktop/mcpServer.cjs` for `arcade://desktop/operating-guide`, `arcade://desktop/authoring-guide`, and `arcade://desktop/capabilities`; `resources/read` now returns real Markdown/JSON contents while keeping the existing unknown-resource and invalid-param error paths unchanged.
- Added a concise operating guide that tells external agents to work through `arcade://` resources plus `apply_changes`, follow the default manifest/source/diagnostics/evidence loop, avoid repository/package/filesystem edits, and treat `select_active_page` as human-facing coordination rather than the normal inspection path.
- Added a separate authoring guide that keeps the authoring contract short and durable: import-free Arcade JSX/Hooks, Aksel-valid Arcade JSX first, `Global config` as shared non-page code, stable page-id navigation, `{{pageRef:name}}` placeholders only inside `apply_changes` batches, and diagnostics/Preview evidence as the feedback loop after source changes.
- Added a compact machine-readable capabilities resource generated from the MCP server constants so the published contract stays in sync with the real endpoint/tool/resource surface. It now exposes the fixed Desktop endpoint, auth model, tool names, stable resource URIs, source/capture URI templates, capture-layer purpose summaries, screenshot scopes, interaction actions, request-size limit, and the v1 omission list.
- Extended `tests/unit/services/desktopMcpServer.test.ts` to cover successful reads for all three Desktop resources, Markdown/JSON MIME types, guide-content expectations, capabilities JSON parsing/shape, and drift guards that compare the capabilities tool/resource lists against the live `tools/list` and `resources/list` surfaces.

Verification:

- `npm run typecheck`
- `npm run lint` (passes with the existing `.eslintignore` deprecation notice)
- `CI=1 npm run test`
- `npm run build`
- Browser/console smoke check at `http://127.0.0.1:4173/aksel-arcade/` using `npm run preview -- --host 127.0.0.1 --port 4173` plus a headless Playwright check confirmed the app title/body render and the browser console stays free of errors/warnings

Next note:

- `#275` can now build on the real Desktop guidance/capabilities resources instead of placeholder Desktop reads, but it still needs to implement the actual `apply_changes` execution path. Keep the capabilities JSON generated from the MCP server constants so later tool/resource additions do not drift from the published contract.

### 2026-06-16 - PR #284 review follow-up for Issue #273

Fixed before merge:

- PR review found that `arcade://desktop/capabilities` published the full later-slice Preview evidence contract without saying those tools/resources still return placeholders in the current build. Kept the full contract visible as required by `#273`, but added machine-readable `implementationStatus` metadata so MCP clients can distinguish the published v1 contract from the parts that are still `not-yet-implemented`.
- PR review also found that the operating guide prescribed `apply_changes` / `capture_preview_evidence` as if they were already executable. Kept the required default loop, but added an explicit read-only guard: if either tool still returns `not-yet-implemented`, the client should stop after discovery instead of falling back to repository or filesystem edits.
- Extended `tests/unit/services/desktopMcpServer.test.ts` to assert the new read-only guard text plus the per-tool, per-layer, per-scope, per-interaction, and per-preview-resource implementation-status fields in the capabilities JSON.

Verification:

- `npm run typecheck`
- `npm run lint` (passes with the existing `.eslintignore` deprecation notice)
- `CI=1 npm run test`
- Direct MCP probe against a local ephemeral `createDesktopMcpServer({ port: 0 })` instance confirmed:
  - the capabilities resource returns `implementationStatus.toolExecution.apply_changes = "not-yet-implemented"` and the matching layer/template statuses
  - the operating guide now tells clients to treat the build as read-only while those tools are still placeholders

### 2026-06-16 - Issue #275: Desktop MCP apply_changes for existing-project edits

Implemented:

- Selected `#275` as the single highest-priority `ready-for-agent` feature from `#274`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#273` had already finished the read-resource slice, and `#275` was the next unblocked mutation path that later page-lifecycle work depends on.
- Added `src/services/desktopMcpApplyChanges.ts` plus the shared protocol/adapter helpers so Desktop MCP now supports the existing-project `apply_changes` subset for `replace_source`, `set_preview_context`, and `rename_project`.
- Kept the mutation flow on the same narrow main -> preload -> renderer bridge as project-resource reads: `desktop/main.cjs`, `desktop/preload.cjs`, `desktop/mcpServer.cjs`, `src/services/shellCapabilities.ts`, and `src/hooks/useDesktopMcpProjectResourceBridge.ts` now route validated apply requests, surface business failures as MCP tool errors, and keep immediate read-after-write consistency for follow-up `resources/read` calls.
- Prevalidated the whole operation batch before mutating React state, including stale `expectedProjectRevision` checks, source-target URI validation, project-name validation, and the existing 5 MB project-size guard.
- Reused shared `arcade://project/source/...` URI parsing from `src/services/desktopMcpProjectSourceUris.ts` so `replace_source` only accepts real manifest-backed source resources.
- Updated Desktop MCP capabilities/guide text to report `apply_changes` as available when an active project writer is connected while leaving `capture_preview_evidence` on the explicit `not-yet-implemented` path.
- Added regression coverage in `tests/unit/services/desktopMcpApplyChanges.test.ts`, `tests/unit/services/desktopMcpApplyChangesAdapter.test.ts`, `tests/unit/services/desktopMcpServer.test.ts`, and `tests/integration/desktop-mcp-settings.test.tsx` for success/failure semantics, adapter registration, server routing, and safe last-activity rendering.
- Fixed two build-only follow-ups before handoff: imported `isArcadePageId` into `src/services/desktopMcpProjectResources.ts` after the shared URI-parser refactor, and updated the typed Vitest mock signature in `tests/unit/services/desktopMcpServer.test.ts` so `tsc -b` passes.

Verification:

- npm install
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run build
- Browser/console smoke check at `http://127.0.0.1:4173/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 4173`, confirming the app shell and live preview render and the console stays free of warnings/errors in the clean flow

Next note:

- `#277` should build on this `apply_changes` path for page-lifecycle mutations instead of introducing a second writer bridge; the current existing-project mutation surface already has revision checks, source-resource targeting, and immediate read-after-write behavior.
- `capture_preview_evidence` is still intentionally unimplemented. Keep the next MCP slices on the manifest/source/diagnostics/apply loop unless that issue is the one being worked.
- Desktop MCP Settings last-activity is currently renderer-managed UI state. If a later issue needs persistence or parity from the main-process server state, that should be a separate follow-up rather than folded into `#275`.

### 2026-06-16 - PR #285 review follow-up for issue #275

Fixed after review:

- Made `apply_changes` persist the full working copy synchronously before returning success, so MCP mutations now fail fast instead of reporting success and relying on delayed autosave. Persistence failures surface as `persistence-failed`, while full working-copy size overflows keep the existing `payload-too-large` contract.
- Added focused hook coverage in `tests/unit/hooks/useDesktopMcpProjectResourceBridge.test.tsx` so persistence failures do not mutate renderer state and the full-envelope size guard still maps to `payload-too-large`.
- Persisted Desktop MCP `lastActivity` in the main-process/server state so Settings survives renderer reloads, while keeping the immediate renderer-side live update for the current session.
- Tightened the renderer bridge to refresh its project/theme/diagnostics/preferences refs in `useLayoutEffect`, closing the post-render race where incoming MCP requests could read stale state or stale working-copy preferences.
- Removed two MCP guide-contract mismatches: the operating guide no longer references the nonexistent `select_active_page` tool, and the authoring guide no longer documents the unsupported `{{pageRef:name}}` placeholder workflow.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run build
- Browser/console smoke check at `http://127.0.0.1:4174/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 4174`, confirming the app shell and live preview render and the console stays free of warnings/errors in the clean flow

### 2026-06-16 - Issue #274 follow-up: fix Desktop MCP hidden Preview capture timeout

Implemented:

- Investigated the reported `capture_preview_evidence({})` failure with a real Electron + MCP regression at `tests/e2e/desktop-mcp-preview-capture.spec.ts` that launches Desktop Arcade, waits for the default Preview, and calls the live MCP HTTP endpoint.
- Confirmed the first misleading local symptom was a stale Electron process still bound to port `3846`; after clearing that, the real bug reproduced inside the hidden isolated capture sandbox.
- Narrowed the timeout to the hidden iframe capture path in `public/sandbox.html`: the isolated render reached `RENDER_SUCCESS`, but the follow-up capture never completed because the offscreen iframe was waiting on nested `requestAnimationFrame` callbacks before collecting evidence.
- Replaced that hidden-sandbox capture scheduling with timer-based settling (`setTimeout`-driven retries / double-settle) so Desktop Electron does not stall the capture step for an offscreen iframe before posting `PREVIEW_EVIDENCE_CAPTURED`.
- Removed the temporary `[DEBUG-*]` route/capture instrumentation after confirming the fix, and kept the new Electron Playwright regression as the permanent seam for this exact Desktop MCP flow.

Verification:

- npm run type-check
- CI=1 npm test -- --run
- npm run lint
- npm run build
- npx playwright test tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line

Next note:

- If a future follow-up touches isolated Preview capture timing, keep the regression at `tests/e2e/desktop-mcp-preview-capture.spec.ts` green and prefer timer-based settling over `requestAnimationFrame` inside the offscreen sandbox unless Electron behavior is revalidated.
### 2026-06-16 - Issue #274 follow-up: stabilize default Desktop MCP Preview capture

Implemented:

- Fixed the remaining default `capture_preview_evidence({})` regressions reported after the initial #274 delivery.
- Threaded the host-requested hidden-preview viewport dimensions into the sandbox capture payload and used them as a fallback in `src/services/previewEvidence.ts`, so frame metadata and SVG screenshots no longer collapse to `0x0` / `1x1` when Electron reports zero inner-window metrics in the hidden iframe path.
- Removed the extra post-render timer chain inside `public/sandbox.html` capture execution and now capture immediately once the expected page is present; this eliminated the flaky hidden-iframe stall where fresh default captures could time out after render success but before any capture result returned.
- Tightened the Desktop MCP Playwright regression to cover repeated default captures across multiple fresh Electron launches, so the suite now protects against both timeout flakes and zero-sized output.

Verification:

- npm run type-check
- CI=1 npm test -- --run
- npm run lint
- npm run build
- npx playwright test tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- Direct Electron + MCP JSON-RPC stress loop over repeated fresh launches confirmed default captures now return nonzero frame viewport metadata and matching screenshot dimensions consistently

Next note:

- The current default project still exposes only `page01`, so the exact "capture a non-active page without changing the visible active page" verifier step remains non-executable until a multi-page default fixture exists; keep treating that as fixture coverage, not a regression in the capture pipeline.

### 2026-06-16 - PR #286 review follow-up for issue #274

Fixed after review:

- Screenshot evidence now preserves the actual Preview canvas backdrop instead of inheriting the transparent sandbox body. `src/services/previewEvidence.ts` resolves the capture background from the real document canvas (`body`, then `documentElement`) so Desktop MCP screenshots match the themed Preview surface.
- Region capture targets that rely on `text`, `name`, or `label` now choose the most specific matching element instead of stopping at an ancestor like `#root` whose descendant text happens to match. This keeps targeted region crops aligned with the intended control.
- Added focused unit coverage for both review fixes in `tests/unit/services/previewEvidence.test.ts`, including the transparent-body/html-background capture path and text-based region targeting resolving to the button-sized crop.

Verification:

- npm run type-check
- CI=1 npm test -- --run
- npm run lint
- npm run build
- npx playwright test tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line

Next note:

- The committed `public/preview-evidence-global.js` bundle must stay in sync with `src/services/previewEvidence.ts` whenever Preview evidence serialization changes; the hidden Desktop capture sandbox executes the generated browser bundle, not the TypeScript source directly.

### 2026-06-16 - Issue #277: Extend apply_changes with page lifecycle batches and temp refs

Implemented:

- Selected `#277` as the single highest-priority `ready-for-agent` feature from `#276`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#275` and `#274` were already closed, `#276` and `#277` were the first unblocked slices, and `#277` won because it completes the multi-page mutation path and directly unlocks `#278` plus the later MCP hardening/smoke issues.
- Extended the Desktop MCP `apply_changes` contract in `src/services/desktopMcpApplyChangesProtocol.ts` and `desktop/mcpServer.cjs` to support `create_page`, `rename_page`, `delete_page`, `set_start_page`, and `select_active_page`, along with `newPageRef`, `tempPageRef`, and `{{pageRef:name}}` placeholder documentation.
- Reworked `src/services/desktopMcpApplyChanges.ts` to plan app-assigned page ids before mutation, rewrite temp page-ref placeholders into permanent ids, apply page lifecycle operations atomically on top of the existing `#275` path, and reject invalid batches for unresolved placeholders, out-of-order temp refs, zero-page final states, and missing Start/Active replacements.
- Kept the MCP writer bridge narrow by reusing the existing main → preload → renderer `apply_changes` route; `desktop/main.cjs` and `desktop/mcpServer.cjs` now accept the new structured success/failure shape without introducing a second mutation channel.
- Updated the renderer bridge in `src/hooks/useDesktopMcpProjectResourceBridge.ts`, `src/hooks/useProject.tsx`, and `src/App.tsx` so successful MCP batches replace the in-memory Arcade project state in one shot, keep immediate read-after-write consistency, and still drive the normal preview/diagnostics refresh path.
- Added focused regression coverage in `tests/unit/services/desktopMcpApplyChanges.test.ts`, `tests/unit/services/desktopMcpServer.test.ts`, and `tests/unit/hooks/useDesktopMcpProjectResourceBridge.test.tsx`, plus a new end-to-end Electron regression at `tests/e2e/desktop-mcp-apply-changes.spec.ts` that exercises the real Desktop MCP endpoint through one create/link/select batch.

Verification:

- npm run typecheck
- CI=1 npm run test -- --run
- npm run lint
- npm run test:e2e -- --project=chromium tests/e2e/desktop-mcp-apply-changes.spec.ts --reporter=line
- Browser/console smoke check at `http://127.0.0.1:4176/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 4176 --strictPort`, confirming the app shell and preview render cleanly with no browser-console warnings/errors

Next note:

- `#278` should reuse the new `#277` temp-ref workflow exactly as-is: agents can now create/link pages durably through `apply_changes`, so Preview interaction work should keep page inspection on `capture_preview_evidence({ pageId })` and treat `select_active_page` as human-visible coordination only.

### 2026-06-16 - PR #287 review follow-up for issue #277

Fixed after review:

- Closed a temp-ref lifecycle hole in `src/services/desktopMcpApplyChanges.ts`: `{{pageRef:name}}` rewriting now verifies that an earlier temp-ref page still exists at the current batch step before substituting its permanent page id, so batches can no longer create a page, delete it, and then persist a stale page reference to that deleted page later in the same `apply_changes` request.
- Added focused regression coverage in `tests/unit/services/desktopMcpApplyChanges.test.ts` for the create -> delete -> replace_source sequence, asserting the batch fails with `invalid-operation-target` instead of writing a stale durable link.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run

### 2026-06-16 - Issue #276: Add accessibility and DOM/layout/style Preview evidence layers

Implemented:

- Selected `#276` as the single highest-priority `ready-for-agent` feature from `#276`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#279` and `#280` are still blocked, `#277` was already complete, and `#276` beat `#278` because it finishes the core Preview evidence contract that later interaction workflows depend on.
- Extended `src/services/previewEvidence.ts` so Preview capture now produces a bounded `accessibility` layer with roles, accessible names, heading levels, focusability, and selected/expanded/checked/current/pressed/disabled states alongside the existing DOM/layout/style evidence, including overflow styles and deterministic truncation metadata.
- Extended `src/services/desktopMcpPreviewCapture.ts`, `src/services/desktopMcpPreviewCaptureProtocol.ts`, `src/services/desktopMcpPreviewCaptureSandbox.ts`, `desktop/main.cjs`, and `desktop/mcpServer.cjs` so `capture_preview_evidence` defaults omitted `layers` to all available layers, publishes short-lived `accessibility` and `dom_layout_style` resources plus the manifest/frame/screenshot set, validates the expanded contract end-to-end, and updates the MCP capability/guide text to mark both new layers as available.
- Regenerated `public/preview-evidence-global.js` via `npm run build:sandbox` so the hidden Desktop Preview capture sandbox stays in sync with the TypeScript evidence serializer.
- Added focused regression coverage in `tests/unit/services/previewEvidence.test.ts`, new `tests/unit/services/desktopMcpPreviewCapture.test.ts`, updated `tests/unit/services/desktopMcpServer.test.ts`, and extended `tests/e2e/desktop-mcp-preview-capture.spec.ts` so the new layers, default-all-layer behavior, manifest/resource payloads, and live Desktop MCP preview-capture flow are all covered.

Verification:

- npm run build:sandbox
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- Browser/console smoke check at `http://127.0.0.1:5173/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort`, confirming the app shell and live preview render cleanly with no browser-console warnings/errors

Next note:

- `#278` can now treat `capture_preview_evidence` as a full four-layer inspection primitive. Reuse the new `accessibility` and `dom_layout_style` resources instead of inventing parallel interaction-inspection payloads, and keep `public/preview-evidence-global.js` regenerated whenever `src/services/previewEvidence.ts` changes.

### 2026-06-16 - PR #288 follow-up: add tool-level MCP resource reads

Fixed after manual MCP verification:

- Reproduced a host-compatibility gap after `#276`: `capture_preview_evidence` succeeded and returned `arcade://preview/captures/...` URIs, but some agent-facing MCP hosts expose only `tools/call` and not raw `resources/read`, so the returned evidence resources could not actually be inspected from that environment.
- Added a new Desktop Arcade MCP tool, `read_resource({ uri })`, in `desktop/mcpServer.cjs`. It reuses the same underlying resource-read path as raw `resources/read`, so tool-only hosts can now fetch Preview capture resources, project resources, and stable desktop resources without widening the surface beyond existing `arcade://...` contracts.
- Kept the existing raw `resources/read` behavior unchanged while centralizing the shared read logic, updated capability/operating-guide output to advertise the compatibility bridge, and added unit coverage that exercises the new tool against Preview capture resources.
- Updated `tests/e2e/desktop-mcp-preview-capture.spec.ts` to verify the real Electron/Desktop MCP flow through `capture_preview_evidence` plus `read_resource`, which matches the failing agent-facing usage more closely than direct `resources/read`.

Verification:

- CI=1 npm run test -- --run tests/unit/services/desktopMcpServer.test.ts
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run

Next note:

- Keep `read_resource` as the compatibility path for MCP hosts that cannot issue raw `resources/read` requests. If a later issue changes Preview capture manifests or adds new `arcade://preview/captures/{captureId}/*` resources, make sure both the raw resource path and the tool-only bridge stay in sync.

### 2026-06-16 - PR #288 follow-up: sanitize accessibility fallback names

Fixed after external retest:

- Reproduced the remaining `#276` gap with a focused unit regression: Preview accessibility serialization was deriving fallback names from raw subtree `textContent`, so a `<main>` landmark could pick up text from excluded descendants like `<script>`, `<style>`, and `<template>`.
- Tightened `src/services/previewEvidence.ts` so fallback accessibility names now use sanitized subtree text and only apply to roles/elements that actually take names from their contents. Landmark/container nodes such as `main` no longer inherit noisy descendant text, while headings, buttons, links, and similar content-named controls still keep their expected names.
- Reused the sanitized text helper for `aria-labelledby`, associated `<label>` text, and other accessibility-name text paths so excluded nodes do not leak back in through referenced labels.
- Regenerated `public/preview-evidence-global.js` so the hidden Desktop Preview capture sandbox uses the same sanitization logic as the TypeScript source, and added a regression in `tests/unit/services/previewEvidence.test.ts` that proves script/style/template text is absent from accessibility output.

Verification:

- CI=1 npm run test -- --run tests/unit/services/previewEvidence.test.ts tests/unit/services/desktopMcpPreviewCapture.test.ts tests/unit/services/desktopMcpServer.test.ts
- npm run build:sandbox
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- Browser/console smoke check at `http://127.0.0.1:5173/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort`, confirming the app shell and live preview render cleanly with no browser-console warnings/errors

Next note:

- If later Preview evidence work expands accessible-name heuristics, keep the current distinction between explicit names and content-derived names. Excluded descendant text must stay out of both the DOM/layout/style layer and the accessibility layer, and `public/preview-evidence-global.js` must be regenerated whenever `src/services/previewEvidence.ts` changes.

### 2026-06-16 - PR #288 review follow-up: align accessible names with browser behavior

Fixed during merge review:

- Confirmed a remaining PR-scoped accuracy gap in the new accessibility layer: fallback name extraction still diverged from browser behavior for three common cases — `<img alt>`, text-like `<input value>`, and content names polluted by `aria-hidden` descendants.
- Tightened `src/services/previewEvidence.ts` so image/image-input `alt` text is used as the accessible name, text-like inputs no longer use their current `value` as a fake name, button-like inputs still keep value-backed names where appropriate, and sanitized subtree text now skips `aria-hidden` / `hidden` descendants as well as excluded nodes.
- Reused the same helper path for label text, `aria-labelledby`, and content-derived names, so the accessibility layer and `target.name` capture matching stay aligned with each other instead of drifting across separate heuristics.
- Added focused regression coverage in `tests/unit/services/previewEvidence.test.ts` proving `<img alt="Hero art">` is named, `<input type="text" value="xyz">` stays unnamed, and `<button>Run<span aria-hidden="true">hidden</span></button>` captures as `Run` instead of `Run hidden`.
- Regenerated `public/preview-evidence-global.js` so the checked-in hidden Preview sandbox bundle matches the updated serializer logic.

Verification:

- CI=1 npm run test -- --run tests/unit/services/previewEvidence.test.ts
- npm run build:sandbox
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- Browser/console smoke check at `http://127.0.0.1:5173/aksel-arcade/` via `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort`, confirming the app shell and live preview render cleanly with no browser-console warnings/errors

Next note:

- Keep accessibility-name extraction grounded in browser/AT semantics rather than raw DOM text. If future work expands roles or target matching, preserve the current split between explicit names (`aria-*`, `alt`, labels, titles), content-derived names for the roles that support them, and sanitized descendant text that excludes hidden/unsafe nodes.

### 2026-06-16 - Issue #278: Add bounded preview interaction sequences to evidence capture

Implemented:

- Selected `#278` as the single highest-priority `ready-for-agent` feature from `#278`-`#280` after checking issue `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#279` depends on `#278`, and `#280` depends on both `#278` and `#279`, so `#278` was the earliest unblocked dependency slice.
- Extended `capture_preview_evidence` end to end so Desktop MCP now accepts bounded Preview interaction sequences for `click`, `fill`, `select`, `press`, `scroll`, and `waitFor`, with step/time caps published through `desktop/mcpServer.cjs` and the Preview-capture contract types.
- Implemented interaction execution inside the fresh hidden Preview sandbox only, keeping interactions scoped to preview-root targets, blocking host-UI selectors and browser/external navigation, and preserving durable Arcade project/workspace state outside the capture.
- Recorded requested/executed interactions, failed-step details, and final transient state in the capture manifest/result path. Successful captures now surface interaction metadata without changing the human-visible active page or saved Preview preferences.
- Fixed two runtime bugs found while running the live human-testable flow: the hidden offscreen iframe could stall capture while waiting on `requestAnimationFrame`, and interactions could start before React effect-driven page-link handlers were attached after render.
- Added focused regression coverage in `tests/unit/services/previewInteractions.test.ts`, extended the Preview-capture/server unit suites, and expanded `tests/e2e/desktop-mcp-preview-capture.spec.ts` to cover click-opened UI state, filled form state, select/press/scroll/wait flows, in-prototype navigation, missing-target failures, host-UI rejection, external-navigation blocking, and non-persistence of human-visible state.
- While closing the slice, also fixed build-only typing drift surfaced by `npm run build` in the Preview-capture manifest typing, `desktopMcpApplyChanges`, and the AppContext test stubs so repo-wide validation stays green.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run build
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line
- Browser/console smoke check at `http://127.0.0.1:4173/aksel-arcade/`, confirming the app shell and live preview render cleanly and the browser console stays free of errors/warnings

Human-testable verification notes:

- All issue `#278` verification steps are testable with the seeded multi-page interaction demo project and now pass in the live Desktop MCP Electron flow.
- The live verification covers click-opened menu state across screenshot/accessibility/DOM evidence, filled-form capture, `waitFor` settling, fail-fast missing-target behavior without a misleading manifest, preserved human-visible active page and saved Preview preferences, host-UI selector rejection, and external-navigation blocking while still allowing in-prototype Arcade page navigation.

Next note:

- `#279` should build on the new interaction manifest/result metadata instead of inventing a parallel post-capture format. Keep all transient UI mutation inside the hidden isolated Preview sandbox, and regenerate `public/preview-evidence-global.js` whenever `src/services/previewEvidence.ts` changes.

### 2026-06-16 - PR #289 review follow-up for issue #278

Fixed after review:

- Closed a real interaction-safety gap in `src/services/previewEvidence.ts`: `press` interactions with activation keys (`Enter` / `Space`) could synthesize a click on external links without passing through the existing external-navigation guard, even though `click` interactions correctly rejected those targets.
- Applied the same `assertPreviewInteractionNavigationAllowed(...)` check to keyboard-triggered activation before synthesizing the click, so bounded Preview `press` interactions now preserve the same host-safety and browser-navigation boundary as direct `click` interactions.
- Added focused regression coverage in `tests/unit/services/previewInteractions.test.ts` and `tests/e2e/desktop-mcp-preview-capture.spec.ts` so external link activation is rejected consistently for both `click` and `press`.

Verification:

- npm run build
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run test:e2e -- tests/e2e/desktop-mcp-preview-capture.spec.ts --reporter=line

Next note:

- If the Desktop MCP preview-capture e2e unexpectedly talks to the wrong project state, first check for a stale local Electron process still listening on `127.0.0.1:3846`; that can make the test hit an old MCP server even when the newly launched window looks correct.

### 2026-06-16 - Issue #279: Harden MCP bridge and remove superseded Agent pairing model

Implemented:

- Selected `#279` as the single highest-priority open `ready-for-agent` feature from `#279`-`#280` after checking parent PRD `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. `#280` is explicitly blocked by `#279`, so the MCP hardening and superseded-pairing cleanup had to land first.
- Disabled the old public Desktop Agent-session path in the normal Desktop capability set by switching `agentSessions.enabled` off in both the shared renderer capability map and the Electron main-process clone. Production Desktop Arcade now exposes the MCP setup path in Settings without showing the legacy Agent access/pairing button, while the legacy transport remains available only in explicitly opted-in compatibility tests.
- Updated the public-surface regression coverage instead of deleting the old transport internals:
  - `tests/integration/desktop-mcp-settings.test.tsx` now asserts the desktop MCP configuration appears without the old Agent-session button, and Web Arcade still shows neither MCP nor Agent UI.
  - `tests/integration/project-controls-layout.test.tsx` now uses a test-only legacy capability fixture for compatibility-only bridge coverage while asserting the real Desktop capability set hides the public Agent-session UI and still shows the MCP settings surface.
  - `tests/integration/share-popover.test.tsx` keeps the compatibility-only export-cleanliness path under the same test-only legacy fixture and broadens the forbidden artifact pattern to include MCP/instruction vocabulary as well as the older Agent-session terms.
- Updated `tests/unit/services/shellCapabilities.test.ts` to lock the new Desktop default and revised `CONTEXT.md` so the old Agent session/access/transport/pairing vocabulary is clearly marked as legacy/superseded while the current `Agent bridge` / `Agent change` language reflects the local MCP model.

Human-testable verification notes:

- Directly smoke-checked in a browser against a built preview:
  - Web Arcade still shows Share, shows no Agent button, and Settings shows no Desktop MCP section.
  - A desktop-preload simulation (Electron user agent + preload bridge injected before load) shows no Share button, no Agent button, and Settings shows the MCP configuration (`desktop-arcade`, `HTTP (MCP Streamable HTTP)`, `http://127.0.0.1:3846/mcp`, no token/header) with no console errors.
- Covered by automated Electron/MCP tests in this headless session because they are not directly human-testable from a plain browser smoke pass:
  - `tests/unit/services/desktopMcpServer.test.ts` covers no-token operation, fixed endpoint behavior, browser-Origin rejection, GET/SSE rejection, and clear `project-unavailable` failures instead of auto-opening UI.
  - `tests/integration/desktop-mcp-settings.test.tsx` covers safe last-activity metadata only.
  - `tests/integration/share-popover.test.tsx` and `tests/integration/project-controls-layout.test.tsx` cover clean export/share payload boundaries so MCP/legacy activity, diagnostics, evidence, and instruction-shaped artifacts do not leak into portable project data.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run build
- Browser smoke at `http://127.0.0.1:4173/aksel-arcade/` plus a desktop-preload simulation of the same build, confirming the MCP-only desktop chrome and the Web/Desktop UI boundary without runtime console errors

Next note:

- The old loopback transport files/tests now survive only as explicit compatibility coverage. If a follow-up fully removes that path instead of keeping it as a guarded internal fallback, the test-only legacy capability fixtures in `project-controls-layout.test.tsx` and `share-popover.test.tsx` are the first places to simplify or delete.

### 2026-06-16 - Issue #279 follow-up: MCP-only verification contract

Implemented:

- Followed up on a real post-PR verification gap reported by an MCP-only tester: the MCP bridge itself was working, but the Desktop MCP operating guide/capabilities resource did not clearly tell a host-limited agent how to discover the surface or which acceptance checks were intentionally outside MCP.
- Updated `arcade://desktop/operating-guide` so MCP-only clients are explicitly told to start with `arcade://desktop/capabilities` and to use `read_resource({ uri })` when the host does not expose raw resource/discovery UI.
- Expanded `arcade://desktop/capabilities` with two new contract sections:
  - `discoveryAdvice`, which points agents at `arcade://desktop/capabilities` plus the `read_resource` fallback path.
  - `verificationBoundaries`, which separates MCP-verifiable checks (auth-free access, structured/redacted failures, origin/transport rejection, safe activity metadata) from host/UI-only checks (Desktop Settings chrome, Web/Desktop UI boundaries, share/package payload cleanliness, host logs, and closed-window lifecycle).
- Added regression coverage in `tests/unit/services/desktopMcpServer.test.ts` so the Desktop MCP self-description keeps documenting this boundary and MCP-only agents do not have to guess again.

Human-testable verification notes:

- The original tester report was valid: several issue-279 acceptance items are not directly observable from the MCP surface alone. The fix here is to make that boundary explicit and discoverable from MCP itself, not to expose Desktop/Web chrome or host logs through MCP.
- After this follow-up, an MCP-only agent can determine up front which checks it can complete autonomously and which ones must remain app-side or human-driven.

Verification:

- CI=1 npm run test -- --run tests/unit/services/desktopMcpServer.test.ts
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run build

Next note:

- If a future issue wants full autonomous verification of host/UI-only items, that is a new product capability discussion. The current contract remains intentional: MCP can describe those boundaries authoritatively, but it should not introspect Desktop chrome, host logs, or window lifecycle just to satisfy a test harness.

### 2026-06-17 - Issue #279 follow-up: execute the full verification loop and fix payload redaction

Implemented:

- Took the issue `#279` human-testable checklist literally and converted it into an executable regression loop instead of relying on a documentation-only interpretation:
  - `tests/e2e/issue-279-mcp-hardening.spec.ts` now drives the real Electron desktop app plus direct MCP HTTP calls to verify:
    - Settings shows Desktop MCP configuration instead of the old pairing path
    - no in-app Agent toggle is required before `tools/list` / `resources/list`
    - no token/header is required
    - MCP request activity shows only safe metadata in Settings
    - MCP error paths do not echo payload text in the public error surface
    - closing the Desktop Arcade window on macOS leaves the app process running but makes project calls fail clearly with `project-unavailable` instead of reopening UI
  - the same spec also checks the live Web surface for no Agent/MCP UI and no `/mcp` endpoint behavior.
- Fixed a real privacy bug found by the new loop:
  - `src/services/previewEvidence.ts` no longer interpolates caller-supplied selector/text/value/key payloads into Preview interaction failure messages.
  - `desktop/mcpServer.cjs` now redacts `capture_preview_evidence` failure interaction payloads before they become MCP tool output. Error results keep only safe step metadata (`action`, `index`, `reason`) instead of echoing the original target/value payload.
- Added focused regression coverage for the redaction fix:
  - `tests/unit/services/previewInteractions.test.ts` now proves failed interaction messages and reasons do not echo a sentinel payload.
  - `tests/unit/services/desktopMcpServer.test.ts` now proves `capture_preview_evidence` failure tool output redacts the raw interaction payload while preserving safe failure metadata.
- Regenerated `public/preview-evidence-global.js` after changing the Preview evidence runtime source.

Human-testable verification notes:

- The issue loop now passes in-session for the exact checklist the issue calls out:
  - Desktop Settings MCP configuration
  - no Agent toggle before MCP listing
  - no auth header
  - Web has no MCP/Agent UI or endpoint behavior
  - clean portable share/package artifact boundary (covered by `tests/integration/share-popover.test.tsx`)
  - safe Settings last activity
  - redacted MCP error payload/log surface
  - clear `project-unavailable` after closing the Desktop window where applicable
- Also did one live Web spot-check against `npm run dev` at `http://127.0.0.1:4173/aksel-arcade/`: Share was present, Settings had no Desktop MCP section, and the browser console stayed free of errors/warnings.

Verification:

- CI=1 npm run test -- --run tests/unit/services/previewInteractions.test.ts tests/unit/services/desktopMcpServer.test.ts tests/integration/share-popover.test.tsx
- npm run test:e2e -- tests/e2e/issue-279-mcp-hardening.spec.ts --reporter=line
- npm run typecheck
- npm run lint
- CI=1 npm run test -- --run
- npm run build

Next note:

- The new `issue-279-mcp-hardening` e2e spec is the right first stop if a future change regresses Desktop Settings copy, auth-free MCP access, payload redaction, or the closed-window `project-unavailable` behavior. If it fails locally because port `127.0.0.1:3846` is already occupied, clear the stale local Electron instance before trusting any Desktop MCP result.

### 2026-06-17 - Issue #280: Add human-verifiable MCP smoke flow and final documentation polish

Implemented:

- Selected `#280` as the single remaining MCP v1 feature after checking parent PRD `#269`, `CONTEXT.md`, and the existing `progress.txt` notes. This slice stayed on the agreed v1 surface only.
- Removed the out-of-contract `read_resource` compatibility tool from `desktop/mcpServer.cjs`, so Desktop Arcade MCP again publishes exactly the two v1 tools from the PRD: `capture_preview_evidence` and `apply_changes`.
- Tightened the Desktop MCP operating/capabilities resources around the real v1 contract:
  - discovery now points clients at `tools/list`, `resources/list`, and `resources/read`;
  - the default mutation loop keeps diagnostics/evidence checks as a fallback unless the human asked for a different workflow;
  - `capture_preview_evidence({ pageId })` stays the normal inspection path while `select_active_page` stays human-facing coordination only;
  - Arcade source is explicitly documented as `arcade://...` resources plus `apply_changes`, not repository/filesystem editing.
- Added a user-facing Desktop MCP v1 setup + smoke checklist to `README.md` with the exact server name, type, URL, no-token/header requirement, the two-tool contract, the stable resources, the batch temp-ref workflow, the Preview evidence reads, and the Web/Desktop boundary checks.
- Polished the Desktop Settings ActionMenu copy from **Copy type** to **Copy transport type** for clearer MCP setup wording.
- Added the final end-to-end smoke coverage in `tests/e2e/desktop-mcp-smoke.spec.ts`, then updated the existing MCP tests (`desktop-mcp-preview-capture`, `issue-279-mcp-hardening`, `desktopMcpServer`, and `desktop-mcp-settings`) so the automated loop now matches the final two-tool contract.

Human-testable verification notes:

- The new Electron/Web smoke spec now covers the practical happy path the issue asked for: exact MCP config, two-tool listing, stable resource reads, manifest/source reads, one batch create/link change via temp refs, diagnostics after the change, targeted Preview capture for a specific page id, reading screenshot/accessibility/DOM-layout-style/frame resources, safe Settings activity metadata, and the Web no-MCP boundary.
- Also did a live Web browser spot-check at `http://127.0.0.1:4173/aksel-arcade/`: Share was present, Settings had no Desktop MCP section, and the browser console stayed free of warnings/errors.

Verification:

- npm install
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run build
- npm run test:e2e -- tests/e2e/desktop-mcp-smoke.spec.ts tests/e2e/desktop-mcp-preview-capture.spec.ts tests/e2e/issue-279-mcp-hardening.spec.ts --reporter=line

Next note:

- If a future MCP host cannot use `resources/list` / `resources/read`, treat that as a new product-capability discussion instead of reintroducing a third helper tool. Keep `README.md` and `arcade://desktop/capabilities` aligned with the exact published two-tool v1 surface.

### 2026-06-17 - Issue #280 follow-up: clarify tool-only MCP host limits and rerun the real smoke loop

Implemented:

- Followed up after two external Copilot-session retests showed the same pattern: the Desktop MCP server itself was behaving correctly, but those particular agent surfaces exposed only the two server tools and did not surface `resources/list` / `resources/read` or Desktop/Web chrome to the agent.
- Kept the v1 contract unchanged — still exactly two listed tools — and instead tightened the issue-280 verification guidance where it belongs:
  - `arcade://desktop/operating-guide` now says the full smoke loop requires a client (or manual inspector) that can read MCP resources.
  - `arcade://desktop/capabilities` now publishes an explicit `smokeChecklistRequirements` note for tool-only hosts.
  - `README.md` now warns that tool-only MCP hosts can exercise `apply_changes` / `capture_preview_evidence` but cannot complete the stable-resource, diagnostics, or evidence-resource read portions of the full smoke checklist from MCP alone.
- Re-ran the direct human-style smoke path against the actual app surfaces instead of treating MCP-only agent sessions as a substitute for the requested manual loop:
  - real Desktop Arcade + MCP HTTP checks for server identity, no-auth access, two-tool listing, stable resource reads, manifest/source reads, durable batch source/page change, diagnostics, targeted capture, evidence-resource reads, non-active-page capture preservation, and safe Settings activity;
  - live Web Arcade browser check for Share visibility, no Desktop MCP section in Settings, and no MCP endpoint behavior.

Human-testable verification notes:

- The direct Desktop/Web smoke loop passes when run through a resource-capable MCP client plus the app UI, which is the environment the issue-280 checklist is written for.
- Tool-only agent hosts can still validate the two v1 tools and some state transitions, but they are not a complete substitute for a human/resource-capable client when the checklist explicitly includes resource reads and Desktop/Web UI checks.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm test -- --run tests/unit/services/desktopMcpServer.test.ts
- npm run test:e2e -- tests/e2e/desktop-mcp-smoke.spec.ts --reporter=line
- Direct Web browser spot-check at `http://127.0.0.1:4173/aksel-arcade/` for Share visibility, no Desktop MCP Settings section, and no browser-console warnings/errors

Next note:

- If a future issue wants MCP-only autonomous verification in tool-only hosts, that is a product-surface expansion discussion. The current v1 contract still intentionally relies on standard MCP resource reads plus human-visible Desktop/Web UI for the full smoke checklist.

### 2026-06-17 - Issue #280 follow-up: stabilize repeat Desktop MCP smoke runs

Implemented:

- Tightened the Desktop MCP end-to-end specs so the smoke loop no longer assumes a pristine one-page starter project. The Desktop MCP smoke, apply-changes, preview-capture, and hardening flows now derive page ids and source URIs from the live manifest, preserve whatever valid open Arcade project is already loaded, and assert only the behavior the v1 contract actually guarantees.
- Confirmed the earlier contradictory reruns were caused by a stale local Electron process still owning `127.0.0.1:3846`, which let the visible Desktop window and the MCP endpoint drift onto different app instances. Cleared the stale listener and reran the full Desktop MCP loop against a single MCP owner.
- Re-ran the real Desktop/Web verification path after that cleanup: the Desktop MCP batch-mutation path updates the visible preview again, targeted Preview evidence reads remain stable, non-active-page capture still preserves the active visible page, safe Settings activity remains redacted, and Web Arcade still exposes no MCP surface.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run test:e2e -- tests/e2e/desktop-mcp-smoke.spec.ts tests/e2e/desktop-mcp-preview-capture.spec.ts tests/e2e/desktop-mcp-apply-changes.spec.ts tests/e2e/issue-279-mcp-hardening.spec.ts --reporter=line

Next note:

- If Desktop Settings reports the MCP endpoint as unavailable because port `127.0.0.1:3846` is already in use, clear the stale local Electron listener before trusting any Desktop MCP smoke result. The issue-280 smoke specs now tolerate an existing valid Arcade project, but they still require a single live Desktop MCP owner.

### 2026-06-17 - PR #291 review follow-up: preserve Desktop MCP read compatibility

Implemented:

- Restored `read_resource` as an unlisted Desktop MCP compatibility alias so pre-#280 tool-only clients do not break on `tools/call`, while `tools/list` still publishes exactly the two v1 tools required by issue #280.
- Added unit coverage that proves the alias remains callable without reappearing in the advertised v1 tool surface.
- Tightened the Desktop MCP guide/capabilities wording and README smoke guidance so they describe the published two-tool discovery surface accurately without implying the hidden compatibility bridge is part of the new v1 contract.

Verification:

- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run test:e2e -- tests/e2e/desktop-mcp-smoke.spec.ts tests/e2e/desktop-mcp-preview-capture.spec.ts tests/e2e/desktop-mcp-apply-changes.spec.ts tests/e2e/issue-279-mcp-hardening.spec.ts --reporter=line
- Direct Web browser spot-check at `http://127.0.0.1:4173/aksel-arcade/` for clean load and no browser-console errors

Next note:

- If we ever want to remove the hidden `read_resource` bridge, treat that as an explicit compatibility change with a migration plan instead of a silent contract break under the same Desktop MCP v1 surface.

### 2026-06-17 - PR #293: self-teaching Desktop Arcade MCP + spaced-component URI fix

Implemented:

- Made the Desktop Arcade MCP server self-teaching so a zero-knowledge agent learns the Arcade mechanics on connect and pulls Aksel component docs/snippets on demand, without preloading them or narrowing what it thinks Arcade is for:
  - Added always-on `initialize.result.instructions` covering only the mechanics it cannot infer (import-free authoring, `goToPage` navigation, app-assigned page ids + `{{pageRef}}` ordering, the apply -> diagnostics -> capture loop), pointing to the authoring guide.
  - Added version-matched, on-demand Aksel snippet resources generated from `src/data/akselCatalog.ts` (single source of truth): `arcade://aksel/catalog` (compact index, v8.11.0, 59 components) and `arcade://aksel/components/{name}` (runnable, import-free snippet + props).
  - Added `arcade://desktop/apply-changes-operations` per-operation field-matrix reference to remove the schema-mismatch friction agents hit, and rewrote the authoring guide with open framing and wiring-only snippets.
  - Generated `desktop/akselCatalogData.generated.cjs` via `npm run aksel:refresh-mcp-catalog`, wired into `desktop:build`; added unit/drift/parity/e2e tests and ADR 0017.

Review fix (caught during PR review):

- Fixed two catalog components whose names contain a space ("Chips Toggle", "Chips Removable"). They advertised `resourceUri` values with a raw space, but the component dispatch pattern only accepted `[A-Za-z0-9.]`, so reading those exact advertised URIs fell through to `resource-not-found` and the case-insensitive fallback never ran (2 of 59 components unreachable).
- Percent-encode component names when building `resourceUri` (`Chips Toggle` -> `arcade://aksel/components/Chips%20Toggle`), widened the dispatch pattern to accept the encoded (and literal-space) form, and decode the captured name before lookup (safe against malformed escapes).
- Added regression guards: a builder test asserting every advertised `resourceUri` round-trips and matches the dispatch pattern, and an HTTP-level test that reads a spaced-name component through its exact advertised URI and expects success.

Verification:

- npm run type-check
- npm run lint
- CI=1 npm test (573 passed)
- npm run desktop:build
- Live in-process MCP server smoke check (initialize instructions, listed resources, catalog v8.11.0/59 components, clean Button snippet, spaced-component read, unknown-component resource-not-found)
- All PR CI checks green (CodeQL, Analyze actions/javascript-typescript, Plan Desktop packaging, Package macOS DMGs, Package Windows NSIS); squash-merged.

Next note:

- The Aksel snippet resources are derived from `src/data/akselCatalog.ts`; after any catalog/Aksel-version bump, re-run `npm run aksel:refresh-mcp-catalog` (also runs during `desktop:build`) so the committed `desktop/akselCatalogData.generated.cjs` stays in sync, or the drift test will fail.

### 2026-06-17 - Desktop MCP replacement-workflow guardrails and Preview navigation fix

Implemented:

- Turned the failed MCP-agent retest into concrete Desktop MCP guardrails for zero-knowledge agents:
  - promoted `read_resource` from hidden compatibility behavior to a listed MCP tool so tool-only hosts can discover/read `arcade://` resources instead of guessing from tool schemas;
  - added focused guidance resources: `arcade://desktop/start-here`, `arcade://desktop/workflows/replace-project`, and `arcade://desktop/workflows/multi-page-navigation`;
  - updated `initialize` instructions, `arcade://desktop/operating-guide`, `arcade://desktop/authoring-guide`, `arcade://desktop/capabilities`, `arcade://desktop/apply-changes-operations`, and README so they no longer describe the outdated two-tool/hidden-resource-reader contract.
- Improved `apply_changes` for coherent replacement work:
  - `{{pageRef:name}}` placeholders in source can now target any matching `create_page.newPageRef` in the same batch, so agents no longer need to create pages in reverse logical order just to link forward;
  - added optional final-state `assertions` (`pageCount`, `startPage`, `activePage`, `forbidImports`) so replacement tasks can fail before persistence if the agent leaves extra pages, picks the wrong entry page, or writes importful Arcade source;
  - added `postChangeSummary` to successful results so clients can immediately see final page count, ordered pages, start page, active page, and warnings.
- Added regression coverage for the exact wasteful replacement pattern: reuse existing pages first, create only the missing page, delete extras, keep the first logical page as Start/Active, and forbid imports.
- Hardened `public/sandbox.html` against the black-preview failure:
  - capture-phase guards now block raw/browser link navigation and form submits before the sandbox iframe can unload;
  - bare `pageNN` anchors are routed through the Arcade page runtime when valid;
  - blocked raw navigation surfaces a runtime diagnostic while keeping the visible Preview rendered.
- Cleaned up outdated raw-anchor examples in MCP tests/E2Es and replaced page-flow examples with `goToPage(...)` or Aksel Link/LinkCard bare page ids.

Verification:

- npm run type-check
- npm run lint
- npm test -- --run
- npm run build
- npm run test:e2e -- tests/e2e/desktop-mcp-smoke.spec.ts tests/e2e/desktop-mcp-apply-changes.spec.ts tests/e2e/desktop-mcp-preview-capture.spec.ts tests/e2e/desktop-mcp-aksel-resources.spec.ts tests/e2e/issue-279-mcp-hardening.spec.ts --reporter=line
- npm run test:e2e -- tests/e2e/desktop-mcp-apply-changes.spec.ts --reporter=line
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` with the app and sandbox rendered and no browser-console errors.

Next note:

- Desktop MCP E2E still requires a single owner of fixed port `127.0.0.1:3846`. During validation, an older `/Applications/Aksel Arcade.app` process was still listening on that port and made the first E2E run talk to the wrong MCP server; clearing that exact stale listener made the targeted Desktop MCP suite pass.


2026-06-17 - Arcade preview: surface cross-page compile errors + fix authoring-guide IIFE

Root-caused an MCP agent test failure: the agent built a 3-page form whose every page JSX was wrapped as `{(() => { ...hooks...; return (<Page/>) })()}` (an object-literal-wrapped IIFE, with hook calls inlined in JSX). That fails to compile (`Unexpected token`), and the broken preview was invisible/stale. Two real defects, neither "fixing the app to accept bad code":

1. The Desktop MCP authoring guide itself taught the broken pattern. The agent copied it.
2. PreviewPane hid compile errors whenever the active page != the error's page, so a project-wide compile failure showed the stale default project (or white siblings) with no error.

Implemented:
- desktop/mcpServer.cjs authoring-guide: documented the two-source-per-page contract (`jsx` is inlined into `return ( ... )`, so it must be a JSX element/parenthesized expression and is NEVER wrapped in `{ ... }`; state and hook calls go in the `hooks` tab and are hoisted into the page component). Replaced the `{(() => {...})()}` snippet with a Hooks-tab + plain-JSX example and a bare `(() => {...})()` for non-hook local JS, plus an explicit anti-pattern warning.
- src/services/agentBridge.ts: aligned the parallel IIFE rule (bare IIFE, never wrap JSX in braces, hooks in the Hooks tab).
- src/components/Preview/PreviewPane.tsx: compile errors now always surface (they break the whole combined module); runtime errors stay gated by the active page. Overlay now names the offending page (`Compile Error (line N) in "<page>"`) when it differs from the active page.

Verification:
- npm run type-check
- npm run lint
- CI=1 npx vitest run (578 passed, incl. new multi-page-preview-sync regression + desktopMcpServer guide guard)
- Browser smoke check at http://localhost:5173/aksel-arcade/: with the agent's broken project on page02 active, the overlay now shows "Compile Error (line 1) in \"Step 1\"" instead of a stale/white preview.


2026-06-17 - Issue #303: Correct Global-config-hooks guidance and add an invalid-hook-call diagnostics hint

Selected `#303` as the highest-priority `ready-for-agent` slice from `#300`-`#303` after reading `#304`, `CONTEXT.md`, and the existing `progress.txt` notes, because the PRD calls the invalid-hook guidance problem the single most-cited agent pain point and it improves both first-try authoring and recovery from the same mistake.

Implemented:
- Tightened the three MCP guidance surfaces that are always or commonly read first:
  - `desktop/mcpGuidanceResources.cjs` `arcade://desktop/start-here` now states that page-level hook calls belong in the page Hooks tab, while Global config `hooks` is module scope for defining shared custom hooks, helpers, constants, and components only.
  - `desktop/mcpServer.cjs` `initialize` instructions now carry the same define-don't-call rule in the connect-time text.
  - `desktop/mcpServer.cjs` `arcade://desktop/authoring-guide` now mirrors the same correction and explicitly says Global config `hooks` stays at module scope rather than becoming a page component.
- Tightened the per-component MCP snippet guidance in `desktop/mcpServer.cjs` so component resources no longer say `hooks` can go in "page Hooks (or Global config)"; they now direct hook code to the page Hooks tab and reserve Global config for shared definitions only.
- Added `src/services/runtimeDiagnosticHints.ts`, a small pure hint mapper that recognizes React's stable `Invalid hook call` runtime message and returns Arcade-specific recovery advice.
- Wired the hint into `src/services/desktopMcpProjectResources.ts` so `arcade://project/diagnostics` now adds a `hint` field on matching runtime issues without changing unrelated diagnostics behavior.
- Added regression coverage in `tests/unit/services/runtimeDiagnosticHints.test.ts`, `tests/unit/services/desktopMcpProjectResources.test.ts`, and `tests/unit/services/desktopMcpServer.test.ts` for the new hint plus the corrected start-here / initialize / authoring-guide / component-resource wording.

Verification:
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run build
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell and sandbox preview rendered and no browser-console errors/warnings

Next note:
- `#300` can now add the single-page stateful-flow + sandbox-constraints guidance on top of the same MCP wording without revisiting the invalid-hook rule; keep the new "Global config hooks is module scope" phrasing consistent across any remaining MCP guidance edits.


2026-06-17 - PR #306 review follow-up

Fixed two real review findings before merge:
- Narrowed the Desktop MCP guidance wording in `desktop/mcpGuidanceResources.cjs` and `desktop/mcpServer.cjs` so it matches the actual transpiler contract: only page-level **top-level hook bindings** (for example `const [value, setValue] = useState(...)`) are hoisted into the page component, not arbitrary bare hook calls.
- Softened `src/services/runtimeDiagnosticHints.ts` so the invalid-hook-call hint no longer misdiagnoses every match as a Global config problem. It now explains that the failure often means a hook is still running at module scope (for example Global config or a bare page Hooks statement) and points agents at page-level top-level bindings or a custom hook defined in Global config and called from a page.
- Updated `tests/unit/services/runtimeDiagnosticHints.test.ts`, `tests/unit/services/desktopMcpProjectResources.test.ts`, and `tests/unit/services/desktopMcpServer.test.ts` to cover the more precise guidance and the softened hint text.

Verification:
- CI=1 npm test -- --run tests/unit/services/runtimeDiagnosticHints.test.ts tests/unit/services/desktopMcpProjectResources.test.ts tests/unit/services/desktopMcpServer.test.ts
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run build
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell and sandbox preview rendered and no browser-console errors/warnings


2026-06-17 - Issue #302: Make Aksel component resolution forgiving while keeping the catalog index lean

Selected `#302` as the highest-priority `ready-for-agent` slice from `#300`-`#302` after reading `#304`, `CONTEXT.md`, and the existing `progress.txt` notes, because component-name dead ends break the core Desktop MCP authoring loop more directly than the docs-only stateful-flow slice (`#300`) or the legacy agentBridge cleanup slice (`#301`).

Implemented:
- Extended `scripts/lib/akselMcpCatalog.ts` and regenerated `desktop/akselCatalogData.generated.cjs` so the committed MCP artifact now carries resolver metadata for known aliases and hidden-root replacements without adding per-entry keyword bloat to the public `arcade://aksel/catalog` index.
- Updated `desktop/mcpServer.cjs` so `arcade://aksel/components/{name}` now:
  - returns the canonical snippet payload for exact and alias hits (`RadioGroup` now resolves to `Radio`);
  - returns sanctioned replacement breadcrumbs for intentionally hidden roots (`Alert` -> `InlineMessage` / `LocalAlert` / `GlobalAlert`, `Modal` -> `Dialog`, `Dropdown` -> `ActionMenu`);
  - returns a structured `did-you-mean` near-match list for unknown names instead of a bare resource-not-found error.
- Added the one-line authoring-guide nudge to read `arcade://aksel/catalog` before guessing a component name.
- Added regression coverage in `tests/unit/akselMcpCatalog.test.ts`, `tests/unit/services/desktopMcpServer.test.ts`, and `tests/e2e/desktop-mcp-aksel-resources.spec.ts` for alias resolution, hidden-root replacements, near-match suggestions, and the new authoring-guide wording.

Verification:
- npm run typecheck
- npm run lint
- npm run test -- --run
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell rendered and no browser-console errors
- Ephemeral MCP runtime spot-check via `createDesktopMcpServer({ port: 0 })` covering exact (`Button`), alias (`RadioGroup`), hidden-root (`Alert`), and suggestion (`Buton`) resolution paths

Next note:
- A separate Desktop Arcade process was already listening on the fixed MCP port `127.0.0.1:3846`, so a shared-port Playwright Desktop MCP E2E can talk to the wrong local app unless that exact stale listener is cleared first. The resolver logic itself was verified against a fresh port-0 MCP server instance.


2026-06-17 - PR #307 review follow-up

Fixed one PR-scoped review issue before merge:
- Updated `desktop/mcpServer.cjs` so hidden-root replacement breadcrumbs are resolved by root name, not only by the full requested component name. Requests such as `Dropdown.Menu` and `Modal.Header` now return the sanctioned replacement payload for `Dropdown` / `Modal` instead of falling back to generic near-match suggestions.
- Extended `tests/unit/services/desktopMcpServer.test.ts` and `tests/e2e/desktop-mcp-aksel-resources.spec.ts` with descendant-name coverage for `Dropdown.Menu` resolving to `ActionMenu`.

Verification:
- npm run typecheck
- npm run lint
- npm run test -- --run
- Ephemeral MCP runtime probe via `createDesktopMcpServer({ port: 0 })` confirming `arcade://aksel/components/Dropdown.Menu` returns a replacement resolution with `ActionMenu`


2026-06-18 - Issue #300: Document the single-page stateful-flow pattern and Preview sandbox constraints

Selected `#300` as the highest-priority `ready-for-agent` slice from `#300`-`#301` after reading `#304`, `CONTEXT.md`, and the existing `progress.txt` notes, because the remaining open work here still affects first-read agent authoring guidance directly, while `#301` is now primarily legacy cleanup after the sharper MCP guidance/resolution pain points landed in `#302` and `#303`.

Implemented:
- Added a short page-independence clause to `desktop/mcpGuidanceResources.cjs` `arcade://desktop/start-here`, so the first-read guide now says Arcade pages do not share React state and stateful flows should stay on one page.
- Updated `desktop/mcpServer.cjs` `arcade://desktop/authoring-guide` with a one-line sandbox-constraints note (`localStorage`/`sessionStorage` unavailable, external `fetch` blocked) plus a generic single-page step-flow recipe that keeps `useState` in page-local hooks and composes `FormProgress`, `Stepper`, and `FormSummary` as wiring rather than a pre-baked form template.
- Reconciled `docs/prd/0030-multi-page-support.md` so multi-page is framed as movement between distinct screens, while stateful step flows that need shared in-memory data stay on one page.
- Extended `tests/unit/services/desktopMcpServer.test.ts` to assert the new start-here page-independence wording and the authoring-guide sandbox/step-flow guidance.

Verification:
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- CI=1 npm test -- --run tests/unit/services/desktopMcpServer.test.ts
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell + preview rendered and no browser-console warnings/errors

Next note:
- `#301` should stay a separate cleanup-only pass. If that removal work later touches the MCP guidance surfaces, keep the new single-page stateful-flow phrasing aligned between `arcade://desktop/start-here`, `arcade://desktop/authoring-guide`, and the reconciled `docs/prd/0030-multi-page-support.md` text instead of broadening this slice again.


2026-06-18 - PR #308 review follow-up

Fixed one PR-scoped review issue before merge:
- Tightened the new single-page step-flow example in `desktop/mcpServer.cjs` so it no longer renders a standalone `Stepper` under `FormProgress`. The guide now wires `FormProgress` itself with `onStepChange={setActiveStep}` and explicitly tells agents to swap the compact `FormProgress` block for `Stepper` only when they want that alternate presentation, instead of rendering both at once.
- Extended `tests/unit/services/desktopMcpServer.test.ts` with a regression assertion for the new "instead of rendering both at once" guidance, so the served authoring-guide text keeps the example non-duplicative and non-misleading.

Verification:
- CI=1 npm test -- --run tests/unit/services/desktopMcpServer.test.ts
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell + preview rendered and no browser-console warnings/errors


2026-06-18 - Issue #301: Remove the legacy in-app Agent bridge

Selected `#301` after reading `#304`, `CONTEXT.md`, and the existing `progress.txt` notes because the remaining work here was a cleanup-only slice: delete the unused in-app Agent-session bridge stack while keeping the Desktop MCP path and its diagnostics coverage intact.

Implemented:
- Removed the legacy Agent-session runtime files from the renderer and Desktop host (`src/services/agentBridge.ts`, `src/hooks/useAgentSession.ts`, `src/components/Header/AgentSessionMenu.tsx`, the lifecycle/transport helpers, `desktop/agentLoopbackTransport.cjs`, and `desktop/agentHandoffRedaction.cjs`) and stripped their wiring from `desktop/main.cjs`, `desktop/preload.cjs`, `src/services/shellCapabilities.ts`, `src/components/Header/AppHeader.tsx`, and `src/hooks/useProject.tsx`.
- Deleted the obsolete bridge/transport/parity regressions, rewrote `tests/integration/project-controls-layout.test.tsx` down to the remaining non-agent layout/import/fullscreen coverage, and trimmed `tests/integration/share-popover.test.tsx` plus `tests/unit/desktopMain.test.ts` / `tests/unit/services/shellCapabilities.test.ts` to the new no-Agent surface.
- Added the replacement regression in `tests/unit/services/desktopMcpProjectResources.test.ts` so sandbox render failures still surface through structured `arcade://project/diagnostics` output after the bridge removal.
- Removed the stale parity-test sentence from `docs/adr/0017-self-teaching-mcp-entry-point.md`; `README.md` did not need an issue-301 change because its current Agent wording already describes the MCP-only direction.

Verification:
- npm run typecheck
- npm run lint
- CI=1 npm test -- --run
- npm run build
- Browser smoke check at `http://127.0.0.1:4173/aksel-arcade/` with the app shell + preview rendered, no Agent control in the header, and no browser-console warnings/errors

Next note:
- Treat the in-app Agent-session bridge as intentionally removed. Follow-up work should extend the Desktop MCP resources/guides/tests directly instead of reintroducing renderer-to-main Agent transport helpers or parity coverage.


2026-07-01 - Issue #315: Add annotation project data lifecycle

Selected `#315` as the highest-priority `ready-for-agent` feature from `#315`-`#326` after reading parent PRD `#314`, `CONTEXT.md`, and ADRs `0020`, `0021`, `0023`, and `0024`. It is the unblocked annotation foundation, and the later human UI, package/share, MCP, target-resilience, and evidence-capture slices depend on it.

Implemented:
- Added an Agentation-compatible Arcade annotation schema with required `pageId`, lifecycle/status/thread fields, known `feedback` / `placement` / `rearrange` kinds, and no durable `_syncedTo` sync marker.
- Added reusable annotation lifecycle helpers for creating pending feedback annotations, page filtering, open/pending counts, edit-to-pending behavior, hard delete, page-scoped clear, status updates, and thread-compatible replies.
- Made `Project` own a top-level `annotations` collection, defaulted new/fresh imported projects to an empty set, and migrated stored current/legacy projects with missing annotations to `[]`.
- Validated stored annotation records for known kinds/statuses, page ownership, UUIDs, timestamps, thread shape, and exclusion of local Agentation sync bookkeeping.
- Deleted page-scoped annotations when deleting an Arcade page at the project-source service boundary.
- Added Agentation acknowledgement to `README.md`.
- Added focused unit coverage for annotation lifecycle behavior, storage validation/migration, non-feedback preservation, dead-target filtering inputs, page deletion cleanup, and README attribution.

Verification:
- `npm install`
- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- Browser smoke check at `http://127.0.0.1:5176/aksel-arcade/` with the app shell + preview rendered and no captured browser console/page errors.

Next note:
- `#316` can now build target resolution on top of `ArcadeAnnotation.pageId` plus the stored Agentation identity fields. Keep saved `x`/`y`/bounding boxes diagnostic-only; normal open/pending counts already accept dead-target filtering input.


2026-07-01 - PR #329 review follow-up for Issue #315

Fixed before merge:
- Added the missing `annotations: []` test fixtures that CI build surfaced after making `Project.annotations` required.
- Bumped `CURRENT_PROJECT_VERSION` to `3.0.0` for the annotation-aware project schema and added a v2 stored-project migration path that restores missing annotations to an empty set.
- Extended annotation lifecycle filtering with explicit hidden/visible target filtering inputs while keeping hidden-but-resolved annotations included in default open/pending counts.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm run test -- --run`


2026-07-01 - Issue #316: Resolve annotation targets through the sandbox boundary

Selected `#316` as the highest-priority `ready-for-agent` feature from `#316`-`#326` after reading parent PRD `#314`, `CONTEXT.md`, ADRs `0021`, `0023`, and `0024`, and confirming `#315` was closed. This is the foundational Annotation target-resolution slice that later Annotation mode UI, marker resilience, package/share, MCP annotation resources, and Preview evidence overlays depend on.

Implemented:
- Added `src/services/annotationTargets.ts` as the stable target-resolution module for point/click, drag rectangle, saved identity, and multi-element group requests.
- Resolution is identity-first: targets get a deterministic identity signature plus DOM path metadata; saved coordinates and bounding boxes are returned only as compatibility/diagnostic snapshot data.
- Added dead-target semantics for zero or ambiguous identity matches, hidden-target semantics for uniquely resolved but non-visible targets, selected-text capture anchored to the containing normalized element, and all-or-nothing group resolution.
- Extended the sandbox message contract with `RESOLVE_ANNOTATION_TARGET` / `ANNOTATION_TARGET_RESOLVED`, wired `public/sandbox.html` to resolve through the preview iframe, and exported the resolver through the checked-in Preview evidence sandbox bundle.
- Added a target-resolution regression matrix covering normalized point targets, selected text, repeated/ambiguous elements, hidden targets, empty drag no-ops, drag multi-select, and all-or-nothing groups.

Verification:
- `npm install`
- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- `npm run build:sandbox`
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` with the app shell + live preview rendered and no captured browser console/page errors.

Next note:
- `#317` can now build the Annotation mode shell against the sandbox target-resolution message contract without adding annotation IDs or data attributes to user-authored Arcade project source. Keep `x`/`y`/bounding boxes diagnostic-only and persist identity metadata from resolver output for later dead/hidden recovery.


2026-07-01 - PR #330 review follow-up for Issue #316

Fixed before merge:
- Tightened saved identity resolution so a matching DOM path can no longer mask multiple plausible identity matches; repeated identical targets now stay dead until unambiguous.
- Extended the target-resolution matrix with real Aksel `Button`, `TextField`, `Checkbox`, and `HStack` DOM so the #316 matrix covers common Aksel components/layouts instead of only generic HTML fixtures.
- Fixed the CI-only `tsc -b` overload issue in the Aksel `HStack` test fixture by using the explicit `as="div"` primitive prop.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run tests/unit/services/annotationTargets.test.ts`
- `npm run test -- --run`
- `npm run build:sandbox`
- `npm run build`
- Browser smoke check at `http://127.0.0.1:5173/aksel-arcade/` with the app shell + live preview rendered and no captured browser console/page errors.


2026-07-01 - Issue #317: Annotation mode preview-header shell

Selected `#317` as the highest-priority `ready-for-agent` feature from `#317`-`#326` after reading parent PRD `#314`, `CONTEXT.md`, and ADRs `0020`, `0023`, and `0024`. It is the first human-visible Annotation mode foundation and unblocks later creation, marker, persistence, MCP, and Preview evidence slices; blocker `#315` is closed.

Implemented:
- Added the host-side preview-header Annotation mode shell with an Aksel `Button`, verified `NotePencilIcon`, page-scoped open annotation count badge, and `Clear all` shell button visible only while Annotation mode is on.
- Made Annotation mode off by default, toggleable without changing project annotation data, and escapable with `Escape` when no annotation popover is open.
- Converted `InspectMode` to controlled state from `PreviewPane` so Annotation mode and Inspect mode are mutually exclusive in both directions.
- Added focused integration coverage for default/toggle state, `Clear all` visibility, Escape behavior, page-scoped zero/one/two-digit badge counts, and Inspect exclusivity.

Verification:
- `npm install`
- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- Browser verification at `http://127.0.0.1:5177/aksel-arcade/` for the #317 human checklist: off/default annotation toggle, active state, `Clear all` only while active, toggle-off behavior, Escape exit, Inspect/Annotation mutual exclusion both ways, zero/one/two-digit supplied badge counts, light and dark header states, and no captured console errors.

Next note:
- `#318` can wire annotation creation to the existing Annotation mode shell. Keep `Clear all` destructive behavior out until `#322`.


2026-07-01 - PR #331 design follow-up for Issue #317

Fixed before merge:
- Used the local Figma Dev Mode MCP for nodes `157:2724`, `157:2903`, `157:1726`, `157:1730`, and `157:3755` after the browser/Figma public fetch required login.
- Corrected the preview-header controls to match Figma: 24px `xsmall` icon-only header buttons, Trash icon Clear all control before the active annotation toggle, 12px top-right annotation badge, `10px` Source Sans 3 badge text, `var(--ax-bg-accent-strong)` badge background, and `var(--ax-bg-neutral-moderate-pressedA)` active annotation state.
- Aligned the fullscreen and Inspect header buttons to the same 24px row so the annotation controls sit in the Figma header layout instead of a mixed 24px/32px row.
- Updated regression coverage for icon-only annotation controls, Clear all placement, capped large badge counts, and xsmall fullscreen/Inspect buttons.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm run test -- --run`
- `npm run test:e2e -- preview-header-layout.spec.ts`
- Browser verification at `http://127.0.0.1:5177/aksel-arcade/` measuring rendered controls: annotation, Clear all, Inspect, and fullscreen buttons are 24x24; badge is 12x12 at the annotation button top-right; Clear all appears before the active annotation button.


2026-07-01 - PR #331 badge size follow-up for Issue #317

Fixed before merge:
- Re-read updated Figma node `157:2903` through the local Figma Dev Mode MCP after the badge was enlarged.
- Updated the annotation count badge to Figma's new values: 14px badge height/min width, `var(--ax-space-4)` inline padding, 12px Source Sans 3 text, 12px line height, and `0.048px` letter spacing.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm test -- --run tests/integration/annotation-mode-header-shell.test.tsx`
- `npm run test -- --run`
- Browser measurement at `http://127.0.0.1:5177/aksel-arcade/`: rendered badge measured 14px tall with 12px font, 12px line height, 4px inline padding, and 0.048px letter spacing.


2026-07-01 - PR #331 badge optical-centering follow-up for Issue #317

Fixed before merge:
- Wrapped the annotation badge count in an inner text span and applied a 0.5px vertical optical offset so the text center aligns with the 14px badge for both one- and two-digit counts.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm test -- --run tests/integration/annotation-mode-header-shell.test.tsx`
- `npm run test -- --run`
- Browser measurement at `http://127.0.0.1:5177/aksel-arcade/`: `9` and `12` both measured with equal top/bottom text overflow (`verticalDelta: 0`) inside the 14px badge, with horizontal centering still within subpixel rounding.


2026-07-01 - PR #331 annotation icon follow-up for Issue #317

Fixed before merge:
- Replaced the annotation toggle icon with the verified Aksel `ChatElipsisIcon`.

Verification:
- `npm run typecheck`
- `npm run lint`
- `npm test -- --run tests/integration/annotation-mode-header-shell.test.tsx`
### 2026-07-01 - Issue #318: Single-element annotation creation

Implemented:

- Selected #318 as the highest-priority `ready-for-agent` feature from #318-#326 because it is the first complete human annotation path and later annotation management, persistence, geometry, evidence, and MCP slices depend on it.
- Read issue #314, issue #318, `CONTEXT.md`, and ADRs 0021/0023/0024 before changing code.
- Added Annotation mode host/sandbox messages for hover/selection, sandbox-side hover/selected target overlays, and preview-interaction click interception while Annotation mode is active.
- Added the host add-annotation UI in `LivePreview` with Aksel `Popover`, `Textarea`, and buttons: target metadata/context, blank-submit disablement, Enter-to-save, Shift+Enter newline, Escape cancel, and project-backed `feedback` annotation creation.
- Rendered page-scoped open annotation markers only while Annotation mode is on, with the badge updating from the persisted project annotations and markers reappearing when mode is re-enabled.
- Extended project updates to persist annotation array changes and added integration coverage for the preview creation flow plus popover keyboard behavior.

Verification:

- npm install
- npm run typecheck
- npm run test -- --run
- npm run lint
- Browser/dev-server practical checks were attempted on `http://127.0.0.1:5177/aksel-arcade/`; this exposed and fixed a hidden-Popover anchor issue. Further Playwright iframe pointer checks were limited by sandbox/frame event tooling, so future work should manually re-check the full #318 Human verification checklist in a visible browser session, especially real pointer selection on an Aksel button/control and prototype navigation blocking.

Next note:

- #319 can build on the marker rendering and `editAnnotationComment`/`hardDeleteAnnotation` services, but should first confirm marker focus/tooltip/edit behavior against the visible browser because the current #318 slice only creates and displays markers.

2026-07-01 follow-up:

- Fixed two #318 regressions found during review:
  - The active-page annotation badge is now hidden when there are zero open annotations instead of showing `0`.
  - Annotation hover no longer crashes the app; the root cause was a host-side read of `iframe.contentWindow.scrollY`, which throws for Arcade's sandboxed opaque-origin iframe during hover rendering.
- Added regression coverage for the zero-badge state and for hover metadata from an iframe whose `contentWindow.scrollY` throws a `SecurityError`.
- Re-ran the practical browser flow on a dedicated dev server at `http://127.0.0.1:5188/aksel-arcade/`: zero badge hidden, Annotation mode hover keeps the app visible with no console/page errors, add popover opens, blank Save is disabled, Enter saves after Shift+Enter newline, badge increments to `1`, marker hides when Annotation mode is off, and marker reappears when Annotation mode is turned back on.
### 2026-07-01 - PR #332 annotation target follow-up

Fixed after user feedback:

- Broadened annotation target resolution from a semantic/Aksel allowlist to any visible rendered preview DOM element, so generic/custom `div` and `span` elements, Aksel layout wrappers (`Box`, `VStack`, `HStack`), `InlineMessage`, and text-containing inline elements can all be annotated.
- Kept exclusions for non-preview chrome and non-rendered implementation nodes (`script`, `style`, `template`, `html`, `body`, `display: none`, `display: contents`, hidden/collapsed visibility).
- Changed identity re-resolution to prefer the stored full DOM path before treating same-signature generic elements as ambiguous, which keeps persisted annotations usable for otherwise-identical custom elements.
- Updated `CONTEXT.md`, ADR 0021, PRD issue #314, and issues #318/#320/#321 so future annotation work does not reintroduce “meaningful/semantic-only” target wording.
- Regenerated `public/preview-evidence-global.js` so the sandbox iframe uses the same broad target resolver.

Verification:

- npm run typecheck
- npm run test -- --run
- npm run lint
- Browser probe at `http://127.0.0.1:5192/aksel-arcade/` confirmed Annotation mode resolves outer Box padding, VStack gap/body, InlineMessage root exposed area, InlineMessage non-strong text span, in-root custom `div`, and custom `span`, and opens the add-popover for the custom `div` without console errors.

Next note: target creation is now intentionally broad. Future drag/multi-select work should collect rendered preview DOM elements, not semantic-only or Aksel-only elements, while continuing to ignore preview chrome and non-rendered implementation nodes.

### 2026-07-01 - PR #332 annotation popover target label follow-up

Fixed after user feedback:

- Replaced the verbose add-popover target metadata with a short human label, e.g. `InlineMessage: Quick tip: Delete this...`.
- Removed the popover's nearby subtree/context text and raw CSS-class display; that detailed data still remains in the annotation target snapshot for persistence/agent use, but it is no longer shown to the human author while adding an annotation.
- Normalized clicks on decorative InlineMessage icon internals back to the InlineMessage root so they do not show as `Img` targets.
- Added regression coverage for the InlineMessage label and for InlineMessage icon internals resolving to the component root, then regenerated `public/preview-evidence-global.js`.

Verification:

- npm run typecheck
- npm run test -- --run
- npm run lint
- Browser check at `http://127.0.0.1:5193/aksel-arcade/` selected the default InlineMessage in Annotation mode and confirmed the add-popover shows `InlineMessage: Quick tip: Delete this...` without `Features: Two tabs`, raw `.aksel-*` classes, or the `Informasjon:` status prefix.

### 2026-07-02 - PR #332 annotation popover width follow-up

Fixed after user feedback:

- Set the add-annotation popover content to a 280px default width while preserving the existing viewport-safe max width behavior for narrow screens.

Verification:

- npm run typecheck
- npm run test -- --run
- npm run lint
- Browser check at `http://127.0.0.1:5194/aksel-arcade/` opened Annotation mode, selected a default preview target, and measured `.live-preview__annotation-popover-content` at `280px` with no console errors.
